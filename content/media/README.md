# Media Asset Inventory

This folder catalogs media assets needed for the website.

The binary files remain in `legacy-content/public/` until a new delivery stack imports them.

These markdown files define what assets exist, how they are used, and which ones still need cleanup or replacement.
