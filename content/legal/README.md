# Legal Content

This folder is the normalized destination for legal website content.

The legal markdown files in this folder are the active migration targets for imported legal copy.

The Google Workspace source mapping lives in `import.yaml` in this folder. Use `pnpm import:legal-content` from the repository root to refresh the markdown bodies while keeping each file's frontmatter metadata intact.
