---
artefact: demand-register
status: DRAFT
date: 2026-09-24
---

# Demand Register

| ID | Defect | Status | Blocks | From | What is required | Answer format | Why | Due | Raised by | Record |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| DEM-0001 | stale | OPEN | SRC-0003, and every requirement citing it | jan-henrik (as the owner of the hub concept) | an amendment to `website-information-architecture.concept.md`: strike the footer placement of contact (line 35) and give `/ueber-uns` its primary conversion (line 205), or say the decisions were wrong | an edited source document, or a decision that reverses DEC-0081 | the source is what a page brief is read from and it still says the opposite of what ships | — | DEC-0098 | CONF-0013, CONF-0014 |
| DEM-0002 | stale | OPEN | SRC-0004 | jan-henrik | a note on the v1.0 prototype saying which of its screens the v2.0 boards replaced | one paragraph in `concept/v1.0/README.md` | a reader cannot tell which parts of the prototype are still the reference | — | DEC-0098 | — |
| DEM-0003 | stale | OPEN | SRC-0005 | jan-henrik | the list of which blockers in the v1.0 notes are closed and which stand | the existing list, marked per row | the inventory's own rationale says it is "partly overtaken by events" and does not say where | — | DEC-0098 | — |
| DEM-0004 | unlocatable | OPEN | SRC-0006, and the 64 requirements citing it | jan-henrik | a re-export of the voice transcript with line breaks — one line per utterance or per speaker turn | the same text as a file with line terminators, committed beside the current one | 64 requirements — the largest group in the repository — rest on a source in which no position can be cited | — | DEC-0098 | — |
| DEM-0005 | incomplete | OPEN | the 18 requirements whose statement the transcript does not carry | jan-henrik | for each of the 18, the source that does state it, or a decision that states it | a source id and a position per requirement, or a DEC | the requirements cite a source that does not support them, which is worse than citing none | — | DEC-0098 | — |
| DEM-0006 | vague | OPEN | SRC-0006's unmeasured passages | jan-henrik | the measure behind "vernünftige Umleitungen", "gute Beschreibungstexte" and the Lighthouse category transcribed as "Dingsbumps" | a scale, a value and a unit per passage | a fit criterion cannot be derived from a qualifier | — | DEC-0098 | — |
| DEM-0007 | stale | OPEN | SRC-0007, NFR-WEB-0048 | the community-calendar maintainer | `docs/performance-budget.md` updated from FID to INP, with the INP threshold stated | an edited budget table with INP and its value | the website's interaction budget names a metric its adopted source does not contain | — | DEC-0098 | CONF-0021 |
| DEM-0008 | unauthoritative | WAIVED | SRC-0010 | jan-henrik (the accountable role at DP-03) | confirmation by the mandate holder of the pre-relaunch site, which has none | a statement from the originator | the pre-relaunch site names no author and carries no mandate | — | DEC-0098 | waived 2026-09-24 — the source is used only to look up facts about itself (its own URL inventory, its eTracker configuration) and the inventory already limits it to that. A waiver is a decision to proceed on thin evidence and is recorded as one |
| DEM-0009 | unlocatable | OPEN | SRC-0016, FUN-WEB-0178, CON-WEB-0079, Q-0057 | gtm | the glossary as a file in a repository — the import of Q-0057 — so that a position in it can be cited and checked | `@schafe-vorm-fenster/glossary` published, or the markdown committed | it is the one source not readable from this repository at all | — | DEC-0098 | — |
| DEM-0010 | unmandated | OPEN | CON-WEB-0034, CON-WEB-0053, CON-WEB-0054 | jan-henrik | the mandate and its clause for each of the three constraints whose source reads "platform default", "existing repo, convention" or "convention" | a decision record per constraint, or a named external mandate | the method blocks approval of a constraint with no mandate | — | DEC-0097 | — |
| DEM-0011 | unauthoritative | OPEN | FUN-WEB-0078, FUN-WEB-0105, FUN-WEB-0122, FUN-WEB-0123, FUN-WEB-0126, FUN-WEB-0152, FUN-WEB-0181, FUN-WEB-0191, NFR-WEB-0063, CON-WEB-0085 | jan-henrik | a source or a decision for each of the ten requirements that name no document, or whose named records do not carry the statement | a source id with a position, or a DEC, per requirement | ten requirements rest on "derived; convention" or on records that are silent on them | — | DEC-0097 | — **Partly answered 2026-09-25:** the `make-contact` clause is closed. It named FUN-WEB-0152, whose `make-contact` appeared in no decision record; DEC-0081's amendment of 2026-09-25 carries the four intent events, the per-channel dimension and the second goal on row 1. The nine other requirements stay OPEN |
| DEM-0012 | unauthoritative | OPEN | BUS-WEB-0015, CON-WEB-0033, CON-WEB-0061, CON-WEB-0080, FUN-WEB-0017, FUN-WEB-0125, FUN-WEB-0132, FUN-WEB-0135, FUN-WEB-0136, NFR-WEB-0048 | jan-henrik | for each of the ten, the source that states it — the cited document was read and does not | a source id with a position per requirement | the citation asserts support the source does not give, and nothing but reading it would have shown that | — | DEC-0097 | CONF-0013, CONF-0014, CONF-0021 |
| DEM-0013 | unauthoritative | OPEN | FUN-WEB-0179 | gtm | a decision on ADR-001's open question 2 ("Packaged or fetched at build time?"), whose undecided preference FUN-WEB-0179 states as a SHALL | a decision recorded in the ADR, or the requirement withdrawn | a requirement is carrying the weight of a sentence that says "is better than" | — | DEC-0097 | — |
| DEM-0014 | stale | OPEN | `concept/v2.0/README.md`, `specs/decisions/DEC-0079--place-search-by-name.md` | jan-henrik | the two dangling citations repointed: `CON-WEB-0007` and `FUN-WEB-0047` were retired by DEC-0093 and both documents still name them | an edit to each line, or a note that the citation is historical | an excerpt taken at those positions drags a citation that resolves to nothing | — | DEC-0097 | — |
| DEM-0015 | incomplete | OPEN | the `loc` field of every requirement whose source supports no position | the maintainer of @leafcutter-strict/library-schemas | an `UNKNOWN` branch on the locator's `loc` pattern, so that the method's own "the locator is UNKNOWN" can be expressed inside the contract | a schema change with the package version it lands in | the method requires the value and the contract's pattern forbids it; the same shape of gap as Q-0074 | — | DEC-0097 | CONF-0020 |
| DEM-0016 | unauthoritative | OPEN | FUN-WEB-0054, NFR-WEB-0024 | legal | a written legal determination that IP geolocation without storage needs no DPIA (or, if it does, the DPIA itself), naming the processing described in SRC-0005/006 | a dated written statement from legal: DPIA required yes/no, with the legal basis cited by article, and any condition attached to the storage-free design | the geolocation-driven relevance path and its privacy NFR cannot be approved while the legal position is unconfirmed | — | Q-0008 | — |
| DEM-0017 | unmandated | OPEN | CON-WEB-0075 | legal | a legal ruling on whether competitor-keyword landing pages and comparison pages are permissible in the intended form, with the mandate and clause it rests on | permitted / permitted with conditions / forbidden, per page type, each with the governing statute and clause and any wording conditions | CON-WEB-0075 cannot be approved, so the competitor-keyword and comparison page family stays unbuildable | — | Q-0009 | — |
| DEM-0018 | unauthoritative | OPEN | FUN-WEB-0033, FUN-WEB-0150, FUN-WEB-0151, CON-WEB-0069 | jan-henrik | a usage-rights clearance per testimonial for the five entries currently at usage_rights: unverified | the usage_rights field of each of the five entries flipped to the cleared value, with the clearing person and date recorded per entry | until cleared, the proof slots render empty and the claims they back are weakened (principle 4); launch itself is explicitly not blocked | — | Q-0014 | — |
| DEM-0019 | incomplete | OPEN | FUN-WEB-0041 | app team | a statement of which of the three counter figures (places, dates, updates today) the events-api /api/stats response actually carries, and the field name for each | a field-by-field mapping: counter figure -> response field name -> present/absent, against the pinned /api/stats schema | FUN-WEB-0041's counter module cannot render figures it has no field for | — | Q-0015 | — |
| DEM-0020 | incomplete | OPEN | FUN-WEB-0039 | gtm · media-echo | the audiences field on media-echo entries modelled (allowed audience IDs) and backfilled across existing entries (the same demand is recorded twice in the register, as Q-0019 and Q-0076) | a published model for audiences (ID list and its owner) plus the backfilled entries, so a scorer can read a non-empty array | w_job cannot score a media-echo entry against audience IDs while the field is empty | — | Q-0019 | — |
| DEM-0021 | unmandated | OPEN | CON-WEB-0026 | legal | a legal confirmation of whether the BFSG applies to this website given self-service sale to institutions rather than consumers, with the clause it turns on | applies / does not apply, with the BFSG clause and the sales-model fact it turns on, and the conformance level that follows | CON-WEB-0026's accessibility obligation, and the conformance level it demands, hang on whether the BFSG binds at all | — | Q-0021 | — |
| DEM-0022 | incomplete | OPEN | FUN-WEB-0183, CON-WEB-0081, CON-WEB-0082, FUN-WEB-0091, FUN-WEB-0092, CON-WEB-0039, NFR-WEB-0065 | envoy team | the DEC-0009 web component widget specification and delivery: CSS variable set, emitted events, spam handling (honeypot plus rate limit per DEC-0014), accessibility conformance statement, and a delivery date | a written widget contract: named CSS custom properties, event names with payloads, the spam mechanism, the conformance level tested against, and a calendar delivery date | the embedded registration and newsletter surfaces cannot be built or styled without the widget contract; the API half (POST /api/registration) is already measured as sufficient | — | Q-0022 | — |
| DEM-0023 | incomplete | OPEN | nothing that ships — enables Q-0071 option 1 | geo-api | a name search over the existing Typesense index (geo-api #130 unified search, #166 coordinate search), so a place can be found by typed name | a shipped endpoint plus its openapi entry: the name parameter, the matching behaviour, and what it returns for an uncovered place | nothing that ships waits on it since DEC-0079; it would let the committed covered-community index be retired and make Germany-wide finding by name (Q-0071 option 1) possible at all | — | Q-0025 | — |
| DEM-0024 | unauthoritative | OPEN | FUN-WEB-0043 | portalize | a confirmation from Portalize that the embed sets no cookies and introduces no consent duty, plus a place-filter parameter on the loader so the demo shows the searched place | a written cookie/consent statement for the embed (storage used, third-party calls), and the loader parameter name with its accepted values | FUN-WEB-0043's embedded demo cannot ship without a cookie-freedom statement, and without the filter it cannot show the place the visitor searched | — | Q-0026 | — |
| DEM-0025 | unauthoritative | OPEN | FUN-WEB-0158, FUN-WEB-0159 | jan-henrik | a review verdict on the drafted IA amendment for route /dein-ort/starten in go-to-market-os/concept/website-information-architecture.concept.md | accepted / accepted with changes / rejected on the named draft, recorded in the concept document itself | FUN-WEB-0158 and FUN-WEB-0159 rest on an IA entry that is still a draft awaiting review in the other repository | — | Q-0028 | — |
| DEM-0026 | incomplete | OPEN | FUN-WEB-0160, FUN-WEB-0161, FUN-WEB-0162, CON-WEB-0073, TS-WEB-0005 D1 | geo-api | a coordinate to administrative-hierarchy resolution in geo-api that returns county level; failing that, a decision to relax FUN-WEB-0160/0161/0162 and CON-WEB-0073 to state level | either a shipped endpoint with its openapi entry (input coordinate, output hierarchy down to county) or a written refusal that lets the requirements relax to state | without county resolution the relevance tiers 0-2 are unreachable before a place search | — | Q-0032 | — |
| DEM-0027 | vague | OPEN | NFR-WEB-0016, NFR-WEB-0058, NFR-WEB-0059, CON-WEB-0025 | brand/design | measured contrast ratios for colour world 3c (Lime, Violett, Himbeere) against the declared contrast targets, and the type scale in use verified against them | a table of foreground/background pairs with the measured contrast ratio, the target it is checked against, and pass/fail per pair and per type size | the accessibility fit criteria of NFR-WEB-0016, NFR-WEB-0058, NFR-WEB-0059 and CON-WEB-0025 cannot be evaluated without the measured ratios | — | Q-0034 | — |
| DEM-0028 | incomplete | OPEN | FUN-WEB-0041 | events-api | fields on events-api /api/stats for the number of places and for updates today, alongside the existing totalEvents, date bounds and quality counters | the two field names with their type and semantics in the /api/stats schema, plus the version in which they ship | under FUN-WEB-0041 those two counters are simply not rendered until the fields exist | — | Q-0037 | — |
| DEM-0029 | incomplete | OPEN | FUN-WEB-0042 | geo-api | caller-supplied radius (km) and maxResults parameters on the geoPoint search, replacing the server-side constant PROXIMITY_SEARCH_RADIUS_KM = 20 | the two parameter names with unit, default, permitted range and behaviour when omitted, in the openapi entry | FUN-WEB-0042's ~15 km relevance rule is approximated in the BFF by haversine over returned positions until the parameters exist | — | Q-0038 | — |
| DEM-0030 | unauthoritative | OPEN | CON-WEB-0029, CON-WEB-0036 | app team | confirmation, from an inspection of the eTracker account, that the app emits conversion completion under the same hub goal ids and that one secure code means one property shared by website and app | yes/no on each of the two points, evidenced by the account configuration (goal ids emitted by the app, property to secure-code mapping) | CON-WEB-0029 and CON-WEB-0036 assume one measurement property and one goal vocabulary across website and app | — | Q-0040 | — |
| DEM-0031 | incomplete | OPEN | FUN-WEB-0070 | app team | a public help URL contract (the URL pattern per article) and a destination for the 37 migrating support articles | the URL template with its slug rules, plus a mapping list for the 37 articles from current slug to target URL | until it exists /hilfe/{slug} redirects to the app root instead of per article (DEC-0047), so FUN-WEB-0070's redirect map stays coarse | — | Q-0041 | — |
| DEM-0032 | incomplete | OPEN | visual generation | design | specifications at implementable depth for context band, proof card and stream, live-module shells, header/navigation, footer, form wrapper and order-flow steps, plus a renders binding per component naming its content type | the design system extended with each named component at the same depth as the fourteen already specified, and a renders field per component naming the content type it renders (SRC-0013 §4) | visual generation cannot turn specs plus content into a page while seven page-level components and every renders binding are missing | — | Q-0044 | — |
| DEM-0033 | incomplete | ANSWERED | FUN-WEB-0033, FUN-WEB-0037 | gtm (task) | a usage_rights field on media-echo entries with the same values as proof (granted by DEC-0063), and the clearing judgement for each of the 32 entries | the field defined as on proof, plus a per-entry cleared value for all 32 entries | the field question is closed; the clearing work for 32 entries is still owed before the entries can be used as evidence | — | Q-0045 | DEC-0063 |
| DEM-0034 | incomplete | OPEN | FUN-WEB-0015 | portalize + envoy | confirmation of whether envoy's POST /api/registration yields a usable Portalize configuration and an id the embed code can carry; if not, the path that does | yes/no from both sides, with the id field name in the registration response and the Portalize configuration it produces (or the named alternative path) | it is a release blocker for FUN-WEB-0015: the 480 EUR order cannot complete into a working calendar without a minted organizer and an embeddable id | — | Q-0046 | — |
| DEM-0035 | incomplete | ANSWERED | FUN-WEB-0012, FUN-WEB-0150, FUN-WEB-0151, CON-WEB-0069 | gtm (task) | a proof element for the permanence promise, evidenced by the Nordkurier article | the minted proof element in the hub, with the Nordkurier article as its evidence and its usage rights recorded | the decision is made; the artefact itself is owed by the hub before the promise can render with proof | — | Q-0049 | DEC-0064 |
| DEM-0036 | incomplete | ANSWERED | FUN-WEB-0012 | gtm (task) | the six channels enumerated by name in the hub's messaging | the named list of six channels in the hub messaging artefact, citable from the page | FUN-WEB-0012's claim cannot name the channels until the enumeration exists; the artefact is still owed | — | Q-0050 | DEC-0064 |
| DEM-0037 | incomplete | OPEN | FUN-WEB-0158, FUN-WEB-0159 | geo-api | a yes/no from geo-api on whether community/search can return the nearest covered community for a place name it cannot resolve, and under which parameter | yes with the parameter and response shape, or no with the reason, against the pinned community/search spec | the core module of /dein-ort/starten (nearest active place) has nothing to measure from for an unknown place name | — | Q-0051 | — |
| DEM-0038 | unlocatable | OPEN | CON-WEB-0003 | design | the three files svf-mark-flat.svg, svf-mark-circle.svg and svf-mark-corner.svg shipped inside @schafe-vorm-fenster/brand-design, or the tokens re-pointed at what the package actually contains | a released package version containing the three named files at the paths the tokens point to, or corrected token values naming files that exist | CON-WEB-0003 binds the brand package, and logo.primary/circle/notched resolve to files that are not in it | — | Q-0054 | — |
| DEM-0039 | unauthoritative | OPEN | CON-WEB-0055, CON-WEB-0056, CON-WEB-0057 | design + gtm | the four category values plus unknown sourced from classification-api as the owning service, replacing the derivation from entre/app/clients/events-api/types/event.types.ts | the token entry re-sourced with classification-api named as origin and the PROVISIONAL marker removed, with the owning service's published list as the citation | the category tokens stay PROVISIONAL, and the constraints that bind the token set cannot be approved on a consumer copy | — | Q-0055 | — |
| DEM-0040 | incomplete | OPEN | FUN-WEB-0178, CON-WEB-0079 | jan-henrik | the glossary source document amended so Actor/Akteur is marked the outward word (copy, audience model) and Organizer the internal one (data model, CRM, APIs), per DEC-0062 | the amended source document with the two registers distinguished per term, ready for import | the document cannot be imported until it carries the distinction DEC-0062 added | — | Q-0056 | — |
| DEM-0041 | unlocatable | OPEN | FUN-WEB-0178, CON-WEB-0079, FUN-WEB-0089 | gtm | the glossary imported into the hub as @schafe-vorm-fenster/glossary by the legal-texts route (Google Doc, script, markdown, package), and specs/glossary/glossary.md given the word to use per locale and the words not to use | a published package version plus the markdown file carrying both columns per term | the requirements that cite the glossary have no citable artefact, and the content concept's two columns do not exist | — | Q-0057 | — |
| DEM-0042 | incomplete | OPEN | CON-WEB-0049, CON-WEB-0050, CON-WEB-0051, TS-WEB-0017 | the design system (the row's Addressee column is —; the owing party is named in the text) | a container width and an outer gutter per breakpoint, for all six breakpoints as the tokens define them | a six-row table: breakpoint name, container width in px, outer gutter in px | the breakpoint constraints are settled by name but the layout cannot be built without width and gutter per breakpoint | — | Q-0058 | — |
| DEM-0043 | unlocatable | ANSWERED | NFR-WEB-0039, NFR-WEB-0040, NFR-WEB-0047, TS-WEB-0003 | jan-henrik | a ruling on the two missing LCP photographs: keep the image LCPs and fill a missing asset with a generated placeholder at the declared aspect ratio | a decision keeping both image LCPs with the placeholder rule and its guardrail (a placeholder may occupy a slot, never assert anything) | the LCP element needed its geometry from day one; without a ruling the performance NFRs had no measurable subject | — | Q-0059 | DEC-0068/DEC-0069 |
| DEM-0044 | unauthoritative | ANSWERED | BUS-WEB-0015, FUN-WEB-0202, TS-WEB-0018 | jan-henrik | confirmation of the publishable-price predicate, including whether 4.000 EUR may appear as an 'ab' anchor | a confirmation or correction of the predicate, plus a named trigger for the unguarded withheld amounts | the pricing surfaces could not be built while it was unclear which amounts may be rendered | — | Q-0060 | DEC-0070 §1/§2 |
| DEM-0045 | unauthoritative | ANSWERED | FUN-WEB-0062, TS-WEB-0011 | jan-henrik | a decision on publishing a machine-readable Offer at 480 EUR and how net and scope travel with the figure | yes/no plus the required priceSpecification fields (valueAddedTaxIncluded, referenceQuantity unitText) | FUN-WEB-0062's structured data could not be emitted while the price's publishability was unconfirmed | — | Q-0061 | DEC-0069 §2 |
| DEM-0046 | unauthoritative | ANSWERED | CON-WEB-0026, TS-WEB-0002 | jan-henrik | the confirmed list of AAA success criteria adopted beyond AA | the adopted criteria named by number, with the rejected ones recorded rather than silently dropped | CON-WEB-0026's conformance target could not be fixed while the criteria list was a proposal (four proposed, eight adopted) | — | Q-0062 | DEC-0069 §3 |
| DEM-0047 | unauthoritative | ANSWERED | FUN-WEB-0041, TS-WEB-0025 | jan-henrik | a build/defer decision on the order preview and its BFF route for V1 | build or defer, with what remains in V1 (the scope configuration stays, the live preview does not ship) | the flow's only upstream dependency hung on it; the spec was kept intact in TS-WEB-0025 D4 either way | — | Q-0063 | DEC-0069 §8 |
| DEM-0048 | unauthoritative | ANSWERED | NFR-WEB-0052, NFR-WEB-0053, TS-WEB-0009 | jan-henrik | a decision on committing the generated resilience snapshots in src/generated/snapshots/ | yes/no on committing, naming the directory the payloads live in | without a committed file the keep-the-last-good-one rule has nothing to keep, so the tier-3 fallback was unbuildable | — | Q-0064 | DEC-0069 §6 |
| DEM-0049 | unauthoritative | ANSWERED | FUN-WEB-0002, TS-WEB-0004 | jan-henrik | a decision on reserving /deine-termine and what the route does until the widget is sellable | reserve/do not reserve, with the behaviour named (404, unlinked, absent from the sitemap) | FUN-WEB-0002's route inventory could not be closed while the name's status was open | — | Q-0065 | DEC-0071 §1 |
| DEM-0050 | incomplete | ANSWERED | FUN-WEB-0184, FUN-WEB-0185, FUN-WEB-0187, FUN-WEB-0152, CON-WEB-0083, TS-WEB-0016 | jan-henrik | the lead fallback address and how it renders | the named destination (the Google Form at /start), the treatment (linked never embedded, via our own /start redirect) and the recipient named beside the email address | the lead capture surfaces had no fallback destination while envoy's form does not exist | — | Q-0066 | DEC-0069 §7 |
| DEM-0051 | unauthoritative | ANSWERED | FUN-WEB-0105, TS-WEB-0003 | jan-henrik | confirmation of the fresh TTLs and the stale windows for dates and counters | the confirmed values with units per cache class (fresh TTL, stale window) and what the long window governs | FUN-WEB-0105's caching behaviour and the correction latency it implies rested on unconfirmed numbers | — | Q-0067 | DEC-0069 §4 |
| DEM-0052 | unauthoritative | ANSWERED | FUN-WEB-0149, CON-WEB-0068, TS-WEB-0005 | jan-henrik | confirmation of the context-proximity weights and of which of them are this spec's own | per value: the source that fixes it or the decision that adopts it (1.0 and 0.3 from SRC-0002; 0.6 and the 0.3 floor for unnamed types this spec's own) | the relevance model could not be fixed while its numbers were tagged proposed | — | Q-0068 | DEC-0069 §9 |
| DEM-0053 | unauthoritative | ANSWERED | NFR-WEB-0039, NFR-WEB-0040, NFR-WEB-0053, TS-WEB-0003 | jan-henrik | a decision on whether the JS byte budget fails a build or only reports | which check gates and which reports, and what happens to a route over budget with Lighthouse green | the performance NFRs' enforcement mechanism was undecided, so the pipeline could not be configured | — | Q-0069 | DEC-0069 §5 |
| DEM-0054 | unauthoritative | ANSWERED | TS-WEB-0015, TS-WEB-0014, TS-WEB-0029, TS-WEB-0028, TS-WEB-0021, TS-WEB-0010 | jan-henrik | confirmation of the eight low-stakes determinations so they can flip from [PROPOSED] to [FIXED] | confirmed/rejected per determination, with the ones not in the block (TS-WEB-0021 D9, TS-WEB-0010 D11) left explicitly at [PROPOSED] | six technical specs carried proposed determinations that nothing downstream could rely on | — | Q-0070 | DEC-0070 §3 |
| DEM-0055 | incomplete | OPEN | FUN-WEB-0186, FUN-WEB-0190, FUN-WEB-0191, CON-WEB-0085, CON-WEB-0086, TS-WEB-0016 D10, DEC-0052 §4 | envoy/ops | a receiving side for the WhatsApp newsletter route: N3 something that records an arriving opt-in as a subscription and adds it to the broadcast list, N4 an unsubscribe, N5 a confirmed-subscription count per channel | a named system or process per item (N3, N4, N5) with who operates it and from when, not an intention | until N3 exists the wa.me link is not built — it would collect opt-ins into an inbox and lose them — and the subscribe-to-newsletter goal cannot be read without N5 | — | Q-0073 | — |
| DEM-0056 | incomplete | OPEN | 420 acceptance criteria, the verification pyramid | library-schemas (the @leafcutter-strict/library-schemas maintainer) | a typed verification_level field on acceptance_tests items (static, unit, integration, e2e, tool, manual), or additionalProperties opened | a released schema version carrying the typed field (or opened additionalProperties); the handover document specs/questions/demand-acceptance-test-verification-level.md states the contract, the conflict and the proposed change | 420 acceptance criteria carry DEC-0040's level outside the contract; putting it in then makes typed data free text, putting it in id makes re-levelling a renumber | — | Q-0074 | — |
| DEM-0057 | incomplete | OPEN | FUN-WEB-0010–0018 | leafcutter-strict (the @leafcutter-strict/method-statement-grammar maintainer) | a mapping form in @leafcutter-strict/method-statement-grammar that can carry a route, focus job and primary conversion tuple as one statement | a released method version defining the mapping form and its slots, after which the nine migrate mechanically | FUN-WEB-0010 to FUN-WEB-0018 stay at form F0 with the reason recorded; flattening each row into three sentences makes 27 artefacts that hide the mapping, and inventing a form is a governance change | — | Q-0075 | — |
| DEM-0058 | contradictory | OPEN | every SDR-####-####-#### this repository writes, CONF-0023 | the maintainer of @leafcutter-strict/library-schemas | the `decision-record` schema's id pattern widened to `^SDR-\\d{4}-\\d{4}-\\d{4}$`, so that two decisions taken on the same day have two identifiers | a schema change with the package version it lands in | `@leafcutter-os/schemas` already types three groups; the two installed contracts cannot both be satisfied by one id, and the narrower one cannot give a second decision on a day its own identifier | — | DEC-0100 | CONF-0023 |
| DEM-0059 | contradictory | OPEN | every SDR recording an AGENT_BOUNDED decision, CONF-0024 | the maintainer of @leafcutter-os/schemas | `mode` widened from `['HUMAN', 'AGENT']` to the four modes `library-schemas` enumerates, so that a bounded agent row can be recorded | a schema change with the package version it lands in | POL-GRADED-BY-IMPACT binds six decision points to `AGENT_BOUNDED` at low impact and the narrower enum cannot express that row | — | DEC-0100 | CONF-0024 |
| DEM-0060 | stale | OPEN | SRC-0008, and every locator taken into the hub's goal and audience files | jan-henrik (as the owner of the hub) | the SRC-0008 inventory row's paths corrected: it names `audiences/` and `strategy/ (business-goals, conversion-goals)` and the hub has moved both under `packages/market/` | the corrected paths in the source-inventory row, or a note that the row names a logical area rather than a path | the registered source names directories that no longer exist, so a reader cannot follow the row to the file a locator cites | — | DEC-0101 | — |
| DEM-0061 | stale | ANSWERED | GOAL-WEB-0010, GOAL-WEB-0011, and the 39 citations of `make-contact` and `subscribe-to-newsletter` in this specification | jan-henrik (as the publisher of `@schafe-vorm-fenster/goals`) | `@schafe-vorm-fenster/goals` published at the version that carries `make-contact.conversion-goal.md` and `subscribe-to-newsletter.conversion-goal.md`, and the dependency moved onto it | the published version number, after which `pnpm update` resolves both files | the installed package is 0.3.0 and holds neither file; the hub repository at 0.3.3 holds both, so this specification cites two hub ids that the installed package cannot resolve | — | DEC-0101 | — — **Answered 2026-09-25:** 0.3.3 was published and is `latest`; the range `^0.3.0` already admitted it, only the lockfile was stale. `pnpm update` moved the install 0.3.0 → 0.3.3, and both goals are in `conversion-goals/`. |
| DEM-0062 | incomplete | OPEN | NEED-WEB-0001 … NEED-WEB-0034 | jan-henrik (the accountable role at DP-01, `@domain-requirements-engineer`) | a ranking of the thirty-four needs — MoSCoW or an ordering — so that `priority` can carry a value instead of UNKNOWN | one priority letter per need id, or an ordered list of the thirty-four | `needSchema` requires `priority`; no source ranks one stakeholder's need against another's, and the SSD's audience order is an order of audiences per page rather than of needs | — | DEC-0102 | — |
| DEM-0063 | incomplete | OPEN | the 38 orphan requirements of content-pipeline, technical-constraints and delivery-pipeline, and SSD-WEB-0001 | jan-henrik (as `@steering-board`, since a change to the specification document is DP-07) | the supply-side stakeholders of this specification named in `SSD-WEB-0001` — who holds the position, in which role, with what mandate — so that a need can be written for the content pipeline, the build constraints and the delivery pipeline | the stakeholder ids added to the SSD's `stakeholders[]` with the role and mandate of each, or a statement that this specification deliberately recognises audiences only | the specification lists six stakeholders and all six are audiences; 38 requirements exist for the operator's sake and `method-chain-linkage` has nowhere to hang them, so they stay orphans | — | DEC-0103 | — |
| DEM-0064 | stale | OPEN | FUN-WEB-0136, and the `/ueber-uns` row of SRC-0003's conversion map | jan-henrik (as the owner of the hub concept) | the three further lines of `website-information-architecture.concept.md` that contradict landed decisions: line 207's "one primary conversion, visually unrivalled" against the repeat rung of DEC-0082 §1, the conversion map's missing `/ueber-uns` row (DEC-0081 §6), and the village argument as 280 inhabitants without the salesperson clause (DEC-0084 §2) | an edited source document, line by line, or a decision that reverses each | the specification now wins the disagreement (DEC-0104 §1), so the source is no longer corrected by the spec waiting — it is corrected by being edited, and until it is, a page brief read from it misleads | — | DEC-0104 | — |
| DEM-0065 | contradictory | OPEN | BUS-WEB-0017, FUN-WEB-0204, TS-WEB-0022 D11 | jan-henrik (as the owner of `@schafe-vorm-fenster/offerings`) | the publishing-path price boundary of DEC-0107 written into the offering records: `community-calendar` naming the standard sources that publish free — a WordPress plugin, a common council information system, an ICS feed — and `custom-data-integration` narrowed to the individual integration into a system the platform does not already support, since its Commercial Model currently puts "an ICS feed" inside the paid scope | the two offering records edited: the standard-source list in `community-calendar`'s publishing paths, and the ICS sentence removed from or qualified in `custom-data-integration` | the installed package contradicts the decision on ICS and is silent on the other two standard sources, so the hint banner on `/mitmachen` would state a boundary the offering documentation denies | — | DEC-0107 | — |
| DEM-0066 | unauthoritative | OPEN | FUN-WEB-0205, NFR-WEB-0061, NFR-WEB-0062, TS-WEB-0016 D15, CONF-0025 | legal | a written determination on whether the registration form embedded on one route, reached deliberately, creates a consent duty under the TTDSG and the GDPR — and, if it does, whether a consent gate or a different form host is the remedy | consent duty yes/no, with the clause it turns on; if yes, the permitted remedy named, since a click-to-load layer contradicts the "visibly embedded" the embed was decided with | the site's banner-freedom is a requirement and a published sales argument, and the embed is the only registration that works today — so both cannot simply be asserted | — | DEC-0013 | CONF-0025 |
