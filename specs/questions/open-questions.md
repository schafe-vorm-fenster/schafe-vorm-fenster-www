---
artefact: question-register
status: DRAFT
date: 2026-09-09
---

# Open Questions

| ID | Question | Blocks | Addressee | Origin |
| --- | --- | --- | --- | --- |
| Q-0001 | ~~International TLD~~ **Resolved by DEC-0035**: `sheepoutside.com`. Residual task: DNS wiring + add to the Vercel team. | FUN-WEB-0060 | — | DEC-0035 |
| Q-0002 | ~~Stage-0 weight redistribution?~~ **Resolved by DEC-0048** — 0.20 to time, 0.15 to job (`w_time 0.35 · w_ctx 0.25 · w_job 0.40`). | FUN-WEB-0149, CON-WEB-0068 | — | DEC-0048 |
| Q-0003 | ~~Proof element counts?~~ **Resolved by DEC-0048** — 3 inline · 5 home · 7 in the `/ueber-uns` stream. | FUN-WEB-0031 | — | DEC-0048 |
| Q-0004 | ~~May w_job outrank geo proximity?~~ **Resolved**: it may, and where is a property of the page — weights are a profile per focus job (TS-WEB-0005 D5). | FUN-WEB-0149, CON-WEB-0068 | — | TS-WEB-0005 D5 |
| Q-0005 | ~~Where does order-promotion-material live?~~ **Resolved by DEC-0052 §2** — deliberately unwired; the goal stays in the hub, no website page. | FUN-WEB-0139, CON-WEB-0060 | — | DEC-0052 |
| Q-0006 | ~~One sentence on local advertising, or none?~~ **Resolved by DEC-0052 §3** — none while the offering is withheld; the guard checks for absence. | CON-WEB-0015 | — | DEC-0052 |
| Q-0007 | ~~Newsletter placement?~~ **Resolved by DEC-0052 §4** — footer everywhere, plus inline on `/ueber-uns`. **Its reason was restated on 2026-09-24**: the original one ("the one page without a primary conversion") was spent by DEC-0081 §6, which gave that page the booking. The block stays, secondary and below the booking, because the two asks serve two readiness levels — ready to talk books, still looking subscribes (DEC-0082's ladder). Placement and treatment unchanged. | FUN-WEB-0140, FUN-WEB-0141, FUN-WEB-0137, CON-WEB-0061, FUN-WEB-0017 | — | DEC-0052 §4 as amended |
| Q-0008 | IP geolocation without storage: legally verified (DPIA)? | FUN-WEB-0054, NFR-WEB-0024 | legal | SRC-0005/006 |
| Q-0009 | Competitor-keyword landing pages and comparison pages: legally permissible in the intended form? | CON-WEB-0075 | legal | SRC-0006 |
| Q-0010 | ~~Language sets per country?~~ **Resolved by DEC-0053** — national language plus English per domain; further languages on real demand. | FUN-WEB-0068 | — | DEC-0053 |
| Q-0011 | ~~First-visit suggestion timing and scope?~~ **Resolved by DEC-0053** — after launch, language only; a country suggestion cannot keep the once-per-session guarantee across origins. | FUN-WEB-0168, CON-WEB-0074, CON-WEB-0076 | — | DEC-0053 |
| Q-0012 | ~~Where is "Portalize" introduced?~~ **Resolved by DEC-0052 §1** — once, on `/dein-kalender` at the 480 € tier. | CON-WEB-0058, FUN-WEB-0132 | — | DEC-0052 |
| Q-0013 | ~~Catamaran verified for accessibility?~~ **Superseded by DEC-0043** — the brand retired Catamaran for **Atkinson Hyperlegible Next**. Continues as Q-0034. | NFR-WEB-0016 | — | DEC-0043 |
| Q-0014 | Five proof testimonials are `usage_rights: unverified` — obtain clearances (content-phase task, prioritised). Launch is **not** blocked: the empty slot shows and the claim is weakened (principle 4). | FUN-WEB-0033, FUN-WEB-0150, FUN-WEB-0151, CON-WEB-0069 | jan-henrik | SRC-0005 |
| Q-0015 | ~~Which app API endpoints?~~ **Resolved by DEC-0021 / SRC-0011** — remaining: do the `events-api /api/stats` fields cover all three counter figures (places · dates · updates today)? | FUN-WEB-0041 | app team | SRC-0002, DEC-0021 |
| Q-0016 | Legacy URL inventory for the redirect map — extract from SRC-0010. | FUN-WEB-0070 | spec work | SRC-0006 |
| Q-0017 | ~~Invoicing process?~~ **Resolved by DEC-0051** — the order goes to envoy as a structured event, then into the existing accounting process; the website issues nothing. | FUN-WEB-0188, FUN-WEB-0189, CON-WEB-0084 | — | DEC-0051 |
| Q-0018 | ~~Website update trigger?~~ **Resolved by DEC-0050** — the hub's package publish fires a `repository_dispatch`; the workflow diffs and opens a PR. | FUN-WEB-0175, FUN-WEB-0176, FUN-WEB-0177 | — | DEC-0050 |
| Q-0019 | `audiences: []` field on media-echo entries — model and backfill so w_job can score against audience IDs. | FUN-WEB-0039 | gtm | SRC-0002 required data |
| Q-0020 | ~~Newsletter system?~~ **Resolved by DEC-0051** — envoy carries signup and double opt-in, as a row of the Q-0022 contract. **Stated plainly (2026-09-24): the system is chosen and no sending system is in operation.** Q-0022's measurement of 2026-09-11 records it — "No newsletter operation exists either" — and the widget's delivery date is still UNKNOWN (TS-WEB-0016 D4 C10). Choosing a system did not produce one, so **no signup ships on either route** until one exists and accepts a subscription (TS-WEB-0016-A21, DEC-0052 §4 as amended). The question does not reopen; what is owed is delivery, under Q-0022 for the e-mail route and Q-0073 for the WhatsApp route. | FUN-WEB-0186, FUN-WEB-0190, FUN-WEB-0191, CON-WEB-0085, CON-WEB-0086 | — | DEC-0051 |
| Q-0021 | BFSG applicability of the website (self-service sale to institutions vs consumers): legal confirmation. | CON-WEB-0026 | legal | DEC-0012 |
| Q-0022 | **Demand to envoy — narrowed 2026-09-11.** The *API* contract exists and is larger than assumed: `POST /api/registration` writes an organizer to the CRM and its payload already carries the three publishing paths (`googleCalendarId`, `icsCalendarUrl`, `onlineCalendarUrl`), the place (`zipcode` drives community assignment) and the commercial tier (`tarif`, `tarifStatus`) — so it covers both the actor registration and the order handover. What is still missing is the **web component widget** of DEC-0009: CSS variable set, emitted events, spam handling (honeypot + rate limit per DEC-0014), accessibility conformance, delivery date. No newsletter operation exists either (Q-0020). | FUN-WEB-0183, CON-WEB-0081, CON-WEB-0082, FUN-WEB-0091, FUN-WEB-0092, CON-WEB-0039, NFR-WEB-0065 | envoy team | DEC-0009, measured 2026-09-11 |
| Q-0023 | ~~Design system missing?~~ **Resolved by DEC-0056** — delivered as `concept/website-design-system.md` with the boards in `concept/v2.0/`; the visual gate is lifted. | visual generation | — | DEC-0056 |
| Q-0024 | ~~Region interim~~ **Resolved by DEC-0034** — active examples + counters + search; residual: events-per-place activity signal (folded into Q-0015/Q-0025 demands). | FUN-WEB-0148, CON-WEB-0065, CON-WEB-0066 | — | DEC-0034 |
| Q-0025 | **Demand to geo-api — re-scoped 2026-09-23 by DEC-0079; it no longer blocks the product.** The measurement stands: `community/search` in 3.1.3 takes `countryCode`, `lat`/`lng`, `zips` and geoname id lists only, with no place-name parameter (pinned `openapi.json`). The conclusion drawn from it was wrong: the website's name search runs on the committed covered-community index (TS-WEB-0008 D2/D7) and ships without this endpoint, so nothing waits here. What is still wanted upstream is a **name search over the existing Typesense index** — tracked as geo-api #130 (unified search) and #166 (coordinate search). Its value: the committed index could be retired, and option 1 of Q-0071 (Germany-wide finding by name) becomes possible at all. `findbyaddress` stays forbidden (DEC-0024). | nothing that ships — enables Q-0071 option 1 | geo-api | DEC-0024, DEC-0079, pinned spec |
| Q-0026 | **Demand to Portalize:** embed cookie-freedom verified (no consent duty introduced), and a place-filter parameter for the loader so the demo shows the searched place. | FUN-WEB-0043 | portalize | DEC-0030 |
| Q-0027 | ~~Preview domain~~ **Resolved by DEC-0035**: `next.schafe-vorm-fenster.de` — it serves only the product's pre-launch preview (not live), which moves aside. | CON-WEB-0020 | — | DEC-0035 |
| Q-0032 | **Demand to geo-api:** coordinate → administrative-hierarchy resolution. FUN-WEB-0160, FUN-WEB-0161, FUN-WEB-0162, CON-WEB-0073 requires county-level geolocation, but platform request headers give country / state / city — no county — and `findbyaddress` is banned for this path (DEC-0024). Either geo-api gains the resolution or FUN-WEB-0160, FUN-WEB-0161, FUN-WEB-0162, CON-WEB-0073 relaxes to state, which would make relevance tiers 0–2 unreachable before a place search. | FUN-WEB-0160, FUN-WEB-0161, FUN-WEB-0162, CON-WEB-0073, TS-WEB-0005 D1 | geo-api | TS-WEB-0010, 2026-09-10 |
| Q-0033 | ~~Which brand package binds?~~ **Resolved by DEC-0044** — two packages, three levels: tokens and assets from `@schafe-vorm-fenster/brand-design`, identity from `brand-identity`. The third candidate no longer exists. | CON-WEB-0003 | — | DEC-0044 |
| Q-0034 | Accessibility verification of the brand as it now stands: the **colour world 3c** (Lime · Violett · Himbeere) against the contrast targets, and the type scale in use. The typeface itself needs no readability case — Atkinson Hyperlegible Next was designed for low-vision readers and the family ships only 400/700/800, so the weight-floor question dissolves (DEC-0043). An earlier version of this question said "Inter", which was wrong. | NFR-WEB-0016, NFR-WEB-0058, NFR-WEB-0059, CON-WEB-0025 | brand/design | DEC-0043 |
| Q-0035 | ~~Nonce or per-build hashes?~~ **Resolved by DEC-0045** — per-build hashes; the shell stays static. | CON-WEB-0030, TS-WEB-0004 D6 | — | DEC-0045 |
| Q-0036 | ~~Where does the last-good store live?~~ **Resolved by DEC-0046** — Vercel Runtime Cache; a cold region falls to tier 3 by design. | FUN-WEB-0102, DEC-0019 | — | DEC-0046 |
| Q-0037 | **Demand to events-api:** `/api/stats` returns `totalEvents`, date bounds and quality counters only — no field for *places* or *updates today*. Under FUN-WEB-0041 those two counters are simply not rendered until the fields exist. | FUN-WEB-0041 | events-api | TS-WEB-0008, 2026-09-10 |
| Q-0038 | **Demand to geo-api:** caller-supplied radius and `maxResults` on the geoPoint search. Today a server-side constant `PROXIMITY_SEARCH_RADIUS_KM = 20` applies, so the relevance model's "~15 km" is approximated in the BFF by haversine over returned positions. | FUN-WEB-0042 | geo-api | TS-WEB-0008, 2026-09-10 |
| Q-0039 | ~~Where do /hilfe URLs and the support articles go?~~ **Resolved by DEC-0047** — articles migrate to the app, `/hilfe/*` redirects there; interim target is the app root. | FUN-WEB-0070, CON-WEB-0010 | — | DEC-0047 |
| Q-0040 | Does the app emit conversion completion under the same hub goal ids, and does one eTracker secure code really mean one property for website and app? DEC-0028 states the intent; the account was never inspected. | CON-WEB-0029, CON-WEB-0036 | app team | TS-WEB-0012 |
| Q-0041 | **Demand to the app team:** a public help URL contract, so `/hilfe/{slug}` can redirect per article instead of to the app root (DEC-0047), and a destination for the 37 migrating support articles. | FUN-WEB-0070 | app team | DEC-0047 |
| Q-0042 | ~~Display weight 800 missing from the tokens?~~ **Resolved** — `@schafe-vorm-fenster/brand-design@0.1.3` adds `font.weight.display: 800`. | NFR-WEB-0016 | — | 2026-09-10 |
| Q-0043 | ~~Lucide SVGs not delivered?~~ **Not a demand** — the website installs the icon set into its own stack as a dependency (CON-WEB-0055, CON-WEB-0056, CON-WEB-0057). The v2.0 boards render without icons locally, which is cosmetic and does not affect the specification. | CON-WEB-0055, CON-WEB-0056, CON-WEB-0057 | — | 2026-09-10 |
| Q-0044 | **Component manifest.** The design system specifies six components (button, search field, badge/chip, event row, photo surface, logo) at implementable depth. The pages additionally need: context band, proof card and stream, live-module shells, header/navigation, footer, form wrapper, order-flow steps. And no component declares which content type it renders — the `renders` binding of SRC-0013 §4 is still absent, which is what turns specs plus content into a page. | visual generation | design | SRC-0013, DEC-0056 |
| Q-0045 | ~~media-echo has no usage_rights~~ **Resolved by DEC-0063** — same field and values as proof. The clearing work itself is owed: 32 entries, each a judgement. | FUN-WEB-0033, FUN-WEB-0037 | gtm (task) | DEC-0063 |
| Q-0046 | **Release blocker — narrowed 2026-09-11.** `calendar-api` only *reads* a Portalize configuration (`GET /organizers/{id}/portalize`); nothing there mints an organizer. The candidate path is envoy's `POST /api/registration`, which writes an organizer to the CRM and accepts `tarif`/`tarifStatus` — open is whether that yields a usable Portalize configuration and an id the embed code can carry. | FUN-WEB-0015 | portalize + envoy | TS-WEB-0025, pinned specs |
| Q-0047 | ~~Unpriceable scope?~~ **Dissolved by DEC-0060** — 480 € is per organisation, not per place or region. Scope configures what the calendar shows; it never drives price. | FUN-WEB-0015, BUS-WEB-0015, FUN-WEB-0202 | — | DEC-0060 |
| Q-0048 | ~~Does the map ship?~~ **Resolved by DEC-0061** — January 2027, bought by Landkreis Rottweil. Advertised with its date until then. | FUN-WEB-0016 | — | DEC-0061 |
| Q-0049 | ~~Permanence promise has no proof element~~ **Resolved by DEC-0064** — a proof element is minted with the Nordkurier article as evidence. Artefact owed by the hub. | FUN-WEB-0012, FUN-WEB-0150, FUN-WEB-0151, CON-WEB-0069 | gtm (task) | DEC-0064 |
| Q-0050 | ~~"Six channels" not enumerated~~ **Resolved by DEC-0064** — the channels get enumerated in the hub's messaging so the page can name them. Artefact owed. | FUN-WEB-0012 | gtm (task) | DEC-0064 |
| Q-0051 | **Demand to geo-api** (extends Q-0025/Q-0032): a place the covered-community index does not know has no coordinates, so "the nearest active place" — the core module of `/dein-ort/starten` — has no anchor to measure from. Can `community/search` return the nearest covered community for a place name it cannot resolve? (The postcode half of the original wording is void: a postcode is not a search input on this website, DEC-0079.) | FUN-WEB-0158, FUN-WEB-0159 | geo-api | TS-WEB-0021 |
| Q-0052 | ~~May entry context set the home focus job?~~ **Resolved by DEC-0059** — no contradiction: the focus job is stable, entry context changes emphasis and order only. | FUN-WEB-0010, FUN-WEB-0052 | — | DEC-0059 |
| Q-0053 | ~~Logo radius conflict~~ **Resolved** — 999/pill, per the design system's systemic argument. The brand-design README is corrected. | CON-WEB-0003 | design (task) | 2026-09-11 |
| Q-0054 | **The logo files named in the tokens do not ship.** `logo.primary/circle/notched` point at `svf-mark-flat.svg`, `svf-mark-circle.svg`, `svf-mark-corner.svg`; the package contains only `Schafe-vorm-Fenster_Logo_V2.1.{svg,png}`. `svf-mark-flat.svg` exists in the v2.0 board assets but not in the package. | CON-WEB-0003 | design | 2026-09-11 |
| Q-0055 | **Category token provenance.** The four categories in the tokens are correct — they match `classification-api` exactly (`community-life`, `culture-tourism`, `education-health`, `everyday-supply`, plus `unknown`). But the token records them as `PROVISIONAL — derived from entre/app/clients/events-api/types/event.types.ts (consumer copy)`. Source them from the owning service instead. | CON-WEB-0055, CON-WEB-0056, CON-WEB-0057 | design + gtm | 2026-09-11 |
| Q-0056 | **Glossary amendment.** The source document says "Akteur = Organizer" and does not distinguish registers. DEC-0062 adds it: Actor/Akteur is the outward word (copy, audience model), Organizer the internal one (data model, CRM, APIs). The document needs the distinction before it is imported. | FUN-WEB-0178, CON-WEB-0079 | jan-henrik | DEC-0062 |
| Q-0057 | Import the glossary into the hub as `@schafe-vorm-fenster/glossary` (same path as the legal texts: Google Doc → script → markdown → package), and give `specs/glossary/glossary.md` the two columns the content concept asks for — the word to use per locale, and the words not to use. | FUN-WEB-0178, CON-WEB-0079, FUN-WEB-0089 | gtm | DEC-0062 |
| Q-0030 | ~~Measure segmentation cache cost before launch?~~ **Resolved by DEC-0055** — observed in production instead; the fallback to municipality level is a parameter change. | FUN-WEB-0052, NFR-WEB-0047, NFR-WEB-0051 | — | DEC-0055 |
| Q-0031 | ~~Assessment drift trigger?~~ **Resolved by DEC-0049** — twelve months or a major version, reported not blocking. | FUN-WEB-0089 | — | DEC-0049 |
| Q-0029 | ~~Is the DPA public?~~ **Resolved by DEC-0052 §5** — yes, as a section at `#auftragsverarbeitung`; municipalities check it before buying. | FUN-WEB-0146, CON-WEB-0067 | — | DEC-0052 |
| Q-0028 | IA amendment for the "start the calendar in my place" page: **draft written** into `go-to-market-os/concept/website-information-architecture.concept.md` (route `/dein-ort/starten`) — awaiting review there. | FUN-WEB-0158, FUN-WEB-0159 | jan-henrik | DEC-0024, DEC-0036 |
| Q-0058 | ~~Breakpoints collide by name~~ **Resolved by DEC-0067** — six, as the tokens define them, and the density below 640 px is deliberate: a 360 px and a 428 px phone are tuned differently rather than sharing one "mobile" layout. TS-WEB-0017's two proposed breakpoints are withdrawn, names included. Residual: the design system still owes a container width and outer gutter per breakpoint (all six). | CON-WEB-0049, CON-WEB-0050, CON-WEB-0051, TS-WEB-0017 | — | DEC-0067 |
| Q-0059 | ~~LCP declares two photographs that do not exist~~ **Resolved by DEC-0068/DEC-0069** — both image LCPs stay. A missing asset is filled by a generated placeholder at the declared aspect ratio, so the LCP element has its geometry from day one and the swap changes bytes, not layout. Guardrail: a placeholder may occupy a slot, never assert anything — no synthetic person, place or record. | NFR-WEB-0039, NFR-WEB-0040, NFR-WEB-0047, TS-WEB-0003 | jan-henrik | TS-WEB-0003 D2 |
| Q-0060 | ~~Confirm the publishable-price predicate?~~ **Resolved by DEC-0070 §1/§2** — it stands: 4.000 € never appears, not even as "ab", because `on-request` means the price is negotiated and an anchor would undo the negotiation before it starts. The withheld amounts shipping inside the build stay **unguarded as an accepted risk**, with an explicit trigger: the first time one reaches a rendered page, the guard gets built. | BUS-WEB-0015, FUN-WEB-0202, TS-WEB-0018 | jan-henrik | TS-WEB-0018 D3 |
| Q-0061 | ~~Machine-readable Offer at 480 €?~~ **Resolved by DEC-0069 §2** — yes, with a `priceSpecification` carrying `valueAddedTaxIncluded: false` and a `referenceQuantity` whose `unitText` names the organisation, so net and scope travel with the figure. | FUN-WEB-0062, TS-WEB-0011 | jan-henrik | TS-WEB-0011 D4 |
| Q-0062 | ~~Four AAA criteria beyond AA?~~ **Resolved by DEC-0069 §3** — eight. Added: 2.4.10 Section Headings, 2.2.6 Timeouts, 3.3.6 Error Prevention, and 2.5.5 Target Size (44 × 44 everywhere, superseding the 24 × 24 AA floor). Rejected criteria are recorded rather than silently dropped. | CON-WEB-0026, TS-WEB-0002 | jan-henrik | TS-WEB-0002 D2 |
| Q-0063 | ~~Build the order preview and its BFF route?~~ **Resolved by DEC-0069 §8** — deferred to the backlog. The scope configuration stays; the live answer to "what would be in my calendar" does not ship in V1, which removes the flow's only upstream dependency. Spec kept intact in TS-WEB-0025 D4 so it need not be re-derived. | FUN-WEB-0041, TS-WEB-0025 | jan-henrik | TS-WEB-0025 D4 |
| Q-0064 | ~~Commit generated resilience snapshots?~~ **Resolved by DEC-0069 §6** — yes. These are the tier-3 fallback payloads in `src/generated/snapshots/`; without a committed file the "keep the last good one" rule has nothing to keep. | NFR-WEB-0052, NFR-WEB-0053, TS-WEB-0009 | jan-henrik | TS-WEB-0009 D8 |
| Q-0065 | ~~Reserve `/deine-termine`?~~ **Resolved by DEC-0071 §1** — reserved but not built: the name is held so no other plan claims it, it answers 404 until the widget is sellable, nothing links to it and it is absent from the sitemap. Same category as `/mitmachen/vor-ort-werben`. | FUN-WEB-0002, TS-WEB-0004 | jan-henrik | TS-WEB-0004 D7 |
| Q-0066 | ~~Lead fallback address and rendering?~~ **Resolved by DEC-0069 §7** — the Google Form already at `/start`: linked never embedded, via our own `/start` redirect so the envoy swap touches one route, with the recipient named and the email address still offered beside it. | FUN-WEB-0184, FUN-WEB-0185, FUN-WEB-0187, FUN-WEB-0152, CON-WEB-0083, TS-WEB-0016 | jan-henrik | TS-WEB-0016 D6 |
| Q-0067 | ~~Confirm the cache lifetimes?~~ **Resolved by DEC-0069 §4** — stale windows raised to three days for dates and counters; fresh TTLs unchanged, so corrections still land in five minutes. The long window only governs the unreachable case. | FUN-WEB-0105, TS-WEB-0003 | jan-henrik | TS-WEB-0003 D5 |
| Q-0068 | ~~Confirm the context-proximity weights?~~ **Resolved — the tag was wrong, not the numbers.** SRC-0002's scoring block already fixes 1.0 and 0.3; TS-WEB-0005 D2 carried an over-broad `[PROPOSED]`. Corrected. Genuinely this spec's own: the single middle value 0.6, and applying the 0.3 floor to unnamed types instead of excluding them (DEC-0069 §9). | FUN-WEB-0149, CON-WEB-0068, TS-WEB-0005 | jan-henrik | TS-WEB-0005 D2 |
| Q-0069 | ~~Confirm the JS budget as a gate?~~ **Resolved by DEC-0069 §5** — the byte budget reports, Lighthouse gates. A route over budget with Lighthouse green warns in the run summary; only the Lighthouse floor fails a build. | NFR-WEB-0039, NFR-WEB-0040, NFR-WEB-0053, TS-WEB-0003 | jan-henrik | TS-WEB-0003 D1 |
| Q-0070 | ~~Confirm the eight low-stakes determinations?~~ **Resolved by DEC-0070 §3** — all confirmed and flipped to `[FIXED]`. TS-WEB-0021 D9 and TS-WEB-0010 D11 were *not* in the block and stay `[PROPOSED]`. | TS-WEB-0015, TS-WEB-0014, TS-WEB-0029, TS-WEB-0028, TS-WEB-0021, TS-WEB-0010 | jan-henrik | PROPOSED review |
| Q-0071 | ~~Search scope — covered-only now, or Germany-wide?~~ **Resolved 2026-09-24 (owner), recorded as the DEC-0079 amendment**: option 2. Suggestions come from the committed covered-community index; a typed name with no match produces no suggestion and still reaches `/dein-ort/starten?ort=<query>` on submit, so the activation path stays intact (FUN-WEB-0158, FUN-WEB-0159). Germany-wide finding by name stays the **target**, carried by Q-0025 as the upstream demand to geo-api. No search surface states a limit or an interim either way; DEC-0024 §1 is re-read accordingly. | — | — | DEC-0079 amendment 2026-09-24 |
| Q-0072 | ~~The contact section's phone row has no record.~~ **Resolved 2026-09-24 by the hub** — `@schafe-vorm-fenster/goals` now carries `contact-channels.md` (SRC-0008), which records all four channels with their addresses, the conversion goal each serves (`request-product-briefing`, for all four) and the responder (the managing director). **Phone and WhatsApp are one number and two rows**: the company's published imprint and privacy policy carry it as the telephone number, and its published QR-code support page names the same number as the WhatsApp number — verified, not assumed. `request-product-briefing` now names phone in its `measurement`, its action and its instrumentation. **No response expectation exists on any channel**: no cleared source states one, so no surface may promise one, and the hub carries that as its own open point. The row therefore **ships**, and its value is content that comes from the hub record at build time or from the legal import — never a value a spec states (DEC-0083). The four rows, their order and what each does are TS-WEB-0016 D13. | DEC-0081 §1, FUN-WEB-0184, FUN-WEB-0185, FUN-WEB-0187, FUN-WEB-0152, CON-WEB-0083 | — | review 2026-09-23 theme C |
| Q-0073 | **Demand to envoy/ops: the WhatsApp newsletter route has no receiving side.** The newsletter reaches people by e-mail **or WhatsApp, WhatsApp preferred** (DEC-0052 §4 as amended, `subscribe-to-newsletter` in SRC-0008). The website side is a day's work — a `wa.me` click-to-chat link with a prefilled subscribe message (TS-WEB-0016 D10 N1, D14). Three things behind it **do not exist** and are not covered by the Q-0022 envoy contract, which carries signup and double opt-in for the e-mail route only (DEC-0051): **N3** nothing records an arriving opt-in as a subscription or adds it to the broadcast list — the customer newsletter is a manual broadcast to a hand-maintained contact list; **N4** there is no unsubscribe, and a broadcast list with no way out is a contact list, not a subscription; **N5** there is no confirmed-subscription count per channel, so the goal cannot be read. Until N3 exists the link is not built: it would collect opt-ins into an inbox and lose them. Separately, **no sending system is in operation on either route** — Q-0020 chose one, delivery is Q-0022 C10. | FUN-WEB-0186, FUN-WEB-0190, FUN-WEB-0191, CON-WEB-0085, CON-WEB-0086, TS-WEB-0016 D10, DEC-0052 §4 | envoy/ops | owner decision 2026-09-24 |
| Q-0074 | **Demand to the `@leafcutter-strict/library-schemas` maintainer: an acceptance test has no slot for its verification level.** The contract closes `acceptance_tests` items at `id`/`given`/`when`/`then` with `additionalProperties: false`, so DEC-0040's level — `static · unit · integration · e2e · tool · manual` — cannot be carried inside the contract; putting it in `then` makes typed data free text, putting it in `id` makes re-levelling a renumber, which the identifier method forbids. Measured in DEC-0087 §6, decided in DEC-0090: the level stays, the deviation is recorded, and the need goes upstream. The handover document is [demand-acceptance-test-verification-level.md](demand-acceptance-test-verification-level.md) — it states the contract, the conflict and the proposed change and names nothing of this project, because the framework is a vendor dependency and the schema argument stands without us. It closes when a typed `verification_level` field or an opened `additionalProperties` ships, and then the 420 criteria migrate mechanically. | 420 acceptance criteria, the verification pyramid | library-schemas | DEC-0087 §6, DEC-0090 |
| Q-0075 | **Demand to the `@leafcutter-strict/method-statement-grammar` maintainer: a requirement that is a mapping has no form.** The method defines seven forms and every one is a sentence; `FUN-WEB-0010`–`FUN-WEB-0018` are one three-column row each — route, focus job, primary conversion — and no slot holds a tuple. Measured and decided in DEC-0093 §3: the nine stay at `form: F0` with the reason on the artefact, because flattening each row into three sentences makes 27 artefacts that together say what nine rows say and hides the mapping, and inventing a form is a governance change. It closes when the method carries a mapping form, and then the nine migrate mechanically. | FUN-WEB-0010–0018 | leafcutter-strict | DEC-0093 §3 |
| Q-0076 | **Demand to `@schafe-vorm-fenster/media-echo`: job fit needs an `audiences` field.** `FUN-WEB-0039` requires the relevance engine to read `audiences` from a media-echo entry; entries carry only `tags` today, so the value is `UNKNOWN` and full `w_job` scoring is blocked. It closes when the field is modelled and populated. | FUN-WEB-0039, FUN-WEB-0149 | media-echo | DEC-0093 |
