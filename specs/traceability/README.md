# Traceability

## Purpose

The requirements traceability matrix: every requirement traced to its
sources, decisions, and open questions — the STRICT chain kept auditable.

## Contents

- `rtm.md`
- `identifier-map.md` — every pre-DEC-0086 identifier against the one it
  holds now, generated by `scripts/migrate-identifiers.mjs --emit-map`
