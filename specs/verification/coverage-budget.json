{
  "note": "The ratchet of `pnpm check:coverage` (DEC-0141). `max` is the worst tally this repository is allowed to have. A run worse than a number here fails; a run better than one fails too, asking for the number to be lowered in the same commit, so that a gain is recorded instead of left as budget to spend again. Raising a number is a governance change: it needs a decision record, and the record says which criteria were let go and why.",
  "measured_at": "2026-09-26",
  "measured_on": "T-17 after QA round 4 (the copy lint, DEC-0136/DEC-0142). NAMED ONLY 31 → 32 is a raise, and it is the one criterion this task let go: TS-WEB-0018-A7. Round 3 left it VERIFIED because a unit-test title named it, while the criterion — “`/dein-kalender` is the only route whose body may contain the product name, at most once” — is not met on the render: `/rechtliches` and `/en/legal` carry it seven times each out of the verbatim legal import (DEC-0012, DEC-0027; measured over all 24 route/locale pairs). QA round 4 called that a false green inside the repository's own instrument, so the title in `src/lib/content/validate.test.ts` now names CG-038 — the rule that suite does prove over the content artifacts — and A7 falls to NAMED ONLY, which is what it is: named in `e2e/copy-structure.spec.ts` comments, verified by nothing. The criterion itself is the spec owner's: CONF-0027 (direct_contradiction, OPEN, recommended_action NEW_VERSION) and state/open.md row 277. MISSING stays 146, VERIFIED falls 252 → 251. Earlier movements, unchanged: MISSING 149 → 146 came with the merge of next-2026 (the copy-lint tests name TS-WEB-0006-A8 and TS-WEB-0029-A15 in their titles), and NAMED ONLY 32 → 31 came with TS-WEB-0007-A13 declaring level `tool` and `scripts/check-content.ts` naming it in its output. The instrument's own inability to mark a criterion as met-by-title-only is DEC-0141 §7 / row 271; the raise is recorded in DEC-0142 §9.",
  "max": {
    "NAMED ONLY": 32,
    "MISSING": 146
  }
}
