# Open Points

Everything the run could not decide itself, plus everything
deliberately deferred. This is the list Jan reads after the run.
Pre-seeded with the known blockers and their working assumptions —
per the mock rule (plan/guardrails.md) missing systems are mocked
with labeled dummy data, so the run never blocks on them. Rows
marked `Mock aktiv` are the checklist for the pre-go-live hardening
round; rows marked `Dummy-Content` drive the content follow-up
workstream.

| # | Point | Source | Severity | Working assumption | Decision needed |
| --- | --- | --- | --- | --- | --- |
| 1 | Q-045: 0/32 media-echo entries carry `usage_rights` — clearance filter admits none | specs Q-045 | high | Archive renders clearly-labeled dummy press entries (`Demo-Daten`) so the function is visible; real entries swap in when clearances land — Mock aktiv | yes (Jan, gtm) |
| 2 | Q-046: nothing mints an `organizerId` during an order — instant embed-code delivery has no mechanism | specs Q-046 | high | Order flow mints a mocked `organizerId` and shows the full instant-embed experience with dummy embed code — Mock aktiv | yes (spec session) |
| 3 | Q-052: contradiction whether entry context may set the home page's focus job | specs Q-052 | medium | PM decision (2026-09-11, plan/component-inventory.md §5 D-1): entry context sets **emphasis and scene order only**, never the focus job — `/` stays `know-what-is-on` at every stage, block 1 / primary CTA / band / closing CTA invariant. Same reading as DEC-059, so no new ADR is written here (decision numbering contended by a parallel session); the behaviour is built in M4 (TS-010, TS-019 D3a) and any implementation-level record is written there | yes (spec session) |
| 4 | Q-044: design system defines 6 components, pages need more (header, context band, proof card, live shells) | specs Q-044 | medium | Developer derives the missing set from the design system, marked [PROPOSED], reviewed at the M2 customer acceptance | yes (design review) |
| 5 | geo-api demands: name search (Q-025), coordinate→county (Q-032), caller radius (Q-038), nearest-covered-community (Q-051) | specs Q-025/032/038/051 | medium | Missing geo-api capabilities are mocked behind the BFF (name search, county resolution, radius) with dummy data — Mock aktiv | yes (geo-api team) |
| 6 | Q-037: events-api `/api/stats` lacks *places* and *updates today* fields | specs Q-037 | low | Missing `/api/stats` fields are mocked so all three counters render, labeled as demo values — Mock aktiv | yes (events-api team) |
| 7 | Q-022: envoy widget contract (CSS vars, events, spam protection, a11y, delivery date) undelivered | specs Q-022 | high | Envoy widget is a mock component behind its interface module: full form UX, dummy submission — Mock aktiv | yes (envoy team) |
| 8 | Q-041: app public help-URL contract for `/hilfe/{slug}` per-article redirects | specs Q-041 | low | Help redirect works against the app root; per-article mapping mocked with a dummy table — Mock aktiv | yes (app team) |
| 9 | Logo corner radius: website design system says `999` (pill), brand-design README says `var(--radius-lg)` | design system vs brand package | low | The website builds `999` — concept doc wins (repo rule). Brand-package README reconciliation stays with Jan | yes (Jan, brand) |
| 10 | Category taxonomy: website's six categories vs the PROVISIONAL five in the brand tokens | design system vs brand tokens | low | The website uses the design-system category table verbatim; token reconciliation happens in the hub | yes (Jan, brand) |
| 11 | Q-034 names "Inter" as the brand typeface — stale, brand is Atkinson Hyperlegible | specs Q-034 | low | A11y verification (weight floor, sizes, contrast) runs against Atkinson via tool-level checks in M5 | yes (spec session) |
| 12 | Q-040: eTracker property/goal-id parity with the app never inspected | specs Q-040 | low | Event registry implements the hub goal ids as specified; account inspection deferred | yes (Jan) |
| 13 | After the prototype (manual break, joint planning — plan/project-plan.md "After the prototype"): (1) content follow-up for Dummy-Content rows, (2) content review + tone sharpening, (3) usability/feature feedback, (4) finish `Mock aktiv` rows + clearances — only then go-live | Jan, 2026-09-11 | high | Prototype stays on the protected preview | yes (own phases) |
| 14 | Preflight YELLOW: `VERCEL_AUTOMATION_BYPASS_SECRET` not in env/.env.local | preflight 2026-09-11, resolved M1 2026-09-11 | medium | **RESOLVED.** The project had no bypass secret; `vercel curl` generated one during M1 (scope `automation-bypass`, `isEnvVar: true`, so deployments see it as `VERCEL_AUTOMATION_BYPASS_SECRET`). The e2e smoke ran green against the protected preview through the `x-vercel-protection-bypass` header. The secret is a credential and is in no repository file — read it with `vercel api /v9/projects/{id}` or `vercel env pull` | no |
| 15 | Preflight YELLOW: port 3000 occupied by another process | preflight 2026-09-11 | low | Dev server runs on port 3100 for the whole run (`PORT=3100`) | no |
| 16 | Preflight YELLOW: Playwright not installed | preflight 2026-09-11 | low | Installed in M0/M1 harness work package | no |
| 17 | Dummy-Content — `/mitmachen`: no proof/media-echo element backs the CTA's permanence-promise reassurance, and the stage-0 live-example reference place is unnamed by any source | state/content-map.md TS-022 | medium | **Resolved in content (2026-09-12, follow-up group 1).** The reassurance is now sourced: `offerings@0.3.3#community-calendar` calls free access "a public commitment, not a pricing decision that can be quietly reversed", and `media-echo@0.3.3#2022-nordkurier-plattform-doerfer` carries the 2022 sentence itself ("Das Basisangebot … werde es auch danach bleiben"). Slot 8 says "seit 2022" and no order of magnitude. The stage-0 reference place stays Groß Kiesow, now cited against `proof@0.3.5#zschiesche-gross-kiesow` (slot 6a, `provenance: sourced`). Both carry `clearance: pending` — the press record has no `usage_rights` (Q-045). Remaining decision: whether a commitment-type proof element gets minted | yes (jan-henrik / gtm: mint a commitment-type proof element or accept a media-echo element in this slot) |
| 18 | Dummy-Content — `/mitmachen/registrieren`: step-2 "who publishes" vocabulary is UNKNOWN — no hub source, the app's account model has not published the enum | state/content-map.md TS-023 | medium | **Resolved in content (2026-09-12, follow-up group 1).** The five labels are now drawn from the audience records' own enumerations (`audiences@0.3.3#actors` field Context: Vereine, Feuerwehr, Kirchengemeinde, Initiativen, Kulturbetriebe, mobile Dienste; `audiences@0.3.3#municipalities` field Context: Gemeinden, Städte, Ämter, Samtgemeinden, Verbandsgemeinden), so the slot is `provenance: sourced`. The **binding value set** is still the app account model's to publish — that half of the row stays open | yes (app team) |
| 19 | Dummy-Content — `/dein-kalender`: the trust block's AI-use sentence has no hub record behind it | state/content-map.md TS-024 | medium | **Partly resolved 2026-09-12 (content follow-up, group 2).** The operations sentence now ships **sourced**: `people@0.3.6#jan-henrik-hempel` (founder, technical lead, lives in Schlatkow) plus `proof@0.3.5#in-operation-since-2018` (`cleared`) — it names a hub record and therefore satisfies TS-024-A19. Legal form and operating address stay out, since they live in the imprint and in no hub record. The **AI-use** sentence is still withheld: nothing in the hub packages or in `go-to-market-os` documents how AI handles publisher data. Its demo placeholder stays `generated`/`demo: true` — Dummy-Content | yes (jan-henrik / gtm hub: mint an AI-use record) |
| 20 | Dummy-Content — `/deine-region`: the two-working-day response promise has no named handling process or owner (Q-022/C11) | state/content-map.md TS-026 | medium | **Unchanged, re-verified 2026-09-12** against `go-to-market-os` (handbook, governance, operations packages): no handling process with a named owner exists anywhere, so the promise wording stays withheld entirely (constant `null`) per TS-026 D5 and TS-016-A13. The generated demo placeholder sentence beside it was already forbidden by F-2-57 and is not reinstated — Dummy-Content | yes (envoy team, ops owner) |
| 21 | Dummy-Content — `/rechtliches`: the accessibility statement (`#barrierefreiheit` / `#accessibility`) has no source document at all — not even a Google Doc | state/content-map.md TS-029 | high | **Unchanged, re-verified 2026-09-12.** Searched the nine installed hub packages and `go-to-market-os` in full; the brand-identity org-profile intake (`packages/identity/brand-identity/intake/org-profile-gdocs/`) holds only `.gdoc` pointer files with no local text, so it is not a source either. A conservative, generic BFSG placeholder statement ships marked `provenance: generated`, flagged for legal-counsel review before any production build — Dummy-Content | yes (jan-henrik, with legal counsel) |
| 22 | Newsletter block: Q-020 leaves the sending system undecided; TS-016 D10 forbids a placeholder form, the mock rule forbids a hole | TS-016 D10 vs plan/guardrails.md; plan/component-inventory.md D-4 | medium | Ships as a visibly labelled mock: full UX, `Demo-Daten` badge, no address leaves the browser, no subscription claimed — Mock aktiv | yes (envoy/ops: Q-020) |
| 23 | Q-044 residue: the single-choice control for `/mitmachen/registrieren` steps 2 and 3 has no design-system component | TS-023 D8; plan/component-inventory.md D-6 | medium | `choice-group` derived from the specified chip: radius 999, ≥ 44 px, real radiogroup, plain GET form — no new shape, no new colour | yes (design review at M2 acceptance) |
| 24 | Context band suppressed on registration steps 2–3 (TS-023 D7) against TS-006 D5/A6, which require it on every page | TS-023 D7 vs TS-006 D5; plan/component-inventory.md D-5 | medium | M2 follows the page-level spec: band on step 1 only, deviation recorded rather than silently passing TS-006-A6 | yes (jan-henrik / TS-006 owner) |
| 25 | The design system carries no measure token; TS-029 D5 sets 66/80 ch for the legal page only | TS-029 open point 7; plan/component-inventory.md D-7 | low | The same measure applies to every long-form text column in M2 instead of each page inventing one | yes (SRC-014 owner) |
| 26 | Content type `partner-mention` (B.3 #16) is assigned to `/deine-region` and `/ueber-uns` by the content concept but composed by no page spec | concept B.3 vs TS-026/TS-027; plan/component-inventory.md §7 | low | Not built in M2 — an unspecified feature goes on this list, not into the code (plan/guardrails.md) | yes (IA / jan-henrik) |
| 169 | TS-017 D2 in `plan/project-plan.md` M1 still says "breakpoints `md:768`/`lg:1024`, container 960px" — the spec itself withdrew those numbers (DEC-067, DEC-056) and the token names collide: the tokens call 768 `lg` and 1024 `xl` | plan/project-plan.md vs TS-017 D2 + DEC-067 | medium | The spec wins over the plan. M1 implements the six `breakpoint.*` token values and `measure.page` = 1200px; the shell uses three of the six (sm 428, lg 768, xl 1024). The plan's M1 row should be corrected so the next reader does not re-derive the withdrawn pair | yes (PM: fix the plan row) |
| 170 | TS-015-A1's metadata surface cannot carry the host half of the indexability predicate without making the prerendered shell a per-request function — the failure mode DEC-041 §8 forbids and DEC-045 protects against | TS-015 D3 vs DEC-045 | medium | The `<meta name="robots">` surface is computed from `VERCEL_ENV` at build time; the host half is carried by the `X-Robots-Tag` header, which the proxy sets per request and which covers every route including assets. Both surfaces are noindex on every non-production deployment; only "production build served under a non-canonical host" differs, and there the header still fires | yes (spec session: amend A1's wording or accept the split) |
| 171 | TS-003 D3 caps fonts at 50KB across both families and three weights; the actual latin woff2 set is 67.5KB (sans 400/700/800 = 37.5KB, mono 400/700/800 = 30KB) | measured M1 2026-09-11 against `@fontsource/atkinson-hyperlegible-*@5.3.0` | medium | All six faces ship — the design system needs mono 700 for labels and 800 for display. The budget is exceeded by 17.5KB and recorded rather than met. Options for M5: drop mono 800, subset further, or amend the budget | yes (spec session / performance owner) |
| 172 | TS-003-A3 asks for a "single variable woff2"; `@fontsource/atkinson-hyperlegible-next` and `-mono` publish static weights only — there is no variable build to import | measured M1 2026-09-11 | low | Six static latin woff2 faces, `font-display: swap`, self-hosted. A3 is unsatisfiable as written against the package the brand ships | yes (spec session: amend A3 or ask the brand owner for a variable build) |
| 173 | DEC-045's per-build inline-script hash extraction does not exist yet — `script-src` ships `'self' 'strict-dynamic'` plus the D1 host allowlist, with an empty hash set | M1 2026-09-11, TS-014 D3 | high | RESOLVED for the mechanism and for `next dev`/self-hosted; still not delivered to the Vercel-deployed Proxy, after three attempts. `scripts/generate-csp-hashes.mjs` runs after `next build` and correctly hashes every distinct inline `<script>` (28 hashes, verified against a genuine `next build && next start`: correct header, zero CSP violations, real hydration). Delivery to Vercel's actual Proxy function failed twice, measured: (1) a hand-written file under `.next/security/` — excluded from the function's traced filesystem (`existsSync` false at runtime, though the build log confirms the file was written); (2) the same file republished under `.next/static/security/` for the proxy to `fetch()` from its own origin at runtime — Vercel's static-asset upload is itself manifest-driven and does not publish a file no Next manifest references, so the fetch 404s live even though the build log again confirms the file was written. A third avenue, Vercel Edge Config, was stopped before any code depended on it: `vercel edge-config add` succeeded, but `vercel edge-config tokens --add` (needed for a read token) was denied by the session's own permission classifier as "Modify Shared Resources"; the store was removed again immediately, nothing was installed, `stack.allow.json` is untouched. `'strict-dynamic'` is removed from the policy for good, independently of all of this: measured in Chromium, it made the browser ignore `'self'` for the parser-inserted script chunks even with real hashes present, so it never helped this app's script layout. Row 31 carries the interim fix that actually ships today (preview-only `'unsafe-inline'`); this row stays open for whoever picks up a real delivery mechanism — Edge Config with a human-provisioned token, or reopening the nonce-vs-static-shell trade-off DEC-045 settled | no (M4 work, not a decision) |
| 174 | The Vercel project still carries the legacy website's build settings: `installCommand: "yarn install"`, `buildCommand: "yarn build"`, `nodeVersion: 22.x` | M1 2026-09-11 | medium | Overridden in-repo by `vercel.json` (`pnpm install --frozen-lockfile` / `pnpm build`), which takes precedence and is reviewable — better than a dashboard setting either way. `nodeVersion` cannot be overridden from `vercel.json`; the build runs Node 22 while local and `.nvmrc` say 24. This run was not permitted to modify the shared project settings | yes (Jan: clear the two stale commands and set Node 24.x, or accept the vercel.json override and the Node split) |
| 175 | `pnpm audit` reports 72 findings in the dev tree (2 critical, 26 high) — all of them in the `vercel` CLI (`tar`, `undici`) and in `eslint-config-next`'s import plugin (`brace-expansion` via `minimatch@3`) | supply-chain audit M1 2026-09-11 | medium | The **runtime** tree is clean: `pnpm audit --prod` finds nothing, so nothing shipped to a browser is affected. `js-yaml` was bumped 4.1.1 → 4.3.2 to clear three high findings on a direct devDependency. The rest have no fix that does not mean downgrading the tool that needs them. TS-014 D12 will gate `pnpm audit --audit-level=high` in CI and will find them there — the gate needs a `--prod` scope or an expiring ignore list | yes (spec session: does D12 gate the dev tree or the runtime tree?) |
| 176 | TS-014 D7 names `lib/security/csp.ts`; the repository has no top-level `lib/` and TS-017-A5 names `app/` and `src/` as the code roots | M1 2026-09-11 | low | The module is at `src/lib/security/csp.ts`. One module, one policy — D7's substance holds; only the path differs | no (assumption recorded) |
| 177 | TS-015 D3 puts `X-Robots-Tag` on *all* routes including assets, so `proxy.ts` runs with no `matcher` — on every request, including `/_next/static/*` | M1 2026-09-11 | low | Implemented as specified. If proxy invocations become a cost or latency problem, the trade is a matcher that skips static assets, which weakens D3's "incl. assets" | no (recorded) |
| 178 | The GitHub Actions pipeline of TS-015 D4/D7 is not built — M1 built the checks, not the workflows | M1 2026-09-11 | medium | `pnpm check` is the gate and the pre-commit hook runs it. `preview.yml` and `deploy.yml`, the five required contexts, auto-merge, `knip`, `jscpd`, Lighthouse CI, axe and the canary promotion are all stage-2 work (DEC-031) and are not in the M1 work package | no (later work package) |
| 27 | `next dev` writes a `<!-- BEGIN:nextjs-agent-rules -->` block into `AGENTS.md` on every run | M1 2026-09-11 | low | Committed as part of the foundation, as the block itself advises — leaving it uncommitted re-creates the diff on every dev-server start | no (recorded) |
| 28 | `@schafe-vorm-fenster/cli@^0.2.0` and `@schafe-vorm-fenster/config-engineering@^0.2.0` answer 404 on GitHub Packages — the packages are gone from the registry, and only a warm local pnpm store hid it | M1 2026-09-11 | medium | Both removed from `devDependencies`: they blocked every clean install, not just the Vercel build. Neither is imported by any code; `config-engineering` is named in `.agents/README.md` as the provenance of the agent-os packages, which still reads true | yes (Jan: republish them, or confirm they are retired) |
| 29 | `CLAUDE.md` and `GEMINI.md` were symlinks to an **absolute** path (`/Users/jan-henrik.hempel/Projects/…/AGENTS.md`), so they dangle on every machine but one — and Next 16's agent-file writer `stat`s `CLAUDE.md` and crashed the Vercel build with `ENOENT` | M1 2026-09-11 | low | Both converted to relative symlinks (`AGENTS.md`). Same meaning, resolves everywhere | no |
| 30 | TS-017-A12 (persistent calendar entry in the header on every page) is not satisfiable in M1 — the header entry's label and target are TS-004 D4, and WEB-C-004 names a different label (open point 11 of TS-017's own list) | M1 2026-09-11 | low | The M1 shell carries the wordmark only. The calendar entry lands with the header component in M2, once the label question is answered | yes (IA owner: which label binds — already an open TS-017 demand) |
| 31 | The CSP blocks **every** script in the running app: `script-src 'self' 'strict-dynamic'` with an empty hash set means `strict-dynamic` disables the host allowlist and no `<script>` executes at all. Measured in the browser on `next dev` at HEAD (2026-09-11, M2): every Turbopack chunk and every inline bootstrap script is refused, so nothing hydrates | M2 component work package, measured; relates to row 21 | high | FIXED everywhere it needs to be for the prototype, including the actual Vercel preview. `next dev` and a self-hosted `next build && next start` both hydrate cleanly with the real per-build hash set (row 21). Two real hash-delivery mechanisms to the deployed Vercel Proxy were tried and measured to fail, and a third (Edge Config) was stopped after its token-creation step was denied by permission — full detail in row 21. Because the preview is the surface reviews and user tests actually run on, `csp.ts` now adds `'unsafe-inline'` there specifically when the hash set is empty (`environment === 'preview'` only; production never gets it, hash set or not — D7 holds unconditionally in production). Verified on a fresh preview deploy (`schafe-vorm-fenster-8bkxy2mov-schafe-vorm-fenster.vercel.app`, via the bypass header): CSP carries `'unsafe-inline'` and no `'strict-dynamic'`; console shows 1 violation — Vercel's own `vercel.live` toolbar script, outside the D1 allowlist by design and unrelated to the app — down from 19; `React hydrated: true`; `back-to-top`'s `data-visible` flips `false→true` on scroll; `motion-reveal` sets `data-armed`; `pnpm e2e` run against that same preview (`E2E_BASE_URL` + the bypass secret) is green, 38/38. Closing this without the preview concession needs a properly human-provisioned Edge Config token, or reopening the nonce-vs-static-shell trade-off DEC-045 settled — both bigger than a developer session can do alone | yes (M4 / security owner: nonce or per-build hashes before any deploy that needs JS) |
| 32 | The design system's type table sets **Lead at weight 600**, while the same document forbids "a weight below 400 or between 400 and 700", and the font package ships 400 · 700 · 800 only | SRC-014 §Typography vs §Do Not; M2 | low | The Lead role ships at 400 at the Lead size (`--type-lead-size`). 700 would read as a heading, 600 does not exist in the package and would be synthesised | yes (SRC-014 owner: correct the table to 400 or 700) |
| 33 | The token package carries neither the placeholder ground `#FBF1DC` nor the design system's nine type roles (Display 54 · Place name 50 · Section head 38 · Sub head 32 · Card title 21 · Lead 20 · Body 18 · Meta 15 · Label 12) — `--font-size-*` is a different, shorter scale | `@schafe-vorm-fenster/brand-design@0.1.3` vs SRC-014; M2 | medium | The one missing colour is declared in `app/styles/brand.css` (the only file a brand value may enter through) and the type roles are composed from the existing tokens in `app/styles/components.css` as `--type-*`. Both belong upstream in the token package so the website stops carrying design values | yes (Jan, brand: take `--color-placeholder-ground` and the `--type-*` roles into brand-design) |
| 34 | "Foto gesucht": the design system's prose puts the badge in the placeholder pair (`status-warning` on the placeholder ground) and lists the photo-wanted badge under *himbeere*, while `Style Guide.dc.html` renders it `himbeere-600`/`paper` | SRC-014 §Status vs §Photo surface vs the board; M2 | low | The inventory's reading is built (`plan/component-inventory.md` §3.5 #58): the placeholder pair, so placeholder photography, unresolved clearance and demo data stay one visual register and the single himbeere element per screen is not spent on a placeholder | yes (design review at M2 acceptance) |
| 35 | The header's four job labels scroll horizontally in their row below the lg switch point — the design system specifies no phone form for the header, and TS-017 D2(d) forbids a second component tree | TS-004 D4 / SRC-014 silence; M2 | medium | One tree at every width: the labels stay visible and scrollable rather than collapsing into a toggle, because a menu behind a button is a second tree and hides the four jobs the header exists to switch between. Marked `[PROPOSED]` in the component | yes (design review at M2 acceptance) |
| 36 | `app/dev/components` renders the component gallery (every component, every state) for the visual check. It answers `notFound()` when `VERCEL_ENV === "production"`, is `noindex`, and nothing links to it — but it is a route in the tree and `src/components/gallery.tsx` is in the bundle graph | M2 component work package | low | Kept for M3–M5 review and user tests; the route and `gallery.tsx` are deleted in the hardening round, or the gallery is moved behind a build flag if it should survive | no (delete before go-live) |
| 37 | Next.js 16.3.4 does not server-render the **body** of a `notFound()` raised inside the route tree: the status is 404 and the page's metadata is emitted, but the HTML document is an empty `__next_error__` shell and the browser leaves it blank. Measured in `next dev` and `next start`, with and without a dynamic root segment, with and without `reactCompiler` and `proxy.ts` | M2 routing 2026-09-11 | medium | `app/global-not-found.tsx` (`experimental.globalNotFound`) is the served 404 — it renders a complete document with the right status. It sits above `[lang]`, so it has no language parameter and no request path: an unknown `/en/…` URL gets the **German** 404, not an English one. `app/[lang]/not-found.tsx` is written and localized and takes over unchanged the moment the framework renders an in-tree `notFound()`. DEC-073 **RESOLVED in the M5 fix round (c0e018c, f2089e3) — and the row understated the damage.** The same postponed-resume mechanism also left the *German* 404 with a zero-character body (F-2-70), which this row never recorded. Both halves are fixed by moving the decision in front of the route: `proxy.ts` rewrites an unservable path onto a two-segment target that matches nothing, so Next's own 404 handling renders `global-not-found` in full, and the language comes down on a request header from the one place that still has the URL. Measured with JavaScript disabled on the local production build and on the preview: `/dies-gibt-es-nicht` and `/uk/mitmachen` 370 characters in German, `/en/anything` 328 in English, place search and jobs band on all of them. No Next upgrade was needed | no — resolved (was: accept the German-only 404 body, or revisit after a Next upgrade) |
| 38 | TS-011 D1 names `proxy.ts` as the redirect map's only consumer and rules out config-level `redirects()`; M2 reads the map in `next.config.ts` instead | TS-011 D1 vs M2 implementation; DEC-073 | low | D1's substance holds — one legacy URL is known in one place (`src/lib/routes/redirect-map.ts`), and Next.js evaluates redirects before every rewrite, which is D1's "step 0". The host-dependent rules of TS-004 D3 (landing-only domains, apex phasing, `/:community`) need the request host and stay for `proxy.ts` in M4; moving the consumer then changes the consumer, not the table | no (recorded) |
| 39 | The language switcher in the layout links to the other language's **home page**, not the equivalent page (TS-001-A7 wants the equivalent page) | M2 routing 2026-09-11 | medium | **Resolved at M4** by the chrome work package's `page.meta.ts`/`PageFrame`/`SiteChrome` mechanism this row predicted: `src/components/site-footer/site-footer.tsx` now renders `<LanguageSwitch route={route} current={locale} .../>` with the real route id, and `src/components/language-switch/language-switch.tsx` (already correct since M2 — it always resolved through `href(route, locale)`) needed no change. Verified by reading both files (M4 analytics/seo/locale work package, 2026-09-11). Not yet fully live end-to-end: `app/[lang]/layout.tsx` still carries the old M2 inline header/footer alongside pages that now also render `SiteChrome` via `PageFrame` — that duplicate-chrome cleanup is the remaining step, owned by whoever is completing the chrome migration (in flight in this same run), not by this row | no (mechanism verified correct; layout.tsx cleanup tracked separately — see the chrome/page-frame work package) |
| 40 | `/start` has two contradictory targets: TS-004 D1 makes it the lead fallback's redirect to the Google Form (TS-016 D6), TS-011 D2 maps the legacy `/start` to `/mitmachen` | TS-004 D1 vs TS-011 D2 | low | **Decided at M4** (analytics/seo/locale work package): no redirect-map row for `/start`. TS-016 D6 makes `/start` a live route joining the TS-004 D1 inventory itself, redirecting on to the Google Form — the same functional job ("Anmelden"/publisher signup) the legacy page at that exact URL did. One path cannot carry both a redirect-map row and a page route (`next.config.ts` evaluates redirects before any route matches), so the two were never really compatible; keeping the URL live under TS-016 satisfies WEB-F-070 ("stable URLs or 301") more directly than moving it would, and lands the same audience one hop closer to signup. Recorded in `src/lib/routes/redirect-map.ts`'s module doc and in `redirect-map.test.ts` | no (decided; TS-011 D2's proposed row is superseded by this decision, not built) |
| 41 | `/en/<German segment>` (e.g. `/en/dein-ort`) answers 200 as well as `/en/your-place`, because the App Router tree's directory names are the German segments (TS-004 D2) | M2 routing 2026-09-11 | low | Harmless for indexing — the canonical of the page points at `/en/your-place` and the sitemap lists only that. A 301 from the German-segment form to the localized one is one row in `localeRedirects()` if a review wants the duplicate gone | no (recorded) |
| 42 | `pnpm check:brand` (TS-017-A4) reports false positives on **built** CSS: it scans `.next/static/**` and its `min-width` regex matches minified declarations such as `min-width:0}` that are properties, not media features. The check passes on a clean tree and fails after any `pnpm build` — which is exactly the state CI is in | M2 routing 2026-09-11, `scripts/check-brand.ts` | medium | Fixed round 1 (26176ff): the walk no longer reaches `.next/`, `node_modules/` or `.vercel/` at all — only `app/**`, `src/**`, `e2e/**`, `scripts/**` — and the `min-width` regex now requires the same `@media (...)` context test the `max-width` half already used, so a minified property can't be mistaken for a media feature again. Negative fixtures in `scripts/check-brand.test.ts`; `pnpm build && pnpm check` verified green in that order | no — resolved |
| 43 | M3 Phase 2 artifact shape: TS-007 D4 fixes one generated file per **slot** per locale (`content/<locale>/<route>/<slot>.<type>.md`), but the reshaped 26-type schema and `derived_from`/`RelevanceFacets` fields it depends on have not landed in `src/domain/content-frontmatter.schema.ts` yet (still the pre-relaunch 8-type schema) | state/content-map.md "Artifact shape" section vs TS-007 D4/D5; M3 content-production run 2026-09-11 | medium | Content & Translation wrote one file per **page** per locale instead — `content/pages/<route-slug>/de.md` (nested to match the route tree, e.g. `content/pages/dein-ort/starten/de.md`), one Markdown section per content slot from `state/content-map.md`, in page-spec composition order, with per-slot provenance/`derived_from`/status recorded in an inline HTML comment (`<!-- id: …; content_type: …; provenance: …; derived_from: […]; status: … -->`) since the frontmatter is page-level and slots differ in provenance. Frontmatter carries a best-effort superset: the old schema's required fields (`id`, `content_type: section` — or `legal` for `content/legal/accessibility.md` — `status: draft`, `locale: de`) plus TS-007-shaped extras (`page_id` TS-###, `route`, `derived_from[]`, `generated_by`, `generated_at`, `provenance`, `tone_profile`, `compliance_check`, `open_points`). `pnpm check:frontmatter` passes on all twelve files, but only because Zod's default (non-strict) object parsing silently drops unknown top-level keys — that is not the same as TS-007 D12 actually validating `derived_from` resolution, facet completeness or harmonisation. `content_type: section` is also a stopgap: neither the current 8-type nor the planned 26-type vocabulary defines a whole-page composite type, so it is the closest existing fit, not a real match | yes (M3 pipeline developer: confirm whether to keep one-file-per-page for the shipped pipeline or migrate to TS-007 D4's one-file-per-slot shape once the schema is reshaped; spec session: decide whether TS-007 D4 should instead admit a page-composite type) |
| 44 | Dummy-Content — `/mitmachen` slot 6a: the stage-0 live-example reference place (state/open.md #17) is now a concrete editorial choice, not just a working assumption | content/pages/mitmachen/de.md slot 6a; state/content-map.md TS-022 row 6a | low | **Resolved in content (2026-09-12, follow-up group 1).** Still Groß Kiesow, but the slot is no longer `generated`: `mitmachen-6a-reference-place` is now `provenance: sourced` against `proof@0.3.5#zschiesche-gross-kiesow`, with `clearance: pending` (the testimonial is `unverified`, the place itself is a configuration and nothing is quoted). Confirm or replace the named place | yes (jan-henrik / gtm: confirm or replace the named place) |
| 45 | Completeness-correction decision (orchestrator, 2026-09-11): for the prototype, the dummy-content rule (plan/guardrails.md) overrides every `Sourced — empty by design` determination in `state/content-map.md`. Every slot the map marked empty-by-design (clearance-gated proof/testimonial pools) or withheld-by-spec-determination (TS-024 D10, TS-026 D5) now additionally carries generated, clearly-labelled demo content so no page renders an empty slot in the prototype | plan/guardrails.md (dummy-content rule) vs state/content-map.md's 11 `Sourced — empty by design` rows and 2 `withheld` rows; Content & Translation run 2026-09-11 | medium | All generated demo additions are on-voice, use only recognizably exemplary names (`Beispielgemeinde Musterdorf`, `Beispielzeitung`, `Beispiellandkreis Musterkreis`, etc. — never a real-looking person, number, award, or quote presented as real), are marked `provenance: generated` and `demo: true` in their slot's inline HTML comment so components can render the `Demo-Daten` badge, and are additive: the original `sourced-empty-by-design`/`withheld` determination and its reasoning stay in the file untouched, addressed in rows #46–52 below | yes (jan-henrik / gtm: this is a prototype-only override; the hub-clearance and no-source blockers it papers over — Q-045, Q-014, TS-024 D10, TS-026 D5/C11 — still need resolving before go-live, per state/open.md #13) |
| 46 | Dummy-Content — `/` (home) slot 8: the proof-stream pool (proof package + media-echo) has no cleared selection today (Q-045, state/open.md #1) | content/pages/home/de.md slot 8; state/content-map.md TS-019 row 8 | low | **Resolved in content (2026-09-12, follow-up group 1).** The five demo cards are replaced by five real candidates: `lehre-lelender`, `noerd-award-2026-smart-community` (`cleared`), `media-echo#2026-04-nord-award-nordkurier`, `volkshochschule-uecker-randow`, `in-operation-since-2018` (`cleared`). `home-8-proof-stream` is `provenance: sourced`, no `demo: true`; three of the five run with `clearance: pending`. Note: `state/content-map.md`'s TS-019 count table says "2 Sourced-empty-by-design" for this page, but only one slot (8) in the shipped file carries that provenance tag — a pre-existing map/file discrepancy from Phase 2, corrected in the map's counts table rather than re-litigated here | yes (jan-henrik / gtm: same clearance blocker as #1) |
| 47 | Dummy-Content — `/dein-ort` slots 3, 4, 5, 6: all four value stories' testimonial parts are empty by design (Q-014, all candidate testimonials `unverified`); story 3 (Kultur) additionally has no backing anecdote at all | content/pages/dein-ort/de.md slots 3–6; state/content-map.md TS-020 rows 3–6 | low | **Resolved in content (2026-09-12, follow-up group 1).** All four demo testimonials are replaced by the real hub quotes they were standing in for — `kurzweg-baeckerei` (Elisabeth Kurzweg), `zschiesche-gross-kiesow` (Dr. A. Zschiesche), `eichler-wasserschloss-quilow` (Uwe Eichler), `wendt-rubkow` (Holger Wendt) — each `provenance: sourced`, no `demo: true`, all `clearance: pending` (Q-014). Story 3 (Kultur) gained a real anchor as well: `volkshochschule-uecker-randow`, so the slot is no longer `sourced-empty-by-design`. Quotes are set with umlauts restored from the packages' ASCII transliteration, wording otherwise verbatim, and are **not** translated on `/en` | yes (jan-henrik / gtm: same clearance blocker as #1, Q-014) |
| 48 | ~~Dummy-Content~~ **resolved for slot 5, partly for slot 6** — `/dein-kalender` | content/pages/dein-kalender/{de,en}.md slots 5, 6; state/content-map.md TS-024 rows 5, 8a/8b | low | **2026-09-12 (content follow-up, group 2).** Slot 5's three invented demo cards are replaced by the three real, verbatim quotes from the pool `portalize-calendar` names: `wendt-rubkow`, `zschiesche-gross-kiesow`, `eichler-wasserschloss-quilow` — `provenance: sourced`, no `demo: true`, clearance pending (row 160). Slot 6's operations sentence is sourced (row 19); only the AI-use placeholder is still generated demo content | yes (jan-henrik / gtm: written clearance per quote before go-live; AI-use hub record) |
| 49 | Dummy-Content — `/mitmachen` slot 7: the publish-weighted proof pool has no cleared selection today (Q-014) | content/pages/mitmachen/de.md slot 7; state/content-map.md TS-022 row 7 | low | **Resolved in content (2026-09-12, follow-up group 1).** The three demo cards are replaced by three real publish-weighted reference cases: `lehre-lelender` (Gemeinde Lehre, 17 Orte, LeLender), `volkshochschule-uecker-randow` (Kursprogramm, Standort Pasewalk), `zukunftswege-ost-newsletter` (Newsletter-Termine). `mitmachen-7-proof-demo` is `provenance: sourced`, no `demo: true`, all three `clearance: pending` (Q-014) | yes (jan-henrik / gtm: same clearance blocker as #1) |
| 50 | ~~Dummy-Content~~ **resolved for slot 6**, unchanged for slot 7 — `/deine-region` | content/pages/deine-region/{de,en}.md slots 6, 7; state/content-map.md TS-026 rows 6, 7 | low | **2026-09-12 (content follow-up, group 2).** Slot 6's three invented demo cards are replaced by three real proof elements: `impftermine-landkreis` (`cleared`), `eichler-wasserschloss-quilow` (`unverified`) and `lehre-lelender` (`unverified`) — `provenance: sourced`, no `demo: true`. The slot states in copy that none of the three is a delivered `portalize-enterprise` territory. `partner-network` was dropped from the pool: it names no partner and has no cleared logo rights. Slot 7 stays withheld (row 20) | yes (jan-henrik / gtm: clearance for the two `unverified` elements) |
| 51 | ~~Dummy-Content~~ **resolved** — `/ueber-uns` slot 3, the reserved `testimonial` place | content/pages/ueber-uns/{de,en}.md slot 3; state/content-map.md TS-027 row 6 | low | **2026-09-12 (content follow-up, group 2).** The invented demo testimonial is replaced by the real quote from `proof@0.3.5#kulturlandbuero-broellin` (Kulturlandbüro Uecker-Randow, Schloss Bröllin), `provenance: sourced`, no `demo: true`, clearance pending (row 160). The same slot now also names the **six cleared** proof elements the other six places can be filled from today (`noerd-award-2026-smart-community`, `in-operation-since-2018`, `founder-former-volunteer-mayor`, `impftermine-landkreis`, `ukraine-integration-dorfleben`, `google-baecker-schlatkow`) — all `usage_rights: cleared`, so the page needs no invented cards at all | yes (jan-henrik / gtm: written clearance for the testimonial, or the place goes back to empty per TS-027 D5) |
| 52 | ~~Dummy-Content~~ **resolved** — `/ueber-uns/archiv` slot 2 | content/pages/ueber-uns/archiv/{de,en}.md slot 2; state/content-map.md TS-028 row 2 | low | **2026-09-12 (content follow-up, group 2).** The six invented demo rows are replaced by **all 31 real media-echo entries**, verbatim from each entry's frontmatter (title, type, date, source, geo, url), chronological newest first, with a link column. `provenance: sourced`, no `demo: true`, clearance pending for every row (row 160). The count is **31**, not the 32 this list and the Phase-1 map quote — `media-echo@0.3.3` ships 31 `.media-echo.md` files (row 164). Four of the eight award-type entries are applications rather than prizes; the rows carry the source titles verbatim ("Bewerbung ohne Zuschlag", "Wettbewerbseinreichung") so nothing reads as a win | yes (jan-henrik / gtm: Q-045 clearance per entry before go-live) |
| 53 | Phase 3 (EN translation) — `/legal` (`content/pages/rechtliches/en.md`): the five imported legal documents plus the generated accessibility statement exist in German only; `import.yaml` has no English targets (TS-029 open point #2) | content/pages/rechtliches/en.md slot 5; state/content-map.md TS-029 row 7 | medium | The EN page frame (h1, registry, footer labels) is a genuine English translation, but slot 5 states explicitly, in English, that the six legal sections themselves are provided in German only — no machine translation, no invented English legal text. Per DEC-027, DE and EN legal texts both arrive later through the same `import.yaml` path, once English source documents exist; this is a pipeline/import gap, not a translation-quality gap this playbook can close **The English-notice half is RESOLVED (5c66ab3, F-2-74):** `dictionary.legal.germanOnlyNotice` renders between the `h1` and the section nav on `/en/legal` only, server-rendered, before the first German body. The translation itself stays open | yes, for the translation (jan-henrik / gtm: commission or confirm English source documents for the five imported texts plus the accessibility statement) |
| 54 | M2 §2.3 (argument blocks): `price-tag` and `origin-story` take the offering's price as typed props (`display`, `figure`) rather than importing `@schafe-vorm-fenster/offerings` directly — that package is a devDependency today (content-pipeline tooling), not a registered runtime dependency, and wiring it into a shipped component would need the stack-harmony ADR this work package was not asked to run | plan/component-inventory.md §3.2 #29 vs `stack.allow.json` | low | `src/components/price-tag/price-tag.tsx` and `src/components/origin-story/origin-story.tsx` accept the resolved figure as props; M3's content pipeline is what will read the real package and pass the resolved value in — one place changes when that lands | yes (M3 content-pipeline owner: confirm the resolver lives in content generation, not in the component) |
| 55 | M2 §2.3: `archive-filter` is a third `"use client"` component beyond `motion-reveal`/`back-to-top` — TS-028 D4/D5 requires client-side, no-refetch filtering, which cannot be server-first by construction | plan/component-inventory.md §3.2 #34; `src/components/README.md` updated to name the exception | low | Implemented with a hydration-safe mount gate (`useSyncExternalStore`, not `useEffect`+`setState`, to satisfy `react-hooks/set-state-in-effect`); renders nothing until mounted, so a visitor without JavaScript sees the full, unfiltered archive rather than a dead filter row. Row 31 (CSP blocking all client script) still gates whether this ever hydrates in a shipped build | no (tracked by row 31; recorded here so the third exception is visible) |
| 56 | `app/dev/components` answered a browser runtime error ("Missing `<html>` and `<body>` tags in the root layout") on every request — `app/[lang]/layout.tsx` is the site's only root layout, and `app/dev` is a sibling tree outside `[lang]` with none of its own. `gallery.test.tsx` never caught it because it renders `<ComponentGallery />` directly, bypassing the page/layout tree entirely | found during M2 §2.3 browser verification (`next-dev-loop`), 2026-09-11 | medium | Added `app/dev/layout.tsx` supplying `<html>`/`<body>` for the `app/dev` subtree (same two stylesheets the real root layout imports) and moved the stylesheet imports out of `app/dev/components/page.tsx` into it. Fixed, not merely worked around — but the gap it exposes (a route that passes every static and unit check while being broken in the browser) is worth a wider look before go-live | yes (app/`**` owner: decide whether every dev-only route needs an explicit browser smoke check in `pnpm check` or `e2e`) |
| 57 | `legal-section`'s heading carries `tabIndex={-1}` as the static half of TS-029's "focus moves to the section heading after an in-page jump" contract, but the runtime half — actually moving focus on hash navigation/`hashchange` — is layout-level behaviour no single component can own, and is not built anywhere yet | plan/component-inventory.md §3.2 #39, TS-029 D1–D6 A11y clause | low | `src/components/legal-section/legal-section.tsx` ships the static prerequisite only; the focus-move script belongs beside the header height / `scroll-margin-top` wiring, which is layout, not component, work | yes (app/`**` owner or M4 rendering work package: add the hash-navigation focus script once) |
| 58 | M3 pipeline determination (resolves #43): the loader reads the shipped **one file per page per locale** shape as is — `content/pages/<route-slug>/<locale>.md` with one `##` section per slot and the slot metadata in an inline comment. TS-007 D4's one-file-per-slot layout is not built; every determination D4/D6 make about content (locale-free id, per-slot provenance, `derived_from` as the update key) holds unchanged | ADR-074 §1; state/content-map.md "Artifact shape"; `src/lib/content/slot-meta.ts` | low | Rewriting eleven finished pages into 172 fragments buys a file layout, costs a content re-run, and would be re-cut again once Layer C says what a composition is. One module knows the comment syntax, so a later migration is contained | no (recorded; revisit when Layer C gets a spec) |
| 59 | TS-007 D5's 26-type schema reshape is **not** done. `src/domain/content-frontmatter.schema.ts` gained the TS-007 layer (page frontmatter, slot metadata, lifecycle, B.3 slot vocabulary) beside the pre-relaunch 8-type taxonomy, which `content/features|support|legal` still validate against. `RelevanceFacets` (D7) is absent from the artifacts and from the schema — TS-005 cannot read facets off this content yet | ADR-074 §5; TS-007 D5/D7 | medium | M3 builds what M3 can check. The facets land with the relevance engine work package, which is the first consumer that needs them; the archive taxonomy is reshaped when archive content is | yes (spec session: does D5's reshape stay a single file, and who writes the facets onto 172 existing slots) |
| 60 | Six of TS-007 D12's twelve checks do not run: 2 length budgets, 4 clearance re-validation, 6 hub-id resolution, 9's composition half, 10 segment independence, 11 glossary conformance (12 legal too, while `content/legal/` is still the flat pre-relaunch tree). Each is blocked on an artefact that does not exist — per-field `max()`, the clearance facet, ids in page frontmatter, Layer C, the glossary's use/avoid columns | `src/lib/content/README.md` "Not implemented yet"; TS-007 D12 | medium | The six are listed in the pipeline README rather than silently absent; `pnpm check:content` names what it does check. TS-007-A3/A4 (clearance revocation fails the build) are therefore **not** discharged | yes (spec session: TS-007-A3/A4 need the clearance facet before they can be built) |
| 61 | Content contract — field labels are translated (`Sucheingabe (Placeholder)` vs `Search input (placeholder)`), so a page addressing `slot.fields["…"]` by label silently gets `undefined` on `/en`. Pages must use `fieldAt(slot.blocks, n)`; `check:content` enforces that both locales carry the same block sequence | content/pages/*/{de,en}.md; `src/lib/content/blocks.ts`; concept B.4 | medium | Positional access, enforced by a harmonisation check. The alternative — locale-stable English label keys in the artifacts — is a content edit across 22 files and is the better long-term shape | yes (content/editorial: adopt stable label keys in the next content pass?) |
| 62 | `check:content` reports 20 warnings on the shipped tree, in two families: four `generated` slots not marked `demo: true` (`mitmachen-6a-reference-place`, `mitmachen-8-closing`, `registrieren-2-wer`, `rechtliches-4-accessibility-note`) and `withheld`/`mixed` slots with an empty `derived_from` (`dein-kalender-6-trust`, `deine-region-7-response-promise`) | `pnpm check:content`; plan/guardrails.md dummy-content rule | low | Warnings, not errors: each is a content question with an existing open row (#21, #44, #45), and two of the four generated slots are editorial choices rather than demo data, so a `Demo-Daten` badge would be wrong. Confirmed per slot in the content follow-up, not by code | yes (content/editorial: per slot, badge or no badge) |
| 63 | `js-yaml` moved from devDependencies to dependencies (same version 4.3.2, registered in `stack.allow.json` under DEC-074): `src/lib/content/loader.ts` is imported by rendered pages, so it is a runtime dependency whatever folder it was listed in. `pnpm audit --prod`: no known vulnerabilities | ADR-074 §3; package.json; stack.allow.json | low | No new package enters the tree and the lockfile edit is limited to the importer section. If the page tree ends up fully static at build, the classification is conservative rather than wrong | no (recorded) |
| 64 | M4 CI (`check.yml`/`preview-e2e.yml`, TS-015 prototype subset): `pnpm install` fails in Actions with `ERR_PNPM_FETCH_403` on every `@schafe-vorm-fenster/*` package (verified live: run `34623751415`, `next-2026`). `secrets.GITHUB_TOKEN` with `packages: read` (as directed) authenticates but is still refused — GitHub Packages requires each publishing repository to individually grant the consuming repo's Actions "Manage Actions access" (dashboard-only, no REST/gh-cli endpoint exists for it; confirmed against GitHub's own community discussion #61495 tracking the missing API) | `.github/workflows/check.yml`, `.github/workflows/preview-e2e.yml`; TS-015-A3–A5 | high | Left as directed (`secrets.GITHUB_TOKEN`, `packages: read`) rather than papering over a dashboard-gated setting with a personal-account fallback token, mirroring how this same work package treats the analogous `VERCEL_TOKEN` gap. Both new workflows are otherwise structurally verified (`actionlint` clean, `git push` succeeded, the run reached the install step and failed exactly where predicted) | yes (Jan, org owner: for each of the 11 packages consumed here — `brand-design`, `audiences`, `brand-identity`, `goals`, `media-echo`, `messaging`, `offerings`, `partners`, `people`, `posts`, `proof` — open the package's GitHub Packages settings page and add `schafe-vorm-fenster/schafe-vorm-fenster-www` under "Manage Actions access" with at least Read; then re-run `check.yml`) |
| 65 | M4 CI: `next-2026`'s build is red intermittently under this run's concurrent agents — two different typecheck failures observed minutes apart (`src/lib/live/last-good.ts:69` `TS7031`, then `src/components/code-snippet/copy-button.tsx:36` `TS2322`), the second confirmed via a real Vercel deployment build failing the same way (`vercel inspect --logs` on the preview for commit `b4163fa`). This blocks `check.yml`'s `pnpm build`/`pnpm e2e` steps and also blocks Vercel from ever producing a `success` deployment for `preview-e2e.yml`'s `deployment_status` gate to react to — both are otherwise verified wired correctly (the `gate` job correctly evaluated `state == 'failure'` as "do not run"). Filed as F-2-1 (`state/findings/round-2.md`, source `ci`) rather than chased file-by-file — outside this work package's owned paths | `src/lib/live/last-good.ts`, `src/components/icon/icon.tsx`/`copy-button.tsx`; state/findings/round-2.md F-2-1 | medium | A transient symptom of many agents landing work in parallel, not a standing bug in one file. Recommend a regression sweep (`pnpm check` on `next-2026` HEAD) once this run's parallel work packages settle, before relying on a green `check.yml`/`preview-e2e.yml` run as a real signal — **Triage (PM, round 2): F-2-1, severity medium, fix-now** — a red typecheck blocks all three gate strands at once (guardrails: nothing enters the pipeline that is not green locally); the fix is one `pnpm check` regression sweep on `next-2026` HEAD once the parallel work packages have landed, not a file-by-file chase. | no (tracked by F-2-1; recheck after this run's parallel work settles) |
| 66 | TS-005-A6 is unsatisfiable as written: it asks a five-year-old element at editorial weight 2.0 to outrank a fresh one, but D4's five-year step is 0.45, so the weight needed is > 1/0.45 ≈ 2.23 | TS-005 D4/A6, M4 | medium | The engine implements D4 verbatim (freshness × editorial weight, uncapped); `src/lib/relevance/freshness.test.ts` asserts both the mechanism (2.5 wins) and the shortfall (2.0 loses) rather than bending the table to fit the criterion (plan/guardrails.md: an AC is never reworded). Either D4's steps or A6's number moves | yes (spec session) |
| 67 | DEC-048 fixes the stage-0 weight split for **one** profile (`w_geo` 0.35 → +0.20 time, +0.15 job). The other three D5 profiles have a different `w_geo` and no decided split | DEC-048, TS-005 D5, M4 | low | `weights.ts` generalises DEC-048's ratio 4 : 3 proportionally, which reproduces DEC-048 exactly for the 0.35 profile — marked [PROPOSED] in code and revisable from H5 | yes (spec session) |
| 68 | The concept's geo scale has seven steps (place · municipality · county · state · neighbouring state · nationwide · international), TS-005 D1's tiers measure administrative **containment** relative to the viewer. For a Niedersachsen visitor an MV award, a Baden-Württemberg podcast and a nationwide award are all tier 4 — "very far · middle · far" of the sequence rule cannot be told apart | concept (relevance model) vs TS-005 D1, M4 | low | The engine orders what the tiers can express; the worked example still reproduces positions 1–7 because the scores separate them. A real distinction needs the distance tier D1 defers to the map view | yes (spec session, with the map) |
| 69 | The content artifacts carry no relevance facets — no `geo`, no `job_relation` (+ reason), no `editorial_weight`, no `clearance`. TS-007 D12 rows 4 and 6 are the same gap seen from the content side | TS-005 D3/D4/D5, TS-007 D12, M4 | medium | Pages hand-map a `RelevanceItem` from what a slot carries (example in `src/lib/relevance/README.md`); the additive changes wanted are `relevanceFacets(slot)` on the loader and four new `check:content` rules. The concept's "testimonials do not age" is realised through `editorial_weight`, so it stays inert until the facet exists — Dummy-Content | yes (content pipeline owner) |
| 70 | Stage-1 geolocation is mocked: `mockLocationResolver` answers a labelled county-level `Demo-Standort`, switched on by `GEO_STAGE1_SOURCE=mock` and **off** everywhere else | TS-010 D4/D6, Q-008, Q-032, M4 | medium | The seam is `ViewerLocationResolver` in `src/lib/personalization/geolocation.ts`: request-ish in, `ViewerLocation` out, no field for an IP address, hard truncation at the ceiling. Swapping the real resolver in touches that one module. Blocked by Q-008 (legal) and by geo-api's missing coordinate → hierarchy endpoint (row 5) — Mock aktiv | yes (Jan/legal, geo-api team) |
| 71 | Nothing hands the request's `Referer`, the platform geo headers or a `Sec-GPC`/`DNT` signal to a page, so stage 2 never fires in the running app although its logic and tests are complete | TS-010 D2/D3/D6, M4 | medium | `composeViewerContext()` takes them as arguments and defaults to `direct`/stage 0; `proxy.ts` (a later work package) reads them once and passes `ViewerLocationRequest` down. Until then every request is stage 0, which by D8 is a complete site | no (M4/M5 wiring) |
| 72 | TS-010 D3 requires the press/podcast referrer allowlist to be **content, not code** — a new outlet should not need a deployment | TS-010 D3, M4 | low | `PRESS_REFERRER_HOSTS` ships as a seed list derived from the `media-echo` publishers, and every caller can override it; the content build has to take it over | yes (content pipeline owner) |
| 73 | TS-010-A9's build check (every regional variant set has a neutral variant, nothing keyed below county) exists as `validateVariantSet()` but runs in no gate — no variant sets are shipped yet | TS-010 D9/A9, M4 | low | The rule has one implementation in `src/lib/personalization/regional.ts`; wiring it into `pnpm check:content` belongs with the first shipped variant set | no (whoever ships the sets) |
| 74 | TS-005 D9 places the engine in `src/services/relevance/`; this repository has no `src/services/` and the M4 work package names `src/lib/relevance/` | TS-005 D9, M4 | low | Built as `src/lib/relevance/` and `src/lib/personalization/`, in the `src/lib/*` idiom the run already uses. D9's actual boundary — pure scoring, no direct fetching, clients called from outside — holds unchanged | no (recorded deviation) |
| 75 | M4 live-data: `@vercel/functions@3.9.7` enters `dependencies` as the Vercel Runtime Cache client — the `last-good` store TS-009 D4 tier 2 reads (DEC-075). The Runtime Cache is **regional**: a cold region falls straight to tier 3, which is exactly the question TS-009's open points put to jan-henrik ("is a regional cache acceptable as the only tier-2 source") | TS-009 D4 open point; DEC-045/046; M4 2026-09-11 | medium | Decided and built behind one interface (`src/lib/live/last-good.ts`, two implementations), so answering the question differently later is a one-module change, not a refactor. Sideways look: no sibling uses `@vercel/functions`, but `assets-api` uses `@vercel/blob` and `translation-api` `@vercel/edge-config`, so first-party Vercel packages for platform services is the family pattern. `pnpm audit --prod` clean; one transitive dep (`@vercel/oidc`), no install scripts | yes (jan-henrik: accept the regional store, or fund a durable one before go-live) |
| 76 | Cache Components (`cacheComponents: true`) is **not** switched on, so `use cache`, `cacheLife()` and `cacheTag()` are unavailable and **TS-009-A1 is not discharged**. The flag makes every unguarded request-value read a build error across the whole route tree — its point, and also why it cannot land from a work package that owns no page while three page developers are mid-flight | TS-009 D1/D10, DEC-075 §4; M4 2026-09-11 | medium | `src/lib/live/cache-profiles.ts` carries TS-003 D5's numbers in both shapes — `cacheControlFor()` for the BFF routes (shipped, in the Vercel SWR semantics DEC-019 fixes) and `cacheLifeProfile()` + `cacheTags` for the islands (ready, unused). The flip is a config change plus one directive per island, never a second table of numbers. Islands are plain async server components inside `<Suspense>` until then — same tree, different cache. **Resolved in M4 step 2:** the flag is on. `dynamic`/`dynamicParams` removed, the content pipeline and every live module wrapped in `use cache` with `cacheLife`/`cacheTag` from `src/lib/live/cache-profiles.ts`, request values read outside every cache boundary. `pnpm build` green, `next build --debug-prerender` clean; eight of twelve routes prerender, four blocking routes recorded in row 131 | yes (orchestrator/`next.config.ts` owner: schedule the flip once the pages are wired, then re-check TS-009-A1/A2) |
| 77 | No read token for **geo-api** or **events-api** exists in any environment file (`GEOAPI_READ_TOKEN`, `EVENTSAPI_READ_TOKEN`), and every data operation of both services is path-scoped (`/api/{token}/…`). So the place search, the place's dates, the nearby module and the app-handover slug validation all answer from the mock backend today — Mock aktiv | measured M4 2026-09-11 against `.env.local`/`.env.preview`/`.env.production`; SRC-011 "the website needs read tokens per service as environment variables" | high | The real clients are built and tested; `LIVE_DATA=auto` (the default) switches each capability to the real client the moment its token is provisioned — no code change. `events-api /api/stats` is the one tokenless operation and **is** called for real, so the *dates* counter is live data even here. `src/lib/live/upstream-live.integration.test.ts` runs against the real services and skips the geo half without a token | yes (Jan / service teams: provision read tokens for the preview environment) |
| 78 | The four geo/events capabilities that have **no upstream operation at all** are mocked by necessity, not by environment: name search (Q-025), the county activity ranking behind `/api/region/{county}/examples` (Q-015 residue), and the `/api/stats` *places* and *updates today* fields (Q-037) — Mock aktiv | rows 5 and 6, now measured against the pinned specs; M4 2026-09-11 | medium | Each is a row in `CAPABILITIES` (`src/lib/live/config.ts`) with `upstream: false` and its open-row number, so `LIVE_DATA=real` cannot accidentally call something that does not exist. A test asserts every `upstream: false` capability names an open row. All four leave the BFF with `demo: true`, which is the `Demo-Daten` badge's only input | yes (geo-api team, events-api team — same as rows 5 and 6) |
| 79 | TS-009-A12 (the build step that regenerates the tier-3 snapshots from the real upstreams) is **not built**. The three snapshot artefacts in `src/generated/snapshots/` are committed as TS-009 D8 requires, but their content was produced from the mock backend — Mock aktiv | TS-009 D8/A12; M4 2026-09-11 | medium | Tier 3 works and is labelled; what is missing is only the refresh mechanism. The generator belongs in `scripts/`, which is outside this work package's ownership, and it cannot produce real payloads before row 77 is resolved anyway. `snapshots.ts` reads the files through one module, so the generator writes files and changes nothing else. Counters deliberately have **no** snapshot file and must never get one (TS-009 D6) | no (later work package: write `scripts/build-snapshots.ts` once tokens exist) |
| 80 | WEB-F-041 / TS-008-A10 reading, recorded rather than asserted: the mock backend supplies a `dates` figure (`1234`) for the forced-mock mode, which is a number the counter band can render. A10 forbids "a numeric traction figure in content sources, snapshots or fallback fixtures" | plan/guardrails.md (mock rule) vs TS-008-A10; M4 2026-09-11 | low | The reading built: A10 forbids a figure that **stands in for a failed count**. There is no counters snapshot and no tier-3 branch, so a cold band is removed rather than filled; the demo figure only ever leaves the BFF inside a `demo: true` envelope beside a `Demo-Daten` badge; and in the default `auto` mode the dates figure is the real, counted `totalEvents` because `/api/stats` is tokenless. The static half of A10 (no figure in content sources or snapshots) is asserted by a test | yes (QA/spec session: confirm the reading, or require `LIVE_DATA=mock` to hide the band entirely) |
| 81 | The BFF's rate limit (WEB-Q-038, TS-004 D5) is a fixed-window counter in module memory: per lambda instance on Vercel, therefore per region and per warm instance, and reset by every cold start. The origin check beside it is a header check, which is advisory and not an authentication | `src/lib/live/bff.ts`; M4 2026-09-11 | medium | Honest about what it is and enough for a preview-only prototype whose BFF is read-only and token-free. A real limit needs shared state — the same Runtime Cache row 75 introduces would carry it, at one extra round trip per request. Not built because nothing in the run measures BFF load | yes (security owner: is a per-instance limit acceptable before go-live, or does the hardening round need a shared counter) |
| 82 | TS-008 D6 (the Portalize embed demo, position 1′) is **not wired** by M4. The `embed-frame` shell exists and takes an `organizerId`; the loader `<script>` itself is page work and is gated by TS-008-A12 — written confirmation that the embed sets no cookie, plus the place-filter parameter (Q-026) | TS-008 D6/A8/A12; M4 work package scope; row 7 | medium | Deliberately out of the live-data layer: the loader talks to its own backend and is **not** a BFF route (TS-008 D6), so it has no interface module and no envelope. The page that mounts it owns the lazy load and the failure behaviour (copy and CTA stay, no empty frame). A12 is a launch gate for `/dein-kalender`, on the same page that carries the no-cookie trust claim | yes (page owner + envoy/Portalize: Q-026 before that module ships) |
| 83 | M4 analytics: the real eTracker adapter (`src/lib/analytics/etracker-tracker.ts`) and its loader tag (`etracker-loader.tsx`) are written but not mounted anywhere — `getAnalyticsTracker()` always returns the mock, so TS-012-A9 ("exactly one analytics loader in the rendered HTML") and TS-012-A10 (manual, account-side) are not discharged | TS-012 D2/D4 rule 4; row 12 (Q-040) | medium | Deliberate: mounting the vendor script before the account/field-mapping confirmation (row 12) would load a third-party script for measurement nobody can yet verify is correctly shaped. `NEXT_PUBLIC_ETRACKER_REAL_ADAPTER=true` plus one `<EtrackerLoader>` line in the root layout is the entire hardening step once row 12 closes | yes (Jan: confirm the eTracker account/field mapping, then flip the flag) |
| 84 | TS-011 D7's three candidate interest landing pages (`/deine-region/landkreis`, `/dein-kalender/gemeinde`, `/dein-kalender/tourismus`) are not built — none is in the TS-004 D1 route inventory (`src/lib/routes/routes.ts`'s `ROUTES`), and D7 rule 6 itself requires the IA to be amended (DEC-022) before any of them is buildable | TS-011 D7; TS-011's own open points ("do the three D7 landing-page candidates enter the IA?") | low | Per the mock/scope rule, a route nobody has put in the inventory is not built speculatively. `src/lib/seo/structured-data` ships the builders D7 rule 3 would need (webPageNode, breadcrumbListNode, etc.) so adding a page later touches one file, not a new library | yes (spec session: amend the IA, TS-004, then this row closes) |
| 85 | TS-001 D1's landing-only domains (`.pl`, `.at`, `.com`) have no distinct page content in this app tree — `proxy.ts`'s new host matrix (`src/lib/routes/host-matrix.ts`) detects and canonicalises these hosts correctly, and `app/sitemap.ts` documents the same gap, but every host still serves the identical `.de` full-site route tree because no landing-page content or route narrowing exists to serve instead | TS-001 D1 "landing page only"; TS-004 D1's per-domain narrowing | medium | Detection/redirect logic is real and tested (`host-matrix.test.ts`, `e2e/locale-detection.spec.ts`); building the landing pages themselves is page/content work this library-only work package does not own. `domainConfigFor()`/`DOMAIN_MATRIX` are the seam a future landing-page work package reads to decide what to render per host | yes (page/content work package: build the three landing-only domains' content, then narrow the sitemap and the route tree per host) |
| 86 | The `.de` apex canonical-host redirect (`proxy.ts`/`host-matrix.ts`) now 301s every bare `schafe-vorm-fenster.de` request to `www.`, matching TS-001-A4 literally — but TS-001 D2's phasing note exempts the apex from this only until the `/:community` village-calendar forwarding (TS-004 D3 rule 6, WEB-F-048) moves to `app.*`. That forwarding is not built anywhere in this app tree, so there is nothing today for the exemption to protect; when it is built, it must run **before** `canonicalHostFor()`'s redirect (or exempt its own paths from it) | TS-001 D2; `src/lib/routes/host-matrix.ts`'s "Phasing note" comment | medium | Documented as a build-order dependency in code rather than guessed at; no route in this tree answers `/:community` today so nothing is currently broken by redirecting the apex | yes (whoever builds `/:community` forwarding: sequence it ahead of the canonical-host redirect) |
| 87 | Organization identity for structured data (`src/lib/seo/structured-data/organization-data.ts`) is a **targeted regex parse** of `content/legal/imprint.md`'s prose §5 DDG block, not a read against typed frontmatter fields — the content pipeline's structured legal schema has not landed (rows 59/60). A wording change to that block silently breaks the parse rather than failing a schema check | TS-011 D4 ("no identity data written into code or into this spec"); rows 59/60 | low | Covered by `organization-data.test.ts` (a fixture) and `organization.test.ts` (the real file, so drift is caught), but the parse itself is the risk this module owns alone until per-field legal frontmatter exists | yes (content pipeline owner: typed legal frontmatter fields, then this module reads those instead of parsing prose) |
| 88 | The structured-data library (`src/lib/seo/structured-data/`) ships builders and tests but is wired into **no page** — TS-011-A5/A6/A14 (Rich Results validation, one-entity rule, BreadcrumbList positions) need a page to actually emit the JSON-LD before they can be checked at all | TS-011 D4; work package scope ("as libraries") | medium | Deliberate split: this work package's ownership is `src/lib/seo/**`, not `app/[lang]/**/page.tsx`, which the page work packages own. `src/lib/seo/structured-data/README.md` documents exactly how a page wires one script tag per page | yes (page work packages: call `jsonLdGraph()`/`jsonLdScriptProps()` per D4's table on their own pages) |
| 89 | `e2e/privacy.spec.ts` (TS-013-A1/A2) checks the closed client-request set and cookie/storage absence per route; TS-013-A4 (the deployed CSP allowlist equals the D2 set exactly, no wildcard/extra/missing host), A6 (unit: the geo resolver returns at most county-level and caches no IP), A7/A8 (manual privacy-policy/PR-review gates) are not covered by this work package — no geo resolver or CSP-vs-D2 diff check exists yet to test | TS-013 D2/D6; TS-013-A4/A6/A7/A8 | low | A4 is close to buildable today (`src/lib/security/csp.ts`'s `ALLOWLIST` vs a D2 constant) but was not added unasked, to avoid duplicating the security work package's ownership of `src/lib/security/`; A6 depends on row 70's geo resolver, which is a different work package's module | yes (security owner: TS-013-A4 static check; row 70 owner: TS-013-A6 unit test once the real geo resolver lands) |
| 90 | `Mock aktiv` — TS-008 position 1 (dates in the place) and position 2 (this week nearby) render **dummy rows** on `/` and `/dein-ort`, and position 1 stands in for the nearest-active-place example on `/dein-ort/starten`. No page fetches anything; the rows come from `src/lib/pages/demo-data.ts` over the invented `Beispielgemeinde Musterdorf` and its neighbours, every module marked `Demo-Daten` by its frame | app/[lang]/page.tsx, app/[lang]/dein-ort/page.tsx, app/[lang]/dein-ort/starten/page.tsx; plan/guardrails.md mock rule | medium | M4 swaps `demoPlaceEvents`/`demoNearbyEvents` for the BFF routes of TS-008 D2 behind the same module props (`items`, `state`); the pages do not change. Demo dates are relative to the build, so a stale preview shows stale days — the badge is what keeps that honest | no (hardening round: remove the mock with the real endpoints) |
| 91 | `Mock aktiv` — TS-008 position 4 (live counters) renders the figure `1.234` on `/` behind a `Demo-Daten` badge. Only the dates figure exists at all (Q-037); no places figure and no updates-today figure is rendered, and no static traction number replaces them | app/[lang]/page.tsx block 2b; TS-019-A14, TS-008 D8 | medium | A figure is a claim, so it renders only inside a badged module. `/api/stats` replaces it at M4 | no (hardening round) |
| 92 | Dummy-Content — `/`: four generated UI strings the artifact does not carry — the caption of the WhatsApp scene's example module (`Aus dem Flyer geworden — Beispieltermin`), the geo label `Beispiel`/`Example` on the demo proof cards, the `Foto gesucht` invitation on the embed scene's media frame, and the short unit word `Termine`/`dates` in the counter badge (the artifact's full label stands beside the badge as text, because a pill of fixed height cannot hold a sentence) | app/[lang]/page.tsx `DEMO_LABELS`; plan/guardrails.md dummy-content rule | low | **Answered in content (2026-09-12, follow-up group 1): yes, they are worth a slot.** `home-12-ui-strings` now carries all four, `provenance: sourced` against the IA and the brand voice. The geo label changed from `Beispiel`/`Example` to `Beleg aus der Region`/`Proof from the region`, because the proof cards now carry real records. **The code binding is open** — `DEMO_LABELS` in `app/[lang]/page.tsx` still holds the old strings | yes (content: are these four strings worth slots of their own?) |
| 93 | Dummy-Content — `/dein-ort`: the artifact writes state A and state B, both of which name a place, but no **S0** copy. The prerendered state therefore fills the state-A headline's `{place}` with a generic `deinem Ort` / `your place`, and adds a module heading for `this week nearby`, an example badge word and the interim search hint (the EN hint, because `place-search`'s default is German) | app/[lang]/dein-ort/page.tsx `PAGE_COPY`; TS-020 D2 S0 | medium | **Answered in content (2026-09-12, follow-up group 1): S0 has its own copy now.** `dein-ort-0-state-s0` carries the S0 headline ("Such deinen Ort, dann steht hier, was dort los ist."), the radius module heading, the example badge and the search hint in both locales, `provenance: sourced` against IA principle 6. **The code binding is open** — `PAGE_COPY` in `app/[lang]/dein-ort/page.tsx` still fills `{place}` with `deinem Ort` | yes (content: write an S0 headline for `/dein-ort`, or confirm the generic filler) |
| 94 | Dummy-Content — `/dein-ort/starten`: the artifact writes a fallback **headline** for the placeless variant but no fallback **CTA label**, so `{ort} eintragen` is filled with `deinen Ort` / `your place`. The EN search hint is generated for the same reason as row 93 | app/[lang]/dein-ort/starten/page.tsx `PAGE_COPY`; TS-021 D4 | low | **Answered in content (2026-09-12, follow-up group 1).** `dein-ort-starten-6-cta` now carries a second field, the placeless fallback CTA label ("Deinen Ort eintragen" / "Add your place"), and `dein-ort-starten-5-search` carries the search hint in both locales, so `/en` no longer inherits the component's German default. **The code binding is open** | yes (content: a fallback CTA label for the placeless variant) |
| 95 | Dummy-Content — the **context band's offer sentence** exists in exactly one artifact (`home-10-context-band`); the other ten pages have no `context-band` slot, while TS-006 D5 renders the band on every page. `_page-frame.tsx` carries the home artifact's two sentences as the per-locale default, so no page falls back to a German string on `/en` | app/[lang]/_page-frame.tsx `BAND_HEADING`; TS-006 D5, content/pages/*/ | medium | **Answered in content (2026-09-12, follow-up group 1): one per page.** `/dein-ort`, `/dein-ort/starten`, `/mitmachen` and `/mitmachen/registrieren` now each carry their own `context-band` slot naming their own three non-focus jobs, `provenance: sourced` against communication principle 2 and the job table of principle 1. The other six pages still have none. **The code binding is open** — `_page-frame.tsx` still reads `home-10-context-band` | yes (content: one shared band sentence, or one per page?) |
| 96 | **Content and spec contradict each other on `/dein-ort`'s testimonials.** The artifact ships four generated demo testimonials (row 47) so the prototype is not empty; TS-020-A6 forbids *any* quote, attributed sentence, portrait or paraphrase in blocks 2a while no quote is cleared (Q-014). The page follows the AC: the four stories render three-part and the four demo slots are not rendered at all | content/pages/dein-ort/{de,en}.md slots 3–6; TS-020-A6, D3; state/open.md #47 | medium | The acceptance criterion wins over prototype completeness where the rule at stake is SRC-001 §4 (never substitute an uncleared proof). The demo slots stay in the artifact, unrendered | yes (jan-henrik / content: drop the demo testimonials, or amend A6) |
| 97 | **The chrome seam moved one level down from the layout.** TS-006 D2 wants the context band and the closing CTA "rendered by the shared layout from `page.meta.ts`", and `src/components/README.md` shows the header and footer in `app/[lang]/layout.tsx`. A Next.js layout receives only `children` and its own segment `params`, so it cannot know which route renders below it — and the language switch (TS-001-A7), the header's `aria-current`, the band and the closing CTA all need the route id. Reading it from a request header would make the prerendered shell a per-request function (DEC-045, TS-010 D8). The chrome therefore lives in one shared component, `app/[lang]/_page-frame.tsx`, that every page wraps its blocks in; the layout keeps `<html>`, `<body>` and the skip link | app/[lang]/layout.tsx, app/[lang]/_page-frame.tsx; TS-006 D2, TS-004 D2 | low | One component for all eleven pages, so "every page" stays enforceable; a page that forgets the frame has no chrome at all, which is visible immediately and is covered per page by the e2e walks | no (recorded; revisit if Next.js ever exposes the route to a layout) |
| 98 | **A third shape for the closing block (TS-006 D6) [PROPOSED].** D6 names two: the goal's CTA repeated, or the merged three-job offer. Neither fits a state whose primary conversion is a *module*: on `/` and on `/dein-ort` in their prerendered state the focus job is fulfilled in place by the place search, and TS-019 D2 gives that state no conversion goal at all ("a search is not a conversion"). Both pages repeat the search module as their closing block — same submit label, same target, without the `data-cta="primary"` marker | app/[lang]/_page-frame.tsx `ClosingBlock`; TS-006 D6, TS-019 D2 S1, TS-020 D2 S0 | medium | "No new text, same target" is satisfied literally; the alternative would be a link to another page, which TS-006 D4 forbids for this focus job. Reviewed under round 2b (F-2-3): that round's `_page-frame.tsx` edit touched only `SiteChrome`'s `BreadcrumbTrail` `label` prop (a distinct accessible name for the breadcrumb nav); `ClosingBlock` and this third shape are untouched, so the assumption above still stands | yes (TS-006 owner: adopt the module shape, or name a goal for the search state) |
| 99 | **`/dein-ort/starten` is a dynamic route today, against TS-021 D10.** D10 wants a static shell with `?ort=` resolved outside the cache boundary; that is PPR, which TS-009 adopts at M4. TS-021-A2 meanwhile requires the `h1` to contain the searched place **with JavaScript disabled**, which only a server render can do — so the page awaits `searchParams` and Next.js renders it per request | app/[lang]/dein-ort/starten/page.tsx; TS-021 D10, A2, TS-009 | medium | The acceptance criterion wins; the fix is one `Suspense` boundary around the parameter-dependent part once `use cache`/PPR lands | no (M4 cache-components work package) |
| 100 | At 360 px the hero content on all three pages is taller than the dark band of `photo-surface`'s gradient (transparent to 18 %, 0.84 at 55 %), so the first headline line can sit on the light part of the image. It is visible today because the DEC-068 placeholder images are light hatches; a real, darker photograph would likely clear 4.5:1. SRC-014 §Photo surface promises the contrast by construction, and on a phone it is not construction but luck | app/[lang]/{page,dein-ort/page,dein-ort/starten/page}.tsx at 360 × 640; src/components/photo-surface/photo-surface.module.css; TS-002 | medium | Left as is rather than changed from a page: the gradient is a [FIXED] design-system value and belongs to the component work package. Screenshots at 360 are in this work package's report — **Triage (PM, round 2): F-2-2, severity high, fix-now** — contrast on the `h1` of the three conversion entry pages at the smallest reference viewport; the fix is a scrim floor in `photo-surface.module.css`. | yes (design / components: a floor on the scrim behind the content box, or a shorter hero content block on the small range) |
| 101 | The `Demo-Daten` badge renders its German label on `/en` as well: `demo-data-badge` takes a `label` prop, but the modules that put the badge on themselves (`live-module-frame`, `place-search`, `proof-card`, `live-counters`, `photo-surface`, `media-frame`) do not forward it. Same for `freshness-label`'s `Stand:`/`Beispiel` and `place-search`'s default hint | src/components/*/; every mocked module on /en | low | Left as is — forwarding a label through six components is a component work package, not a page change. The badge is a prototype marking, and it is legible either way — **Triage (PM, round 2): F-2-4, severity medium, fix-now** — German strings on every English conversion page, and the language switch is a chaos target at gate 2.. **Resolved in M4:** the words moved into the dictionary (`live.demoData`/`stale`/`snapshot`/`dates`/`places`/`updatesToday`) and every self-badging module takes a `locale`; the German strings stay the default, so no existing caller changes | yes (components: forward the label, or read the dictionary) |
| 102 | Finding outside this work package: `/mitmachen` (and any page that follows it) resolves its language with `resolveLocale` in the page body instead of `localeFrom`, so an unsupported language code renders the page instead of answering 404. `src/lib/routes/routing.integration.test.ts` (TS-004-A2) fails on it | app/[lang]/mitmachen/page.tsx:100; app/[lang]/_locale.ts; TS-001 D4 | medium | Reported, not fixed — the file belongs to another developer in this run. **Resolved in M4 step 2:** `/mitmachen` had already moved to `localeFrom`; the defect was live on five *other* pages (`deine-region`, `deine-region/angebot`, `ueber-uns`, `ueber-uns/archiv`, `rechtliches`), all five now use it. It became load-bearing when Cache Components forced `dynamicParams = false` out of the layout | no (the page's owner: swap `resolveLocale` for `localeFrom` in the page body; `generateMetadata` keeps `resolveLocale`) |
| 103 | TS-011 D5 / WEB-F-089 — a page's `<title>` and meta description still come from the dictionary's M2 placeholders (`src/lib/routes/metadata.ts` → `dictionary(locale).pages[route]` and `descriptionTemplate`), not from the page's content frontmatter: the shipped artifacts carry no `seo` block and `content-frontmatter.schema.ts` defines none. Visible on every page (`/` reads "Schafe vorm Fenster — Platzhalter aus dem Routing-Gerüst (M2)…"), and it is the half of TS-021-A11 that cannot pass today — the other half (no runtime value in the indexed surface) holds by construction | src/lib/routes/metadata.ts; src/lib/i18n/dictionary.ts; content/pages/*/{de,en}.md; TS-011 D5, TS-021-A11 | medium | Left as is: metadata sourcing is the SEO/content-pipeline area, not a page composition change, and it needs a schema field plus a value in 22 artifacts **RESOLVED in the M5 fix round (18c94ac, F-2-72)** — see row 138 | no — resolved (was: TS-011 / content pipeline owner: add `seo.title`/`seo.description` to the slot schema and read them in `pageMetadata`) |
| 104 | Two competing `page.meta.ts` shape modules exist: `src/lib/pages/page-meta.ts` (`JobId` incl. `"why-us"`, `JOB_REGISTRY`/`jobLabelKey`/`jobRoute` helpers, closed `CONVERSION_GOAL_IDS`/`AUDIENCE_IDS`/`LIVE_MODULE_IDS` unions, `checkPageMeta`) and `src/lib/routes/page-meta.ts` (`FocusJobId` incl. `"understand-who-is-behind-it"` instead of `"why-us"`, loose `string` types, no registry/helpers). `app/[lang]/_page-frame.tsx` (the actual `PageFrame`/`ContextBand`/`ClosingCta` consumer) imports only the `pages/` module; `mitmachen`, `mitmachen/registrieren`, `dein-kalender` and `dein-kalender/bestellen`'s `page.meta.ts` files all import the `routes/` module instead, which is not structurally assignable to what `PageFrame` expects (different `focusJob` string literals, looser id types) — those four routes' `page.meta.ts` will not typecheck against `PageFrame` once wired in | `src/lib/pages/page-meta.ts` vs `src/lib/routes/page-meta.ts`; TS-006 D1 | high | This work package's four `page.meta.ts` files (`deine-region`, `deine-region/angebot`, `ueber-uns`, `ueber-uns/archiv`, `rechtliches`) import `@/src/lib/pages/page-meta` — the one `_page-frame.tsx` actually consumes — and all pass `pnpm check` end to end with `PageFrame`. The other four files are outside this work package's ownership and were not edited. **Resolved in M4 step 1:** all twelve `page.meta.ts` files import `@/src/lib/pages/page-meta`, `src/lib/routes/page-meta.ts` no longer exists anywhere in the tree or in its history (it was never committed), and the manifest set is now validated as a set by `src/lib/pages/manifests.test.ts` (TS-006-A1/A11) inside `pnpm check` | no (resolved) |
| 105 | TS-029's own open point #5: a sender surface (`/rechtliches`) has no focus job in the four-job sense, but `page.meta.ts`'s `focusJob` is a required, closed-set field with no `null`/sender variant — "this spec does not invent the value" | `app/[lang]/rechtliches/page.meta.ts`; TS-029 open point #5; TS-006 D1 | medium | `focusJob: "why-us"` — TS-006 D2 itself groups `/rechtliches` with `/ueber-uns` and `/ueber-uns/archiv` as "the sender surfaces", so the merged three-job closing block excludes the same job as those two. A documented assumption, not a spec decision — the value is still owed | yes (TS-006 owner: admit a `null`/sender `focusJob`, or confirm `why-us` as the reading for every sender surface) |
| 106 | TS-026 D2's own composition sheet names a `scene-block mechanism="embed"` inside block 1, alongside `hero-block`; the content artifact (`content/pages/deine-region/de.md` slot 1) authors one Aha-Frage/Text/CTA set, which became the hero's headline/lead/cta, and gives no second, distinct copy for a standalone scene instance | `app/[lang]/deine-region/page.tsx` block 1; TS-026 D2, TS-006 D7 | low | Not built as a separate `scene-block`: a second instance would duplicate block 4's `embed-frame` (the one concrete "embedded calendar" instance TS-006 D7 asks for) with no authored content of its own. D7's three-part shape (opener → one mechanism → one concrete instance) is carried across the hero's copy and block 4 instead | yes (TS-026 owner: confirm this reading, or write scene-specific copy for a literal block-1 `scene-block`) |
| 107 | Dummy-Content — `/deine-region` block 3 (interim module, D4): with no county anchor available in this run (Q-032 unbuilt) and no `/api/region/{county}/examples` ranking (Q-037), block 3 renders 6 generated example-place chips (`Beispieldorf Nord` … `Beispielhagen`) at `state="mocked"`, and the county-scoped counter is omitted entirely rather than estimated (D4's own rule) | `content/pages/deine-region/de.md` slot 3; TS-026 D4, A9–A11 | low | `place-example-set` renders the demo chips with `Demo-Daten`; `live-counters` is not rendered at all for this block (no backed figure exists to show). TS-026-A9/A11 (integration-level stubs for the county-examples/counter upstreams) are not-yet-M4: no `/api/*` route exists in this run to stub. **2026-09-12:** the block-6 half of this row is resolved (row 50). Block 3 is unchanged — its example chips are generated **in the page component**, not in the content artifact, so the content follow-up could not touch them | yes (events-api/geo-api teams, same blockers as rows 5–6; jan-henrik/gtm for the example-place naming) |
| 108 | TS-026-A4's static check ("no price string exists in a content source file") conflicts with the already-shipped `content/pages/deine-region/de.md` slot 5, whose authored prose states "Zusätzlich zu allem, was der 480-€-Tarif bietet…" — a literal price mention inside a content file. Not introduced by this work package: the content was written by the content-production playbook before this page existed, and this page never rewrites content (repository working rule) | `content/pages/deine-region/de.md` slot 5; TS-026-A4 | low | The page renders the sentence verbatim (content binds, per plan/guardrails.md) and additionally renders the enterprise price via `price-tag` (`"Auf Anfrage"`) — the 480 € comparison figure itself is never re-typed by the page, only quoted from content, so TS-026-A5 (480 € at most once, from the price component) still passes; TS-026-A4's literal "no price string in a content file" clause does not | yes (content/gtm: decide whether slot 5's prose should name the figure at all, given `price-tag`/`price-tag`'s exclusive-renderer intent, TS-006 D10) |
| 109 | Dummy-Content — `/ueber-uns` block 3 (proof stream, D5): `app/[lang]/ueber-uns/page.tsx` builds the six filled cards from a hard-coded `DEMO_PROOF` array with `demo: true`, because TS-005's relevance engine is not built | `app/[lang]/ueber-uns/page.tsx` block 3; TS-005 (unbuilt), TS-027 D5 | low | **2026-09-12 (content follow-up, group 2): the content side is resolved, the code side is not.** `content/pages/ueber-uns/{de,en}.md` slot 3 now names six **cleared** real proof elements and fills the reserved seventh place with a real, clearance-pending testimonial (row 51). The page still ignores the artifact and renders its own invented cards. The fix is a code change this workstream may not make: read the six elements off the slot instead of `DEMO_PROOF`. The reserved-slot tension recorded earlier stands — TS-027 D5/A6/A7 say "backfill: never", and the artifact's testimonial is a real `testimonial`-type record rather than a backfill of another type, so filling it is consistent with D5 once clearance lands | yes (M4 developer for the code; jan-henrik/content to confirm the reserved place may show a clearance-pending testimonial in the protected preview) |
| 110 | `/ueber-uns` team portraits and the origin section's village/founder images use the DEC-068 generated placeholders already minted for this exact page (`ueber-uns/dorf`, `ueber-uns/gruender`, `placeholders.manifest.json`) rather than `@schafe-vorm-fenster/people`'s real portrait files — this work package's page code never opens a hub package at request/build time (the same boundary `state/open.md` #54 already draws for `price-tag`/`origin-story`) | `app/[lang]/ueber-uns/page.tsx`; TS-027 D3/D7; state/open.md #54 | low | Both team members render with `notDepicting`/the "Foto gesucht" hatch, correctly per D7 ("never omitted, never a blank box"); no photo asserts anything untrue | yes (M3 content-pipeline owner, same as #54: resolve real portrait paths into props at generation time) |
| 111 | The pre-existing `deine-region/karte` placeholder slot (`placeholders.manifest.json`, generated at M2 for a `feature-benefit` gallery demo) is a hatched box captioned "Kartenausschnitt" ("map excerpt") — exactly the "hatched `ratio-map` box" / "placeholder shaped like a map" TS-026 D3 forbids anywhere on `/deine-region`, regardless of which `MediaRatio` token renders it | `placeholders.manifest.json`; `src/generated/placeholders/deine-region/karte.svg`; `src/components/gallery.tsx`; TS-026 D3 | medium | Not used anywhere on the built page. Two new placeholder slots were minted instead for this work package's own images — `deine-region/hero` ("Gebietsübersicht") and `deine-region/gebietsschnitt` ("Gebietsschnitt") — deliberately not map-labelled. The `karte` slot itself was left in the manifest and in `gallery.tsx` (component-inventory/dev-gallery territory, not this work package's file to delete) with a note explaining why it is unused | yes (design/component owner: retire or relabel the `karte` placeholder before `/dein-kalender` or any other page reaches for it) |
| 112 | `archive-row` took one additive prop, `demo?: boolean` (sets `data-demo="true"` on the row) — needed because the archive page renders the content artifact's dummy-content rows (row 1/52) and the mock rule requires rows to carry `data-demo`, which `archive-row` had no way to express | `src/components/archive-row/archive-row.tsx`; plan/component-inventory.md §4 TS-028; plan/guardrails.md mock rule | low | Backward-compatible (optional, defaults `false`); committed on its own pathspec commit, separate from the page | no (recorded per the ownership rule: "small additive prop additions … separate pathspec commit, say so") |
| 113 | `archive-filter`'s chip `id` has to equal the same label string `archive-row`'s `types` prop carries, because `archive-row` has one `types` prop for both the visible badge text and the `data-archive-type` filter attribute — there is no separate stable-id slot. `/ueber-uns/archiv` therefore filters by the German label ("Presse", not `press`), not by TS-028 D4's hub-vocabulary id | `app/[lang]/ueber-uns/archiv/page.tsx`; `src/components/archive-row/archive-row.tsx`; `src/components/archive-filter/`; TS-028 D4 | low | Filtering works correctly end to end (verified: selecting a chip shows exactly its rows, "Alle" restores the full set) — only the *identifier* is localized where D4 says it should not be | yes (component owner: add a second, id-only prop to `archive-row` so `archive-filter` can key on the hub id while still displaying the localized label) |
| 114 | TS-029-A14's "no skipped level inside any imported document" half fails: `content/legal/privacy-policy.md` itself skips from `## 5. Wenn du dich registrierst („Akteur")` (h2, line 100) straight to `#### Zweck` (h4, line 108) with no `###` between them — a pre-existing structural defect in the imported source document, unchanged by `shiftHeadings`, which applies one uniform shift and cannot fix a level that was already wrong before the shift | `content/legal/privacy-policy.md` lines 100–108; TS-029-A14 | medium | Not fixed: this page never rewrites legal text (repository working rule / Content & Translation role's "never rewrite legal text"). `e2e/pages/rechtliches.spec.ts` asserts the satisfiable half (one `h1`, one `h2` per section) and records this half as a skipped, named finding rather than silently weakening the assertion | yes (jan-henrik / legal source owner: insert the missing `###` heading in the Google Doc and re-import) |
| 115 | TS-029-A11 (production build fails while `#barrierefreiheit` has no document) and TS-029-A12 (axe-core, zero violations) are not exercised by this work package's e2e pass: A11 needs an actual `next build` with `VERCEL_ENV=production`, which this run's `next dev`/preview-oriented suite does not perform (the `page.tsx` throw guarding it is written and typechecks, per TS-029 D8); A12 needs an `axe-core` dependency, which is not installed and would need the stack-harmony ADR (plan/guardrails.md) this work package was not asked to run | `app/[lang]/rechtliches/page.tsx`; `e2e/pages/rechtliches.spec.ts`; TS-029-A11, A12 | medium | Both recorded as named, skipped tests rather than silently omitted or falsely marked passing — **Triage (PM, round 2): F-2-6, severity medium, fix-now** — the axe half only. Installing the instrument (stack-harmony ADR + one e2e sweep over the route table) unblocks TS-002-A1 for the whole gate; the A11 production-build half stays out of scope (plan/gate-2-scope.md §4). | yes (M4/CI owner: run an actual production build in CI for A11; stack-harmony a11y-testing dependency decision for A12) |
| 116 | TS-026-A13 (submitting the `/deine-region/angebot` quote form fires exactly one `request-licence-quote` event) is not-yet-M4: `envoy-form-mount` is a labelled mock with every field unnamed and no real submission target (Q-022, row 7), and no analytics/event-registry wiring exists anywhere in the tree yet (TS-012) | `app/[lang]/deine-region/angebot/page.tsx`; TS-026-A13, TS-012 | low | Recorded as a named, skipped e2e test. The "confirmation state" TS-026's composition sheet names for this route is in the same position — it depends on the real widget's success callback, which does not exist yet. **Resolved in M4 step 5:** `envoy-form-mount` takes a `ConversionBinding` and wraps its own submit; `/deine-region/angebot` passes `request-licence-quote`/`completed`. The mocked submit is its success signal, as row 129 already reads it for the order flow | yes (M4: envoy widget + TS-012 event registry, same blockers as rows 7/20) |
| 117 | `page.meta.ts`'s `liveModules: []` is used for `/deine-region/angebot`, `/ueber-uns/archiv` and `/rechtliches` (pure flow, no-live-data, and fully-static pages respectively), against TS-006 D1's own "≥ 1 live module" floor — the same contradiction `dein-kalender/bestellen/page.meta.ts` already recorded for its own form-flow route | `app/[lang]/{deine-region/angebot,ueber-uns/archiv,rechtliches}/page.meta.ts`; TS-006 D1 | low | Not resolved here, consistent with the existing precedent rather than inventing a fake live module to satisfy the field. **Resolved in M4 step 1 as a checked exception:** `NO_LIVE_MODULE` in `src/lib/pages/manifests.ts` lists the four routes with the determination that put each there, and `manifests.test.ts` fails a fifth route that declares none as well as an entry that no longer applies | no (tracked once; TS-006 owner: admit an empty `liveModules` array for pages with none by design) |
| 118 | `scripts/make-placeholders.mjs` deletes `src/generated/placeholders/README.md` on every run (`rmSync(OUT, {recursive: true})` clears the whole output directory before regenerating only the manifest's slots, and the README is not one of them) | `scripts/make-placeholders.mjs`; `src/generated/placeholders/README.md` | low | Restored via `git checkout` after running `pnpm placeholders` to add two new slots (row 111) — not fixed at the script level, since that script is outside this work package's ownership | yes (script owner: write the README back after clearing `OUT`, or exclude it from the clear) |
| 119 | `app/[lang]/_page-frame.tsx`'s `BreadcrumbTrail` is always given `label={d.nav.home}` ("Startseite") instead of the component's own default `"Seitenpfad"` — and `site-header`'s job nav also carries `aria-label="Startseite"` (`d.nav.home`), so the five second-level pages each have two `<nav>` landmarks with the identical accessible name | `app/[lang]/_page-frame.tsx`; `src/components/breadcrumb-trail/breadcrumb-trail.tsx`; `src/components/site-header/site-header.tsx` | low | Not fixed — `_page-frame.tsx` and `site-header.tsx` are outside this work package's ownership. `e2e/pages/deine-region.spec.ts`'s breadcrumb test disambiguates with `.filter({ hasText: … })` rather than relying on the accessible name alone — **Triage (PM, round 2): F-2-3, severity medium, fix-now** — a11y defect on the five second-level pages, three of them conversion pages; one prop in `_page-frame.tsx`. | yes (chrome owner: give the breadcrumb its own distinct accessible name, e.g. restore `"Seitenpfad"`) |
| 120 | Bug found and fixed: `photo-surface.tsx`'s no-image branch rendered `placeholder-surface` **instead of** the caller's `children`, so every `hero-block` without a `src` (every hero on the site today — no photography asset exists anywhere) silently dropped its `h1`, lead and CTA. Measured on `/mitmachen` and `/dein-kalender`: no `<h1>` in the rendered DOM at all | `src/components/photo-surface/photo-surface.tsx`; affects every page using `hero-block` without a photo | high | Fixed: a missing photo now renders the section's real background/gradient plus a "Foto gesucht" badge (`Badge tone="placeholder"`, not `placeholder-badge`, which stays reserved for `notDepicting`) alongside the caller's children, instead of replacing them. `placeholder-surface` itself is unchanged and still used verbatim for a photo slot with no content of its own (`/ueber-uns` origin photo, archive rows). All 651 unit tests plus the full e2e suite stay green after the fix | no (fixed; flagging for the other page-implementation work packages, whose hero/scene blocks were silently affected until now) |
| 121 | Bug found and fixed: `choice-group.tsx`'s `<form method="get">` baked `query` into the `action` URL, but a GET form submission **replaces** the action's query string with the serialized form fields only (the browser never merges the two) — so any carried-forward parameter (`?ort=`, an earlier answer) was silently dropped on every `choice-group` submission, breaking step continuity on any multi-step flow that uses it | `src/components/choice-group/choice-group.tsx`; used by `/mitmachen/registrieren` steps 2–3 | medium | Fixed: hidden `<input type="hidden">` fields now carry `query` forward, the same pattern `search-field.tsx` already used. Covered by `e2e/pages/registrieren.spec.ts` (TS-023-A2/A3/A9/A10, which all depend on `ort`/`wer` surviving a `choice-group` submit) | no (fixed) |
| 122 | Small additive component changes made while building TS-022–025 (each backward-compatible, no existing caller's behaviour changes): `button.tsx` gained an `href`/`newTab` external-link mode (needed for the registration handover's primary-treatment external CTA, which is neither a route nor a plain unstyled `<a>`); `outbound-link.tsx` gained `dataCta` (the `/dein-kalender` equal-weight briefing link needs `data-cta="equal-weight"` on the anchor itself); `publishing-path.tsx` gained `availabilityLabel` (content-authored, per-locale override of `status-badge`'s German-only default, needed for the EN page); `trust-block.tsx` gained `privacyLabel`/`dataProcessingLabel` (same EN-localization gap, its two legal-link labels were hardcoded German); `section-shell.tsx` gained `dataBlock` (renders a `data-block` attribute — TS-024's whole composition sheet is specified in terms of `data-block` ids, which `section-shell` had no way to carry) | `src/components/{button,outbound-link,publishing-path,trust-block,section-shell}/*.tsx` | low | All five are additive optional props; no existing call site or test needed a change (`pnpm check` green throughout). New component: `src/components/conversion-tracker/conversion-tracker.tsx` — the first real caller of `src/lib/analytics` (`ConversionTracker` fires on click via a `display:contents` wrapper so a Server Component page can hand a pre-rendered `Button`/`OutboundLink` to it as `children` without passing a function across the RSC boundary; `FireConversionOnMount` fires once on mount, for TS-025 D11's "fires when step 4 renders a code", which is a render-time trigger, not a click) | no (recorded; design review can confirm the two localization props read as intended) |
| 123 | TS-022-A11 and TS-024-A19 each carry an explicit, numbered acceptance criterion that a promise/claim ships **only** with a cleared backing (the 2022 permanence commitment; a hub record behind the trust-block's operations/AI sentences) — while the content artifacts also carry a `provenance: generated` demo version of exactly those sentences, added under the prototype-completeness override (`state/open.md` #45, "every slot ships something, never empty"). The two rules conflict for these two specific slots | `content/pages/mitmachen/de.md` slot 8; `content/pages/dein-kalender/de.md` slot "dein-kalender-6-trust-demo"; TS-022-A11, TS-024-A19 | medium | The specific, numbered AC wins over the general completeness note: `/mitmachen`'s closing CTA ships with no reassurance text, and `/dein-kalender`'s trust block ships the data-protection subject only — both ACs pass as literally written, and the generated demo sentences the content pipeline produced for these two slots are not rendered. Not a silent resolution: both content files already document the same tension in their own inline comments | yes (jan-henrik: confirm the AC-over-completeness-note precedent for these two slots, or decide the reassurance/trust sentences should ship despite being unbacked) |
| 124 | TS-023 D7 suppresses the context band on steps 2–3 and on the handover, and suppresses the closing CTA on steps 1–2 (the handover *is* the closing CTA); TS-025's composition sheet renders both **once**, after step 4 only. `PageFrame` (`app/[lang]/_page-frame.tsx`) has no step parameter — it renders the band/closing unconditionally from `page.meta.ts` — so both flow pages render their chrome via `SiteChrome` directly and hand-roll their own band/closing sections (same components, same `#context-band`/`#closing-cta` ids, own conditionals) rather than using `PageFrame` | `app/[lang]/mitmachen/registrieren/page.tsx`; `app/[lang]/dein-kalender/bestellen/page.tsx`; `app/[lang]/_page-frame.tsx`; TS-023 D7, TS-025 | low | Not a defect — `SiteChrome` is exported precisely for callers that need the chrome without the manifest-driven band/closing. Recorded because a later `PageFrame` enhancement (e.g. an optional `step`/`suppressBand`/`suppressClosing` prop) could let these two pages rejoin the shared component | no (chrome owner: optional convenience, not required) |
| 125 | Two configured-value placeholders introduced, both `Mock aktiv`: (a) the registration handover target — TS-023 D6 leaves the app's registration-entry path UNKNOWN, so `/mitmachen/registrieren`'s handover uses `{APP_ORIGIN}/registrieren` as a documented assumption; (b) the Google Calendar briefing URL — TS-016 D7 asks for "one configured value" but none exists anywhere in the tree, so `src/lib/live/briefing.ts` (`BRIEFING_URL`, env-overridable via `NEXT_PUBLIC_BRIEFING_URL`) is a new, small, shared constant every S3 placement (`/dein-kalender`, `/dein-kalender/bestellen`) reads, ready for `/deine-region` to reuse | `src/lib/live/briefing.ts`; `app/[lang]/mitmachen/registrieren/page.tsx`; TS-023 D6; TS-016 D7 | medium | Both real values are unknown from any source; a swap touches one constant each, never a page. Registered here rather than guessed silently | yes (app team: the real registration-entry path; ops: the real Google Calendar appointment-schedule URL) |
| 126 | TS-023-A6 ("a search resolving to a municipality with several communities does not advance") has no naturally-occurring fixture in the shared live-data mock: `mockSearchByZip` (`src/lib/live/mocks/geo.ts`) answers at most one place per postcode, so the ambiguous branch never occurs against today's demo data | `app/[lang]/mitmachen/registrieren/resolve-place.ts`; `src/lib/live/mocks/geo.ts`; TS-023-A6 | low | The page's own handling of an ambiguous result (D3: render every candidate as a chip, never auto-select) is built and unit-tested against a stubbed multi-suggestion `searchPlaces` result (`resolve-place.test.ts`), but not e2e-walkable today. Not a page defect — a shared-fixture gap — **Triage (PM, round 2): F-2-5, severity medium, fix-now** — one fixture in the shared live-data mock, so the gate sweep can discharge TS-023-A6 on a conversion path. | yes (live-data owner: either a real geo-api credential or one municipality fixture with several communities, so this branch becomes e2e-testable) |
| 127 | TS-025 D9 sets `noindex, follow` on `/dein-kalender/bestellen`, amending TS-011 D9 (which calls the route indexable) — a contradiction TS-025's own spec text already names. Implemented the meta-tag half (`generateMetadata` returns `robots: {index: false, follow: true}`, verified by `e2e/pages/bestellen.spec.ts` TS-025-A10); the `X-Robots-Tag` **response header** half is `proxy.ts`'s job (README: "the CSP, the HSTS variance and the X-Robots-Tag, on every response") and is not built here | `app/[lang]/dein-kalender/bestellen/page.tsx`; `proxy.ts`; TS-025 D9 vs TS-011 D9 | medium | Meta tag ships; header does not. A crawler honouring only the header would still index step URLs | yes (TS-011 owner: resolve the TS-011/TS-025 contradiction in writing; security/proxy owner: add this route's header once resolved) |
| 128 | `/mitmachen`'s live example (D5) uses a page-local mock (`app/[lang]/mitmachen/example-place.ts`, reference place "Groß Kiesow" — the specific editorial choice `state/open.md` #44 already names) and inline dummy event rows, rather than the shared `src/lib/live/places.ts` interface `placeEvents()`/`resolvePlace()`. Reason: TS-008 D1 has no row assigning this route a live-module position at all (an open point TS-022 itself names), and the shared mock's fixture set (`DEMO_PLACES`) does not include "Groß Kiesow" — only fictitious `Beispiel…` places, with no per-locale name field. `/mitmachen/registrieren` and `/dein-kalender/bestellen`, by contrast, do use the shared interface (`resolvePlace`/`searchPlaces` via `resolve-place.ts`) for their own place lookups, since those needed no page-specific place | `app/[lang]/mitmachen/example-place.ts`; `src/lib/live/places.ts`; TS-008 D1; TS-022 D5 | low | `selectExamplePlace()`'s selection policy (skip a zero-date candidate, fall back to the reference place) is unit-tested (TS-022-A7) and unaffected by which backend eventually answers it — swapping in `placeEvents()` once TS-008 D1 gets a row for this route touches this one small file. **Resolved in M4 step 3:** the module now runs on `placeEvents()` like every other live module; which place it names is row 137 | yes (TS-008 owner: assign `/mitmachen` a live-module position; live-data owner: decide whether "Groß Kiesow" belongs in the shared fixtures or stays a page-specific reference) |
| 129 | Mock aktiv, full mocked experience per the mock rule's row 2 ("the order flow mints a mocked `organizerId` and shows the full instant-embed experience with dummy embed code"): `/dein-kalender/bestellen` step 4 always renders the **successful** code-shown state (`state="mocked"` on `code-snippet`, dummy `demo-organizer-bestellen` id) rather than TS-025 D7's "cannot be issued synchronously" fallback note — per this work package's own dispatch instructions. `buy-calendar-licence` fires (`stage: completed`) whenever step 4 renders, via a mount-time trigger (`FireConversionOnMount`), not a click | `app/[lang]/dein-kalender/bestellen/page.tsx`; TS-025 D7, D11 | low | Consistent with row 2's pre-existing `Mock aktiv` entry; recorded again here because it is this work package's concrete implementation of that row | no (superseded once Q-026's synchronous-provisioning demand is answered — row 2) |
| 130 | Real conversion-event wiring added for the first time anywhere in the tree: `register-as-publisher`/`handover` (registration handover click), `request-product-briefing`/`handover` (every briefing link on `/dein-kalender` and `/dein-kalender/bestellen`), `buy-calendar-licence`/`completed` (order step 4 render). All three resolve in `src/lib/analytics/event-registry.ts` as `wired: true` with a matching `stage`, so `eventRegistryViolations()` stays clean. `getAnalyticsTracker()` still always returns the mock (`src/lib/analytics/README.md`), so every fire is a labelled `console.info`, never a network call | `app/[lang]/{mitmachen/registrieren,dein-kalender,dein-kalender/bestellen}/page.tsx`; `src/components/conversion-tracker/conversion-tracker.tsx`; TS-012 D2/D4 | low | e2e-verified via the console log (`e2e/pages/{registrieren,dein-kalender,bestellen}.spec.ts`) rather than the real vendor, consistent with the mock rule. **Extended in M4 step 5:** `save-calendar-to-homescreen`/`handover` and `request-licence-quote`/`completed` now have call sites too, and `src/lib/analytics/call-sites.test.ts` makes the registry's `wired` flag checkable in both directions | no (recorded; flips automatically once row 12/Q-040 turns the real eTracker adapter on) |
| 131 | Cache Components is on (`cacheComponents: true`, open row 76). Four routes carry `export const instant = false` and are dynamic (ƒ) rather than prerendered: `/dein-ort`, `/dein-ort/starten`, `/mitmachen/registrieren`, `/dein-kalender/bestellen`. Each reads a request value that *is* the page — TS-020 D2 keys five states on `?ort=`, and the three flow routes' step is the query — so there is no static shell to split off that a visitor would ever see | `next.config.ts`; `app/[lang]/{dein-ort,dein-ort/starten,mitmachen/registrieren,dein-kalender/bestellen}/page.tsx`; TS-009 D1 | medium | Eight of twelve routes prerender (six fully static ○, two partial-prerender ◐ with cached-island holes). The four blocking routes still compose from cached pieces — `_content.ts` and `_islands.tsx` are `use cache` — so their per-request work is a cache read, not an upstream call. Splitting a flow route's step body into a dynamic island would prerender a skeleton and nothing else; for `/dein-ort` a skeleton fallback is what TS-020-A10 forbids by name | no (recorded reading; revisit if TS-009 D1 wants a shell on a per-request page) |
| 132 | **A dynamic route cannot hydrate under DEC-045's hash-only CSP.** Next's flight payload (`self.__next_f.push([1,…])`) is generated at request time and page-specific, so `scripts/generate-csp-hashes.mjs` — which scans the prerendered HTML in `.next/server/app` — can never produce its hash. Measured against the local production build with `VERCEL_ENV=preview` and the hash asset present: `/mitmachen/registrieren` reports one CSP violation and does not hydrate at all (React error #412, no `onClick`, no conversion event); `/dein-ort` reports one and hydrates. It also explains why an in-route `notFound()` renders an empty document (row 76's note and `app/global-not-found.tsx`'s) | `src/lib/security/csp.ts`; `scripts/generate-csp-hashes.mjs`; DEC-045, TS-014 D7 | high | Not a preview problem and not introduced by M4: the three flow routes were already dynamic (reading `searchParams` always was), and on a Vercel **preview** the hash asset does not reach the proxy (rows 21/31), so `'unsafe-inline'` applies and every route hydrates. It bites **production**, where D7's ban is unconditional — and it now covers a fourth route. A nonce-based `script-src` (Next propagates a `'nonce-…'` it finds in the CSP header to its own scripts) is the mechanism that covers request-time scripts; choosing it is a DEC-045 amendment, not a page work package's call | yes (TS-014/DEC-045 owner: hashes cannot cover request-time scripts — decide nonce or accept dynamic routes shipping unhydrated in production) |
| 133 | `e2e/a11y.spec.ts` (new, another work package's) reports twelve serious `color-contrast` violations, all pre-existing and none introduced by M4: `outbound-link`'s `secondary` text (`#b6de6d` on `#f9fbf7`, 1.47:1) wherever a live module carries an onward link — `/` and `/dein-ort`, both viewports, both languages — and `/ueber-uns`'s origin-story `proof-card` claim (`#f9fbf7` on `#eef2e9`, 1.08:1) | `src/components/outbound-link/outbound-link.module.css`; `src/components/origin-story/*`; TS-002-A1 | high | **RESOLVED (round 2b, 7da518f, filed as F-2-28/F-2-29).** Not a token choice after all: both components already had the correct token (`--color-neutral-ink`) available, they just did not re-assert it with enough specificity/explicitness against `section-shell`'s ambient dark-ground rule. `outbound-link.module.css`'s `.secondary` became `.link.secondary` (specificity 0,2,0, beats `section-shell`'s 0,1,1 regardless of import order); `proof-card.module.css`'s `.claim` got an explicit `color: var(--color-neutral-ink)`. No new colour literal, `pnpm check:brand` still passes. `e2e/a11y.spec.ts`: 49/49 passing, 0 serious/critical violations across all 24 routes x 2 viewports | yes (design-system owner: which token pair the secondary link and the on-photo proof claim use) |
| 134 | `/gibt-es-nicht` and `/uk/mitmachen` render an empty 404 body under `next dev` while passing against `next start` (TS-004-A4). The `afterFiles` rewrite added in M4 sends an unknown first segment to Next's own `/_not-found`, which is prerendered in a production build (hashes in the set, full body) and is not in dev | `src/lib/routes/next-routing.ts`; `e2e/routes.spec.ts`; row 132 | low | Same root cause as row 132 — a request-time inline script with no hash. `playwright.config.ts` runs the local suite against `next dev` and CI against `next start`, so the two tests are red locally and green in CI; verified by running the same spec against the production build | no (closes with row 132) |
| 135 | `/dein-ort` grows no proof stream, although `page.meta.ts` declares four `proofSlots` and M4 wired the relevance engine into the other five proof surfaces. TS-020-A6 forbids a quote component, an attributed sentence or a paraphrase anywhere in its blocks 2a while every candidate is `usage_rights: unverified` (Q-014) | `app/[lang]/dein-ort/page.tsx`; TS-020-A6, TS-005; Q-014 | low | The four value stories carry the page's credibility through their live example rows instead, which M4 moved onto the shared live layer. The selection is one call away the moment a quote clears | no (opens with Q-014) |
| 136 | The stage-0 anchor is configuration, not data: with no request geo handed down (TS-010's own open point 1) and no coordinate→hierarchy endpoint (Q-032), the placeless variants of every live module speak about one configured reference community. `Mock aktiv` | `src/lib/pages/live-anchor.ts`; TS-010 D1; Q-032 | medium | `STAGE_ZERO_ANCHOR` names the demo backend's own first community, so every payload it produces arrives `demo: true` and badges itself. `resolveLiveAnchor()` is the one function that would gain a second source when the proxy hands the request geo down — no page changes | no (closes with Q-032 and the proxy handover) |
| 137 | `/mitmachen`'s live example renders the stage-0 anchor rather than TS-022 D5's configured reference place: `selectExamplePlace()` still decides (its unit tests are unchanged) and answers `gross-kiesow`, but that slug resolves only against the real geo-api, which this environment has no read token for — so the module would render nothing at all | `app/[lang]/mitmachen/page.tsx`; TS-022 D5; row 128, live-data row 5 | low | Both slugs go through the same `resolvePlace()` call, so provisioning `GEOAPI_READ_TOKEN` swaps it back with no code change. Row 128 (the page-local mock) is resolved by the same commit: the module now runs on `src/lib/live/places.ts` like every other one | no (closes with the geo-api read token) |
| 138 | TS-011 D5 is not discharged: a page's `<title>` and meta description still come from the dictionary's M2 placeholders (row 103 stands). D5 is explicit that both come from `seo.title`/`seo.description` in the page's **content frontmatter** and that neither "is ever derived at runtime from body copy or from the `h1`" — so the deriving fix D5 forbids is the only one this work package could have made alone | `src/lib/routes/metadata.ts`; `src/domain/content-frontmatter.schema.ts`; `content/pages/**`; TS-011 D5, WEB-F-089; row 103 | medium | Left as row 103 describes it. Closing it needs a schema field plus twelve content files in both languages — `content/` is the content playbook's, not this work package's. The JSON-LD graph reads the same source as `generateMetadata`, so both move together the moment the frontmatter carries the fields **RESOLVED in the M5 fix round (18c94ac, F-2-72).** `content/pages/**` carries an `seo` block per route, `src/lib/content/page-seo.ts` reads it through the loader's own frontmatter parser, and `metadata.ts` stands nothing in: an absent description is omitted rather than replaced. All 24 (path, language) pairs green on `check:seo-budget`; `e2e/metadata-compliance.spec.ts` fails on any internal identifier in a title or description | no — resolved (was: content pipeline + content playbook) |
| 139 | TS-023-A9/A10 and TS-025-A11 fail against a **local** production build and pass against `next dev` and (expected) against a Vercel preview. Both assert a conversion event fired by a client `onClick` on a route that is dynamic, so row 132's unhydrated page is the cause: no hydration, no handler, no event | `e2e/pages/{registrieren,bestellen}.spec.ts`; row 132 | medium | Reproduced and traced to the CSP violation rather than to the wiring: the same `ConversionTracker` on the static `/` fires and is asserted by TS-019-A13 against the same production build. The preview smoke is what settles it, because a preview ships `'unsafe-inline'` (rows 21/31) | no (closes with row 132) |
| 140 | **The editorial gate of TS-007 D11 now exists, and every content artefact is still `draft`.** F-2-40 built the gate the criterion always described: `loader.ts` renders `approved`/`imported` only in a production build and everything in preview and locally, and `pnpm check:content` exits non-zero on an unapproved artefact when `VERCEL_ENV=production`. The 22 page artefacts under `content/pages/**` and the six files under `content/legal/**` were not flipped: D11 fixes that `approved` is set **only at the editorial decision point by a person** (`reviewed_by`, `reviewed_at`), so an agent setting it would be the run granting itself the clearance the gate exists to require. `state/findings/round-2.md` F-2-40 and `plan/round-3.md` both record the same reading ("the gate ships with everything still `draft`") | `src/lib/content/lifecycle.ts`, `loader.ts`, `validate.ts`; TS-007 D11/A14; F-2-40 | high | The prototype is unaffected — it is built on preview and locally, where D11 renders `draft` and the review happens on the rendered page, which is exactly what A14 describes. What is blocked is a **production** build: it would today contain no page copy at all. Flipping an artefact to `approved` is one line per file plus `reviewed_by`/`reviewed_at`, and is the first step of the pre-go-live hardening round (row 13, phase 2) | yes (jan-henrik: per-artefact editorial clearance, after the content review of row 13) |
| 141 | **The strict-frontmatter clause of F-2-46 is done for pages, open for legal.** `PageFrontmatterSchema` and `SlotMetaSchema` now reject unknown keys, so a misspelt `status`/`reviewed_by` can no longer pass `check:frontmatter` and be invisible to the D11 gate. `LegalFrontmatterSchema` was **not** made strict: `content/legal/accessibility.md` is the one legal file that is generated rather than imported (row 21), and it carries `provenance: generated`, `derived_from`, `generated_by`, `generated_at` — the very keys TS-007 D10 forbids on a `legal-section`. Making the schema strict would force removing the dummy-content marking that `plan/guardrails.md` requires. Spec against spec, so it is recorded rather than decided | `src/domain/content-frontmatter.schema.ts`; TS-007 D10/A11 vs plan/guardrails.md dummy-content rule; F-2-46 (open list), F-2-40 | medium | Legal frontmatter keeps validating non-strict until the `content/legal/<locale>/` restructure of F-2-46 happens; the accessibility statement keeps its generated marking in the meantime | yes (TS-007 owner: does a *generated* legal placeholder belong to D10's `legal-section` type at all, or to its own type?) |
| 142 | **`--color-status-error` and `--color-status-success` fail AA on a dark ground.** The new token-contrast guard (`pnpm check:contrast`, TS-002-A3, F-2-43) measures 68 token pairs across the four themes `@schafe-vorm-fenster/brand-design@0.1.3` declares and is green — with two pairs scoped to the light themes: the two status colours are palette-level, the sheet declares no dark variant of either, and on `[data-theme="dark"]`'s `--bg` they measure 2.82:1 and 2.67:1 (3.19:1 / 3.02:1 on the dark high-contrast ground). The site is light-only by declaration (`app/styles/base.css`: `color-scheme: light`; `[data-theme]` is set in no file), so no visitor can reach that combination today | `scripts/check-contrast.ts`; `@schafe-vorm-fenster/brand-design@0.1.3` `tokens/svf-tokens.css`; TS-002-A3 | medium | Scoped, not silently skipped: the two rows carry an `onlyThemes` line naming the reason, so the day the site offers a dark theme the guard fails until the token package ships a dark status pair. The fix belongs upstream in the brand package, not in this repository — `app/styles/brand.css` could override it, but TS-017 D3 wants brand values reconciled back into the package (same open point as `--color-placeholder-ground` and `--font-size-label`) | yes (brand-design owner: add dark variants of `--color-status-*`, or state that the site stays light-only) |
| 143 | **`pnpm check:terms` (TS-026-A8, F-2-43) is written and green-able but not yet in the `pnpm check` chain.** The guard implements A8 in full — the response-time wording may occur in `src/components/response-promise/` and in no other module, and in no content file at all. A8's **content clause is now green**: package A removed the `deine-region` claim in `9e0d7ff` (F-2-57). Three module-level violations remain, all package B's: `src/components/gallery.tsx:1112` and `src/components/live-modules-and-conversions.test.tsx:246,247`. Wiring a red guard into `pnpm check` mid-round would have blocked every other developer's commit hook on lines package C does not own | `scripts/check-terms.ts`; TS-026-A8/A7, TS-026 D5, F-2-57, F-2-43 | medium | Run it with `pnpm check:terms`. The moment those five lines are gone the guard is one line in `package.json`'s `check` chain away from blocking — and the criterion says "removed, never softened", so the demo sentence has to go rather than be rephrased | no (developer task: fold into `check` once F-2-57 and the two demo strings land) |
| 144 | **Two `@schafe-vorm-fenster/brand-design@0.1.3` type-scale values fall below TS-002-A10's 15px floor, and one bare literal existed outside the token import — F-2-44's fix stays local rather than upstream.** `--font-size-label` ships at `0.875rem` (14px); `components.css`'s own `--type-label-size` (0.75rem/12px) and `--type-microlabel-size` (0.6875rem/11px, the design system's own documented 11px placeholder-badge value, SRC-014 §Photo surface) were both below it too. All three are corrected in `app/styles/brand.css` — the one file TS-017 D3 lets a brand value enter through — rather than at each of the ~15 call sites; `--type-label-size`/`--type-microlabel-size` now collapse to the same 15px value pending the fuller type-scale redesign the finding scoped out (moving the whole scale into the token import, the ~30 `font-weight` literals, teaching `scripts/check-brand.ts` the size/weight halves) | `app/styles/brand.css`; `app/styles/components.css`; `@schafe-vorm-fenster/brand-design@0.1.3` `tokens/svf-tokens.css`; TS-002-A10; F-2-44 | medium | `pnpm check:brand` stays green (no new literal outside the token file); a script (ad hoc, not wired into `check`) confirmed these were the only three sub-15px roles and the only bare non-token `font-size` in `app/styles/**`/`src/components/**` (`event-row.module.css`'s `28px` day-figure, now `var(--type-figure-size)`, a new role added to `components.css` for the one fixed-size literal) | yes (brand-design owner: raise `--font-size-label` to ≥15px upstream, same reconciliation as `--color-placeholder-ground`; design-system owner: decide the label/microlabel roles' real hierarchy rather than the interim collapse-to-15px) |
| 145 | **F-2-39 and F-2-56 (Suspense boundaries + PPR shell on the eight non-blocking routes) could not be built without breaking a foundational, already-tested requirement, and are left unresolved rather than shipped broken.** The plan was to wrap the D3 islands (`app/[lang]/_islands.tsx`) in `<Suspense>` with `moduleSkeleton` fallbacks, per TS-009 D7/A3/A9 and the migration guide's own pattern (`node_modules/next/dist/docs/01-app/02-guides/migrating-to-cache-components.md`, "cookies, headers, and searchParams" / "wrap in or move into Suspense"). Two independent blockers surfaced once built and measured: (1) `PlaceDatesIsland` and `NearbyIsland` are shared between the eight allowed routes and the two blocking ones that use them (`/dein-ort`, `/dein-ort/starten`) — the only reliable per-call discriminator available without editing package A's `page.tsx` files (`announced`, documented as "true where arriving content changes the page's meaning (`/dein-ort`)") exists for `PlaceDatesIsland` but not for `NearbyIsland`, and even the `announced`-based split was falsified once mid-round when package A's F-2-30/F-2-49/F-2-61 rework removed the `invitation` prop this row's first draft had relied on — a second, independently-confirmed instance of the same shared-component problem; (2) more fundamentally, **any** `<Suspense>` boundary is, per Next's own glossary ("Suspense boundary" / "Static Shell": "Suspense boundaries define where the static shell ends and streaming begins"), a point whose real content can only reach the DOM via the client-side RSC replacement script — confirmed empirically: wrapping even an already-`use cache`d, fast-resolving island in `<Suspense>` left the fallback permanently in place in a `javaScriptEnabled: false` context, failing `TS-019-A11` ("with JavaScript disabled the page is complete") on `/`, a criterion this codebase enforces repeatedly and by design (`e2e/pages/{home,archiv,registrieren,rechtliches,dein-ort,dein-ort-starten}.spec.ts`). The two requirements are in direct tension for this architecture: TS-009's Suspense/PPR-shell mechanism and the site's progressive-enhancement guarantee cannot both hold for the same island. `_islands.tsx` was left at its original, fully-blocking shape; `moduleSkeleton` stays written and uncalled, exactly as F-2-39 found it | `app/[lang]/_islands.tsx`; TS-009 D7/A3/A9, TS-009-A2 (F-2-56); TS-019-A11 and the other no-JS-completeness criteria; F-2-39, F-2-56 | high | Not fixed this round — the guardrail against rewording an unsatisfiable criterion applies to whichever of TS-009-A3/A9/A2 or the no-JS criteria the architecture owner decides must yield; this run does not decide it silently by shipping one broken | yes (rendering-and-resilience spec owner: does TS-009's Suspense/PPR-shell requirement apply to routes that also carry a no-JS-completeness AC, or are the two mutually exclusive by design — and if PPR is kept, is a no-JS regression on those specific routes accepted, or does another delivery mechanism apply) |
| 146 | **F-2-68 is resolved on 23 of 24 measured (route × viewport) pairs; one narrow residual remains on `/mitmachen` at 1280×800.** The font-swap reflow (the finding's actual root cause) is fixed via a metric-matched `@font-face` fallback (row 144's neighbour in `app/styles/brand.css`) calibrated by measuring real vs. fallback text width/ascent/descent in the browser (`canvas.measureText`, not the `OS/2.xAvgCharWidth` field — verified wrong first, see the file's own comment) — this took the deterministic worst-case (fonts artificially delayed) shift on `/` from 94–174px down to 0.6–2.4px, and `e2e/layout-stability.spec.ts` (new, TS-009-A8) is green on all twelve routes at both DEC-067 viewports except this one. The residual: `/mitmachen`'s hero headline ("Ein Foto vom Flyer per WhatsApp, und der Termin steht im Kalender?") sits close enough to a line-wrap boundary at 1280px that the ~1–2% width residual any metric-matched fallback still carries is enough to tip it onto a different line count between fallback and real font; `photo-surface`'s content block is vertically centred within a fixed-height section (`src/components/photo-surface/photo-surface.module.css`), so a one-line difference visibly moves the `h1` (measured: 32px, exactly one line). This is a different, harder class of residual than the smooth width drift the calibration fixes — a discrete wrap-boundary tip that no single-percentage `size-adjust` value can guarantee against for every possible headline length and viewport width | `app/styles/brand.css`; `src/components/photo-surface/photo-surface.module.css`; `e2e/layout-stability.spec.ts`; TS-009-A8; F-2-68 | low | Reproduced deterministically (font requests delayed via `page.route`) and via the real, undelayed test twice in a row — not a flake. Two paths exist, neither taken this round: change `photo-surface`'s content alignment from centred to top-anchored (a visual-design change beyond "fix the font swap"), or accept occasional single-line-height CLS on long-headline pages near a wrap boundary as the residual cost of a fallback-font strategy | yes (design-system owner: is `photo-surface`'s vertical centring worth keeping given this sensitivity, or should hero content anchor to the top) |
| 147 | **A bare `pnpm build && pnpm start` does not reproduce the preview — it reproduces *production*, and the prototype has no production content.** `.env.production` (working copy, gitignored, dated before this run) sets `VERCEL_ENV="production"`, and Next loads `.env.production` for `next build`/`next start` but not for `next dev`. TS-007 D11's editorial gate then admits `approved`/`imported` only, every page artifact is `status: draft`, and the built site renders its chrome with **no page copy at all** — `/mitmachen` came out at 1 355 characters against 3 329 in dev, with an empty `<h1>`. The gate working as specified, not a defect; but it silently invalidates any local production-build measurement taken without the flag, and it cost this round an hour and ~40 red e2e cases before it was traced | `.env.production`; `src/lib/content/lifecycle.ts`; TS-007 D11/A14 | medium | The M5 round verified against `VERCEL_ENV=preview pnpm build && VERCEL_ENV=preview pnpm start`, which is the deployment target the preview actually is. Belongs in `plan/process.md` and `CONTRIBUTING.md` as *the* local production-build command — or `.env.production` should not carry `VERCEL_ENV` at all | yes (process owner: write the command down; Jan: should the pulled `.env.production` keep `VERCEL_ENV`?) |
| 148 | **The hydration half of the suite cannot pass against a local production build that carries its own CSP hash asset** — row 132's consequence, now measured end to end. With `.next/static/security/csp-script-hashes.json` present the proxy serves a hash-only `script-src`, the request-time flight payload has no hash, nothing hydrates, and **30 of 471** e2e cases fail (every conversion event, the archive chips, the block-1 reveal, `/rechtliches`'s back-to-top). Removing the asset before `next start` puts the local server on the same `'unsafe-inline'` branch a Vercel preview uses (`csp.ts`, `isPreview && !hasHashes`) and the suite is green. Not a new finding; what is new is that the whole local production-verification path depends on knowing it | `src/lib/security/csp.ts`; `scripts/generate-csp-hashes.mjs`; rows 132, 139 | medium | The M5 round ran the hydration suite with the hash asset removed and the CSP/no-JS assertions against a build that kept it — two runs, each measuring what it can | no (closes with row 132's nonce decision) |
| 149 | **`app/global-not-found.tsx` is a blocking (`ƒ`) route now.** It reads the 404's language off a request header (row 37), which is runtime data, so it can no longer be prerendered. A `<Suspense>` boundary — the framework's other answer — is the one thing this surface may not have: the fallback is what a visitor without JavaScript would be left with, and TS-004 D6 asks for a complete document. Nothing is lost (the body is static markup once the language is known, and a 404 is not a cached surface), but the 404 is a function invocation per request rather than a static asset | `app/global-not-found.tsx`; `src/lib/routes/not-found-routing.ts`; TS-004 D6, TS-009 D1 | low | Recorded reading, not a question: the alternative is a German-only 404 on English URLs, which row 37 already refused. `generateMetadata` reads the same header, so the `<title>` follows the body | no (revisit if TS-009 D1 wants the 404 in the static set) |
| 150 | **`proxy.ts` runs a place lookup for two paths, and the page repeats it.** `/dein-ort` and `/dein-ort/starten` with `?ort=` resolve through `place-hop.ts` → `resolvePlaceOutcome` before the route renders, because a page `redirect()` cannot produce an HTTP redirect under Cache Components (F-2-49); the pages kept theirs as the second line of defence. On a request that does **not** hop that is the same resolution twice, and `resolvePlace()` is an uncached direct client call — against a real geo-api up to four upstream calls and ~1.6 s of added TTFB on the founding path, plus a cache key per distinct visitor input. It also puts `src/lib/live/**` (and the mock fixtures) into the proxy bundle, which has no matcher: 11 modules / 55 KB before, 34 / 137 KB after, loaded on every request including assets. Per-request CPU is nil — `placeHopRoute()` answers after four string comparisons — the cost is bundle and cold start. `src/lib/live/boundary.test.ts` walks only `app/` and `src/`, so `proxy.ts` is outside every BFF-boundary assertion although it is now a client caller | `proxy.ts`; `src/lib/routes/place-hop.ts`; `src/lib/live/places.ts`; `src/lib/live/boundary.test.ts`; TS-021-A7, TS-008 D7 | medium | Acceptable while the backend is an in-memory mock and the runtime cache absorbs the second read; a throw in the hop falls through to the render, so an upstream that cannot answer never costs a visitor her page. Two fixes named by the differential review: pass the verdict down on a second request header so the page short-circuits, and split a `placeOutcome`-only entry point out of the live layer | yes (live-data owner, at the geo-api hardening: lookup in the proxy, or move the hop behind the BFF?) |
| 151 | **The German legal bodies on `/en/legal` carry no `lang="de"`** (WCAG 3.1.2, Language of Parts). The English disclaimer above them now says the texts are German (F-2-74), so a sighted reader is told; a screen reader still reads German prose in an English voice. The markup belongs to `legal-section`, which neither F-2-74 nor F-2-41 owned | `src/components/legal-section/**`; `app/[lang]/rechtliches/page.tsx`; TS-029, row 53 | medium | Not fixed in M5: a third finding's worth of work in a component no finding assigned. One `lang` attribute on the imported body wrapper closes it | yes (component owner) |
| 152 | **`pnpm check:terms` is still outside the `check` chain** — row 143 unchanged. The M5 round did not clear its three remaining module-level violations (`src/components/gallery.tsx:1112`, `src/components/live-modules-and-conversions.test.tsx:246,247`), so TS-026-A8 still fails | `scripts/check-terms.ts`; row 143 | medium | Deliberately not swept in: no finding assigned it, and A8 says "removed, never softened", so the demo sentence has to go rather than be rephrased — a content decision | no (one line in `package.json` once the three lines are gone) |
| 153 | **`ASSET_EXTENSIONS` is a closed list and now decides 404s site-wide.** Before this round it only governed what a landing-only domain let through; `isUnservablePath()` consumes it on every domain. `public/` does not exist today, so there is no live defect — but the list has no `.txt`, `.xml`, `.pdf`, `.mp4`, `.webm`, `.zip`, `.ttf`, `.otf` or `.wasm`, and the moment someone drops `public/agb.pdf` in, the proxy answers 404 for a file that is on disk. (`/sitemap.xml`, `/robots.txt`, `/llms.txt` survive only because they are D1 rows.) | `src/lib/routes/landing-domain.ts`; `src/lib/routes/not-found-routing.ts` | low | **RESOLVED (round 4, 9591a02, with F-3-13).** The list no longer decides 404s on its own. `isUnservablePath()` asks `isServableAssetPath()`, which is `/_next/**` plus `PUBLIC_FILES` — a named list of the files this repository really ships, empty today because `public/` does not exist. `landing-domain.public-files.test.ts` walks `public/` and fails if a file in it would be answered 404, which is the guard this row asked for; `ASSET_EXTENSIONS` keeps its original, narrower job in the landing-only rule, where a missing extension can no longer produce a 404 for a file on disk | no |
| 154 | **`ReservedChipRow` hand-copies `chip.module.css`'s class composition** (`archive-filter.tsx`). F-2-69's whole premise is that the reserved box wraps to the same number of lines as the real chip row at 360/768/1024. If `Chip` changes its classes, adds a wrapper or resizes its glyph, the geometry diverges and the 262 px shift returns — and nothing fails, because jsdom has no layout and the only guard is `e2e/layout-stability.spec.ts`, which needs a running server | `src/components/archive-filter/archive-filter.tsx`; `src/components/chip/**` | low | Named by the differential review. The structural fix is `Chip` taking `as="span"` (or exporting a `chipClassNames()` helper) so the reserved row renders through `Chip` itself and cannot drift | no (component owner) |
| 155 | **`landmark-unique` (moderate) on `/` and `/dein-ort`, both viewports, both languages** — surfaced by F-3-1's change from impact-based to presence-based failing in `e2e/a11y.spec.ts`, which is what the finding asked for. The two `role="search"` forms (block 1's focus search and block 4's closing repeat) carry no accessible name, so they are two identical search landmarks | `src/components/place-search/**`; `app/[lang]/page.tsx`; `app/[lang]/dein-ort/page.tsx`; TS-002-A1 | medium | Not a markup fix and therefore not this round's: giving each form a distinct accessible name is a copy decision, and TS-006 D6 requires the closing block to repeat block 1 with "no new text". Named in `KNOWN_OPEN_RULES` in `e2e/a11y.spec.ts` and printed on every run, so it cannot go quiet again | yes (content/design: two names, or one name and an `aria-hidden` landmark role on the repeat) |
| 156 | **`heading-order` (moderate) on `/rechtliches` and `/en/legal`** — the same presence-based sweep. `<h6>Zweck</h6>` sits inside the imported legal bodies | `content/legal/**`; `src/components/legal-section/**`; TS-029, TS-002-A1 | low | The heading levels are the imported document's own, so changing them is a content change, not a component one — and the legal texts are imported verbatim by decision. Named in `KNOWN_OPEN_RULES` and printed on every run | yes (legal-content owner: may the imported heading levels be normalised) |
| 157 | **F-3-2's `photo-surface` half — the hero on `/` and `/dein-ort` is the measured LCP element and cannot carry `loading` or `fetchpriority`.** Its image is a CSS `background-image` (`--photo-image: url(…)`), while TS-003 D2 declares **text** as the LCP element for both pages | `src/components/photo-surface/**`; TS-003-A8, TS-003 D2 | medium | The `/ueber-uns` half is fixed (2392913): the declared LCP image is `loading="eager"` + `fetchPriority="high"`, asserted in `e2e/lcp-image.spec.ts`. This half is a TS-003 D2 conversation rather than a page fix — A8's second and third clauses are unreachable for those two pages as long as the hero is painted with a CSS background. Travels with F-3-3 to the performance owner | yes (rendering/performance owner: does the hero become an `<img>`, or does D2 name it as the LCP element) |
| 158 | **`/` transiently renders a third place-search input during hydration, with a duplicate DOM id.** Observed while writing F-3-12's spec against the local production build: `page.locator('#ort-suche-fokus')` resolved to **two** elements mid-hydration (three `input[name="ort"]` in total), and one element after `networkidle`. A duplicate `id` in a hydration window is invalid HTML while it lasts, and it is a plausible second mechanism behind F-3-12 itself — a second click landing on an element that is about to be replaced | `app/[lang]/page.tsx`; `src/components/place-search/**`; F-3-12 | low | Not chased in round 4: the finding it was found under is fixed by its own guard, the duplicate is gone once the page settles, and identifying which render produces the extra copy is a Cache-Components question rather than a page one. `e2e/search-double-submit.spec.ts` waits for `networkidle` and takes `.first()`, with the reason in a comment rather than as a silent workaround | yes (rendering owner: why does the flight payload render block 1's search a second time) |
| 159 | **F-3-12 was not reproducible locally, in four gestures.** The finding measured `/dein-ort?ort=` — the parameter empty, the typed value gone — on the production build and the preview, 100 % on the 404's recovery widget. `e2e/search-double-submit.spec.ts` tried two synchronous `click()`s in one task, `dblclick`, and two Playwright clicks with and without a beat between them, against `next dev` **and** against the local production build: the value survived every time | `e2e/search-double-submit.spec.ts`; `src/components/search-field/search-form.tsx`; F-3-12 | low | The guard is shipped anyway (efe16c3) — a second submit inside the same navigation is never wanted — but it is a fix against the described mechanism rather than against a reproduction this round owns. The spec is a regression net that is green on both sides of the change locally. QA's retest on the preview is what would confirm or refute it | no (QA: re-drive the gesture on the preview at the retest) |
| 160 | **Clearance register for the protected preview (content follow-up, group 2).** Jan's instruction was to replace generated dummy content with real sourced text wherever a source exists. Real proof and media-echo items whose `usage_rights` is `unverified` or absent (Q-014, Q-045) are therefore used with `provenance: sourced`, **without** `demo: true`, and are visible in the protected preview only. **Go-live clears them or drops them — this row is the single place that rule is recorded.** Pending items by page: `/dein-kalender` slot 5 — `wendt-rubkow`, `zschiesche-gross-kiesow`, `eichler-wasserschloss-quilow`; `/deine-region` slot 6 — `eichler-wasserschloss-quilow`, `lehre-lelender`; `/ueber-uns` slot 3 — `kulturlandbuero-broellin`; `/ueber-uns/archiv` slot 2 — all 31 media-echo entries. The four testimonial records carry an explicit note in their own `Rights` section: "nicht für neue öffentliche Flächen verwenden" | content/pages/{dein-kalender,deine-region,ueber-uns,ueber-uns/archiv}/*.md; Q-014, Q-045 | high | The prototype does not go live (plan/guardrails.md); the hardening round resolves each clearance or removes the element | yes (jan-henrik / gtm: obtain written clearance per item) |
| 161 | **The slot-metadata comment cannot carry a clearance flag.** `SlotMetaSchema` is a `z.strictObject` over exactly `id`, `content_type`, `provenance`, `derived_from`, `status`, `demo` (`src/domain/content-frontmatter.schema.ts`), so `clearance: pending` in a slot comment fails `pnpm check:content`. `derived_from` likewise accepts only `<package>@<version>[#<id>]` and `ia` (`SOURCE_REF_PATTERN`) — there is no `gtm:<path>` form for unpackaged go-to-market-os material — and a plain HTML comment beside a slot is not ignored by the parser: it becomes a visible paragraph block. The group-2 pass therefore recorded every clearance and every source note in the page frontmatter's `open_points[]` and in row 160, which is prose, not machine-checkable | `src/domain/content-frontmatter.schema.ts`; `src/lib/content/slot-meta.ts`; `src/lib/content/source-refs.ts` | medium | Clearance stays a prose note until D12 row 4 (clearance re-validation) lands with the `RelevanceFacets` fragment. A `clearance` key on the slot comment plus a `gtm:<path>` ref form would make both checkable | yes (pipeline owner / spec session) |
| 162 | **Three page components hard-code demo labelling that now contradicts the artifact.** `app/[lang]/ueber-uns/archiv/page.tsx` passes `demo` to every `ArchiveRow` and renders a `DemoDataBadge` whenever any row exists; `app/[lang]/dein-kalender/page.tsx` passes `demo: true` plus the context line "Beispielhafte Rückmeldung" / "Example feedback" and a geo label "Beispiel"; `app/[lang]/deine-region/page.tsx` reads `demo` correctly via `isDemoSlot` but hard-codes `proofContext: "Beispielhafte Rückmeldung"` and `proofGeo: "Beispielregion"`. After the group-2 content pass those slots carry real, cited, clearance-pending material, so the rendered pages label real press coverage and real named quotes as example data. The error direction is conservative (it understates) and the prototype is preview-only, but it is wrong | `app/[lang]/ueber-uns/archiv/page.tsx`; `app/[lang]/dein-kalender/page.tsx`; `app/[lang]/deine-region/page.tsx` | medium | **Fixed (2026-09-12, M4 developer).** All three pages now derive `demo` from `isDemoSlot(slot)`, never a hard-coded prop: the archive's `demo`/`DemoDataBadge` come off `isDemoSlot(rowsDemo)` (currently `false` — 0 badges, 12 real outbound links wired from each row's own `url`, TS-028-A14/R-2); `/dein-kalender` and `/deine-region` read `contextLine`/`claim`/`attribution` off the artifact via the shared `parseDemoProofElement`, with the "Beispielhafte Rückmeldung"/"Beispiel…" wording now conditional on `isDemoSlot`, never fixed. The same fix was applied to `/mitmachen` (row 163 below, group 1) and to `/ueber-uns` block 3 (row 109: `DEMO_PROOF` replaced by the six real elements read off `ueber-uns-3-proof-stream`, reserved 7th slot left untouched per TS-027 A6/A7). Verified: `pnpm check` green, `pnpm e2e` (509 passed, 8 pre-existing skips), and a live crawl of all 24 routes shows the `Demo-Daten` badge only on the pre-existing, unrelated mocks (`embed-frame`, `envoy-form-mount`, `newsletter-block`, `live-module-frame`) — never on a proof card or archive row | no (M4 work, not a decision) |
| 163 | **`/dein-kalender`'s trust block renders only the data-protection subject.** `app/[lang]/dein-kalender/page.tsx` builds `subjects` from one hard-coded entry reading `fieldAt(trust.blocks, 1)`. Slot `dein-kalender-6-trust` now also carries a **sourced** operations sentence (row 19) that satisfies TS-024-A19, and it does not reach the page | `app/[lang]/dein-kalender/page.tsx`; TS-024 D10, A19 | medium | **Fixed (2026-09-12, M4 developer).** `subjects` now carries a second, `"operations"` entry (`TrustBlock`'s own pre-declared `TrustSubjectId`) reading `fieldAt(trust.blocks, 2)` — the sourced sentence naming `people@0.3.6#jan-henrik-hempel` and `proof@0.3.5#in-operation-since-2018` now renders under the label "Betrieb"/"Operations". The AI subject stays unpublished, unchanged, since no hub record exists for it yet. Verified live on `/dein-kalender`: the trust block now shows both the data-protection and the operations paragraph; `e2e/pages/dein-kalender.spec.ts` (including TS-024-A14) stays green | no (M4 work, not a decision) |
| 164 | **`media-echo@0.3.3` ships 31 entries, not 32.** Measured 2026-09-12 by counting `.media-echo.md` files in the package `index.json`. Row 1, the Phase-1 content map (TS-028 row 2) and several page artifacts quote 32. The clearance statement itself is unchanged: 0 of the 31 carry `usage_rights` | `node_modules/@schafe-vorm-fenster/media-echo/index.json`; state/open.md row 1; state/content-map.md TS-028 | low | The corrected count is used in the new archive artifact and in this list. Row 1's wording is left alone because it belongs to another workstream's history | no (a correction, not a decision) |
| 165 | **`content-pipeline.integration.test.ts` asserted a demo slot on `/ueber-uns/archiv`.** The archive has none any more (row 52), so the assertion was retargeted to `/dein-kalender`, whose AI-use placeholder is the one dummy-content slot left in this page group. If that placeholder is ever resolved (row 19) the test needs a new target, or a rewrite that scans every page and skips when no demo slot exists anywhere | `src/lib/content/content-pipeline.integration.test.ts` | low | Retargeted, with the reason in a comment above the assertion. A scan-all rewrite is the better shape but is a code decision this workstream did not take alone | no (M4 work, not a decision) |
| 166 | **`/dein-kalender/bestellen` had nothing to convert.** All five slots were already `sourced` against the IA and the TS-025 field-set determination; no slot carried `provenance: generated` or `demo: true`, and no hub record adds a claim the order flow is missing. Recorded so the next pass does not reopen the page looking for work | content/pages/dein-kalender/bestellen/{de,en}.md; state/content-map.md TS-025 | low | No change made | no |
| 179 | **Clearance sweep for the go-live hardening round.** The content follow-up (group 1) deliberately ships real proof and media-echo records whose `usage_rights` is `unverified` or absent, marked `clearance: pending` in the artifacts. Affected records: `kurzweg-baeckerei`, `zschiesche-gross-kiesow`, `eichler-wasserschloss-quilow`, `wendt-rubkow`, `volkshochschule-uecker-randow`, `lehre-lelender`, `zukunftswege-ost-newsletter`, `media-echo#2022-nordkurier-plattform-doerfer`, `media-echo#2026-04-nord-award-nordkurier`, `media-echo#2026-05-noerd-2026-rostock`, `media-echo#2026-08-abend-der-engagierten-lehre`, `media-echo#2024-09-kulturlandbuero-volkshochschule` | `content/pages/{home,dein-ort,dein-ort/starten,mitmachen}/**`; Q-014, Q-045, state/open.md #1 | high | Jan's instruction for this round: use the real material in the **protected preview** and clear it before go-live. Every affected slot carries a `clearance: pending` comment naming its records, and no such slot carries `demo: true`. The hardening round either obtains the written clearances or swaps the slot back to an empty state — it must not be discovered by reading the diff | yes (jan-henrik / gtm: obtain the clearances, or name which records may not go public) |
| 180 | **Five new content slots have no component reading them.** `home-12-ui-strings`, `dein-ort-0-state-s0`, `dein-ort-9-context-band`, `dein-ort-starten-7-context-band`, `registrieren-6-context-band` were written for open rows 92–95, but the content follow-up may not touch code, so the pages still render their hardcoded strings | `app/[lang]/page.tsx` `DEMO_LABELS`; `app/[lang]/dein-ort/page.tsx` `PAGE_COPY`; `app/[lang]/_page-frame.tsx` `BAND_HEADING`; `content/pages/**` | medium | **Bound (2026-09-12, M4 developer).** All five slots read, plus `mitmachen-9-context-band` (the sixth page carrying its own band kicker): `home-12-ui-strings` replaces `DEMO_LABELS`' four literals (`uiStringsFrom()`, `app/[lang]/page.tsx`) — the geo badge now reads "Beleg aus der Region", not "Beispiel"; `dein-ort-0-state-s0` supplies S0's own headline (`"Such deinen Ort, dann steht hier, was dort los ist."`, only when no place is resolved at all — state A/B keep `stateA`'s own template) plus the nearby heading, example badge and search hint; `dein-ort-starten-5/6`'s search hint and placeless CTA label are now read too (state/open.md row 94, same pass); the four context-band slots replace each page's reuse of `home-10-context-band` (`mitmachen`, `registrieren`) or the generic `_page-frame.tsx` fallback (`dein-ort`, `dein-ort/starten`) with the page's own kicker — the visible sentence is unchanged (byte-identical across pages today), so this is a provenance fix, not a copy change. Hardcoded fallback constants (`DEMO_LABELS`, `PAGE_COPY`) stay as the `slot()` empty-with-reason contract's own fallback, not deleted. Verified: `pnpm check` green, `pnpm e2e` (509 passed), live-crawled all 24 routes for `clearance:`/`source_note:` leakage (grep of rendered HTML) — none found, including on `/dein-ort`, whose `testimonial_pool` sibling comments (mid-slot, not before a heading) do reach `slot.blocks` as a stray paragraph but are never read (`fieldAt` only, no page maps over raw paragraphs there) | no (bound; the loader gap for mid-slot sibling comments is a separate finding — see the new row below) |
| 181 | **`/dein-ort`: TS-020-A6 forbids rendering the value-story testimonials that are now sourced.** A6 was written when every testimonial candidate was `unverified` and the artifact only held generated demo quotes. The four proof cards are now real, `provenance: sourced`, `clearance: pending` — so the rule and the artifact now disagree about the same four sentences | `app/[lang]/dein-ort/page.tsx`; TS-020-A6; content/pages/dein-ort/**; state/open.md #47 | medium | The content side did what this round asked for (real material, clearance pending, preview only) and did not touch the acceptance criterion, which is never reworded. The page still renders the stories three-part, so nothing uncleared reaches a screen today | yes (spec session: does A6 gate on `usage_rights: cleared` or on the protected preview) |
| 182 | **`/mitmachen`: two page-level decisions are now out of step with the artifact.** `page.tsx` hardcodes `demo: true` on every proof candidate of `mitmachen-7-proof-demo` (they are sourced now, so the `Demo-Daten` badge is wrong), and TS-022-A11 suppresses the permanence promise because "the 2022 commitment has no `proof/` element" — slot 8 now cites the offering record plus the media-echo entry instead | `app/[lang]/mitmachen/page.tsx`; TS-022-A11; content/pages/mitmachen/** | medium | **Badge half fixed (2026-09-12, M4 developer):** `demo` now comes off `isDemoSlot(proofDemo)` (`false` — the three quotes are sourced), and `contextLine`/geo label are read off the artifact via `parseDemoProofElement`, "Beispielhafte Rückmeldung"/"Beispiel" only when actually demo. Verified: `/mitmachen` carries no `Demo-Daten` badge on its proof cards in a live crawl; `e2e/pages/mitmachen.spec.ts` stays green. **A11's spec question is untouched** — still a spec-session decision, not resolved here | yes (spec session + page owner) |
| 183 | **The slot-metadata schema cannot carry `clearance` or `source_note`.** `SlotMetaSchema` is a `strictObject` over six keys, so the clearance state and the unpackaged-source locator this round needed are written as a **sibling HTML comment** directly above each `<!-- id: … -->` comment (where the parser drops them) rather than as metadata | `src/domain/content-frontmatter.schema.ts`; `src/lib/content/slot-meta.ts`; TS-007 D7 (`clearance` is a `RelevanceFacets` field that has not landed) | medium | Readable and checked-compatible, but not machine-readable: D12 check 4 (clearance re-validation) still cannot run, and `check:content` cannot tell a `clearance: pending` slot from any other sourced slot. Proposal: add `clearance` and `source_note` to `SlotMetaSchema` when D7's facets land | yes (pipeline owner) |
| 184 | **`derived_from` has no form for go-to-market-os material that is not packaged.** `SOURCE_REF_PATTERN` accepts `<package>@<version>#<record>`, `<package>@<version>` and `ia` only, so the communication principles, the relevance model and the IA page briefs — which are files in the sibling repo, not hub records — can only be cited as `ia` | `src/domain/content-frontmatter.schema.ts`; TS-007 D6; gtm:concept/*.concept.md | low | This round cites those documents as `ia` in `derived_from` and names the exact file and section in a `source_note:` comment, so a reader can find the source and a tool cannot. A `gtm:<path>` form (or packaging the three concept documents) would close it | yes (pipeline owner / gtm: package the concept documents, or extend the pattern) |
| 185 | **Four slot ids still end in `-demo` although their content is sourced.** `dein-ort-3-story-baeckerwagen-demo-testimonial`, `dein-ort-4-story-ratssitzung-demo-testimonial`, `dein-ort-5-story-kultur-demo`, `dein-ort-6-story-radius-demo-testimonial`, `mitmachen-7-proof-demo` | `content/pages/dein-ort/**`; `content/pages/mitmachen/**`; `app/[lang]/mitmachen/page.tsx` | low | Ids were deliberately left untouched: `mitmachen-7-proof-demo` is read by name in `app/[lang]/mitmachen/page.tsx`, and renaming ids in the same pass that changes their provenance would make the diff unreadable. Rename together with the code binding of row 161 | no (content/page owner, at the next binding) |
| 167 | **The org-profile intake in go-to-market-os holds no readable text.** `packages/identity/brand-identity/intake/org-profile-gdocs/` (company history, core messaging, funding, corporate data, market positioning, USP) consists of `.gdoc` pointer files — a JSON stub with a `doc_id`, no content — so nothing in it could be used as a source this round | gtm:packages/identity/brand-identity/intake/org-profile-gdocs/ | low | Only `values.md` and `tone-of-voice.md` have been worked out of that drop and into the package. Everything else is unreachable from a repository checkout, so any claim that would come from it (company history, funding figures, corporate data) has no locator and was not written | yes (jan-henrik / gtm: work the drop, or export the documents into the package) |
| 168 | **The loader drops a sibling `clearance:`/`source_note:` comment only when a heading sits between it and the next `<!-- id: ... -->` — a mid-slot placement leaks into that slot's own `body`/`blocks` as a stray paragraph.** `cutAtLastHeading` (`src/lib/content/loader.ts`) cuts a region at its *last* heading line, discarding everything from that heading on; a sibling comment placed directly above the next slot's id-comment (the documented convention, row 161/164) is always past such a heading and is dropped correctly. `content/pages/dein-ort/{de,en}.md` slots 3-6 instead carry a `<!-- testimonial_pool: [...]; clearance: pending (...) -->` comment *mid-slot* — after the "Testimonial:" field, before the slot's own closing prose and before the next heading — so `parseBlocks` turns it into a real `{kind: "paragraph"}` block that `slot.body` and `slot.blocks` both carry. Measured 2026-09-12 by parsing every page artifact and diffing `slot.body` against the two substrings; found in 4 slots x 2 locales, nowhere else | `src/lib/content/loader.ts` `cutAtLastHeading`; `content/pages/dein-ort/{de,en}.md` | low | Not visible today: `app/[lang]/dein-ort/page.tsx` reads these four slots through `fieldAt(story.blocks, 0)`/`fieldAt(story.blocks, 1)` only, which filters to `kind: "field"` and never touches the leaked paragraph — verified live, a crawl of all 24 routes for `clearance:`/`source_note:` in the rendered HTML found zero hits. It stays a latent trap for the next page that maps a slot's paragraphs generically. Two independent fixes would close it: move the comment to immediately precede the slot's own id-comment (impossible here, it annotates a field *inside* the slot, not the next one), or teach `parseBlocks`/the loader to strip any line matching `<!-- ... -->` outright, which the row 161 sibling-comment convention already assumes happens | yes (pipeline owner: strip HTML comment lines in `parseBlocks`, the safer general fix) |


Note (2026-09-12): duplicate row numbers from concurrent appends were renumbered — the M1 developer's rows that shared 17–26 with the content/PM rows are now 169–178; the second content agent's 160–166 are now 179–185. Same-day reports may cite the old numbers.