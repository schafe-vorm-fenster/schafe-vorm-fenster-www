# QA protocols

One protocol per QA run: `M<milestone>-run-<n>.md`. Contains: scope
(which TS ids / AC ids), per-criterion verdict (pass / fail →
finding id / not-testable → open-point id), and the regression-sweep
result. Written by QA, read by Project Manager and Customer. QA never
fixes — findings go to `state/findings/`.
