# M1 Gate — Customer Acceptance

Read first: `reports/qa/M1-run-1.md` (24 pass, 0 fail, 4 not-testable,
14 named out-of-scope, findings F-1-1/F-1-2/F-1-3, all low/medium, gate
recommendation "Clear"). This protocol is the customer's own judgment,
not a repeat of QA's run: it opens the preview, reads the delivered
repo state at the gate commit, and asks whether what stands satisfies
what was ordered for a foundation milestone — not whether every test
passed.

Scope: the M1-scoped acceptance criteria of TS-017 (technical
foundation), TS-015 (delivery pipeline), TS-014 (security baseline),
per `plan/project-plan.md`'s M1 row ("Gate scope: TS-017, TS-015 ACs
plus one deployed, reachable preview"; TS-014 named separately in the
M1 work-package table under "security headers + CSP scaffold").
Judged against the gate commit `9f47af4` ("test(qa): M1 gate run 1"),
since the working tree has already moved into M2 (files deleted/
modified) by the time this protocol was written.

## What I did

- Read the QA protocol and its findings/round-1 file in full.
- Opened the preview
  (`https://schafe-vorm-fenster-4veymgqid-schafe-vorm-fenster.vercel.app`)
  myself. `.env.local` now carries `VERCEL_AUTOMATION_BYPASS_SECRET`
  (state/open.md #14: generated mid-run, wasn't there when QA ran) —
  used it as `x-vercel-protection-bypass` and independently confirmed:
  unauthenticated request → `302` (Vercel SSO challenge); with the
  header → `200`; `X-Robots-Tag: noindex, nofollow`, the full D4
  header set and the exact D2 CSP string all present; body carries
  `<meta name="robots" content="noindex, nofollow"/>`; `/robots.txt`
  disallows all; visible copy is the labelled M1 placeholder
  ("Diese Seite ist ein Platzhalter aus M1 und trägt keine echten
  Inhalte. Prototyp · preview").
- Spot-read at the gate commit: `tsconfig.json` (`strict: true`),
  `stack.allow.json` (register vs. `package.json` `dependencies` —
  8/8 match, denySet present), `app/styles/brand.css` (single
  token-import file), `specs/decisions/072-the-foundation-stack.md`
  (the stack-harmony ADR), `vercel.json` (no header block — confirms
  half of TS-014-A2).
- Checked `next.schafe-vorm-fenster.de` myself: `200`, but serving a
  different product entirely (Astro "Community Calendar"), unprotected
  — confirms this is an external coordination gap, not this
  codebase's doing.

## TS-017 — Technical Foundation

| ID | Verdict | Reason |
| --- | --- | --- |
| A1 | **accepted** | Verified myself: every `package.json` dependency (8) has a `stack.allow.json` reason line citing a decision id; no register entry without a matching dependency; no deny-set hit. |
| A2 | **accepted** | Single lockfile, pinned `packageManager`, scoped `.npmrc` — per QA's file read, consistent with the pnpm-family stack DEC-072 documents. |
| A3 | **accepted** | Verified myself: root `tsconfig.json` has `strict: true`; `pnpm typecheck` is wired into `pnpm check`, which QA ran green. |
| A4 | **accepted** | Trust QA's evidence (grepped the *built* CSS, not just source: only `min-width` breakpoint-token values, no `max-width`) — a credible, reproducible method. |
| A5 | **accepted** | Verified myself: `app/styles/brand.css` is the only import point for tokens and fonts; the one literal colour pair QA flagged sits inside that same exempted file. |
| A6 | **accepted** | Trust QA's directory search; no font/logo file found. |
| A7 | **accepted** | `stack.allow.json`'s own reason line states "pinned exact"; `package.json`/lockfile spot-check is consistent. |
| A8, A9 | **accepted** | Named, per-AC Playwright tests (`pnpm e2e`, 9/9 green) — the naming discipline (`TS-017-A8`, `TS-017-A9` as literal test titles) gives confidence these check what they claim, not just "some e2e passed." |
| A10, A11 | **accepted (vacuous — flagged)** | True today only because no `/api/*` route and no handover builder exist yet; nothing yet could violate either rule. Accepted as a foundation-stage pass, but I expect this closed by an automated guard before M4 introduces the BFF routes and the handover module that would first be capable of violating it (QA's F-1-3 already tracks this — I am not adding a new finding, just naming that I read F-1-3 as load-bearing, not cosmetic). |
| A12 | **not in this gate — M2** | The header component (with the persistent calendar entry) is explicit M2 work per `plan/project-plan.md`; the M1 shell correctly ships wordmark-only. Would be a rejection if claimed done now; it isn't claimed. |
| A13 | **accepted** | `pnpm check` is the single gate (frontmatter → specs → stack → brand → typecheck → lint → test) and exits 0 at the gate commit — this is the "single gate" the foundation promised, and it exists, runs pre-commit (Husky), and is green. |
| A14 | **not in this gate — M3** | Content pipeline with the `TS-###` frontmatter field is explicit M3 work; `content/` is documented pre-relaunch archive, not a target of this AC yet. |
| A15 | **accepted (vacuous)** | No image ships yet — nothing to fail the imagery review against. |
| A16 | **accepted** | Read `specs/decisions/072-the-foundation-stack.md` myself: it tabulates the five sibling repos per concern (framework, test runner, e2e, lint, package manager) before deciding, and every `stack.allow.json` reason cites substance plus a decision id — exactly the "sibling pattern is intent, not code" discipline the guardrail demands, not "the sibling uses it." |
| A17 | **accepted** | One icon dependency (`lucide-react`); the only icon import found resolves to a real export. |

## TS-015 — Delivery Pipeline

| ID | Verdict | Reason |
| --- | --- | --- |
| A1 | **accepted** | Verified myself on the live preview: `X-Robots-Tag: noindex, nofollow`, the meta tag, and `robots.txt` disallow-all are all present together on this non-production deployment. |
| A2 | **accepted** | QA could not re-verify this (the bypass secret wasn't reachable that run); I could, since `.env.local` now carries it. Verified myself: unauthenticated → `302` (Vercel's own challenge), header present → `200`. Upgrading QA's "not-testable" to accepted on my own evidence. |
| A3–A5, A7–A10, A12 | **not in this gate — post-prototype** | No `.github/workflows/` exists; `plan/project-plan.md` places DEC-031's CI-pipeline stage 2 (required checks, canary promotion, auto-merge, Budgets job, branch-protection contexts) after the prototype, in no M1–M5 milestone as written. Nothing here was ordered for M1; nothing here is missing against M1's own work package ("Preview chain: `vercel deploy` … deployment protection respected, e2e smoke against the preview URL"). |
| A6 | **accepted** | `state/open.md` #14 records the e2e smoke ran green against the *protected* preview through the bypass header this run. I independently confirmed the preview serves the expected M1 placeholder content and headers through that same header — consistent with a real, not stubbed, smoke pass. |
| A11 | **not in this gate — blocked on external coordination** | Verified myself: `next.schafe-vorm-fenster.de` resolves and answers `200`, but serves a *different, unrelated product* (an Astro "Community Calendar" app), unprotected. TS-015's own open points name this exactly: the host's vacating by the current occupant is an external coordination point (DEC-035) with no date and no owning milestone. This is not a defect in what was delivered — there is nothing of this codebase to test at that host yet — but it is also not "done" in any sense a customer could accept today. Recorded here as unresolved and tracked, not accepted, not rejected as a build defect. |

## TS-014 — Security Baseline

| ID | Verdict | Reason |
| --- | --- | --- |
| A1 | **accepted, with a reservation carried forward** | Verified myself on the live preview: the CSP's `script-src`/`connect-src` list exactly the D1 hosts (etracker, portalize, envoy, app), no wildcard, no bare scheme, no `unsafe-inline`/`unsafe-eval` in the script directive. The rule is genuinely met today. My reservation: TS-014 D7 explicitly asks that this "fail in CI rather than in review," and no script in `pnpm check` enforces it yet (QA's F-1-2, medium) — today's correctness rests on one manual read, not a durable gate. I accept the criterion's substance now; I expect the automated guard closed before a later round re-asks this question, since the foundation's whole premise is one automated gate, not periodic manual re-reading. |
| A2 | **accepted** | Verified myself on the live preview: every D4 header present with the exact specified value (`X-Content-Type-Options`, `Referrer-Policy`, the full 20-feature `Permissions-Policy`, `X-Frame-Options`, `Cross-Origin-Opener-Policy`, `Cross-Origin-Resource-Policy`, `Reporting-Endpoints`) plus the full D2 CSP string; `vercel.json` carries no header block (verified myself — confirms the "no vercel.json header block" clause directly, not just by absence of contradiction). The `next.*` D5-preview-variant clause is not yet checkable — same external blocker as A11, not a defect here. |
| A3, A4, A8, A9, A10, A13 | **not in this gate — M2/M4** | Portalize embed, envoy widget, BFF routes and the logging facade are explicit M2/M4 deliverables; none exists yet, so none of these ACs has anything to check against. |
| A5, A11 | **not in this gate — post-prototype** | A5 requires "the production origin," which doesn't exist for this codebase (guardrail: preview only, production is Jan's, after the run). A11's `Quality` CI job shares TS-015's unscheduled CI-pipeline gap. |
| A6 | **not in this gate** | `www.schafe-vorm-fenster.de` still serves the pre-existing legacy production site — this repository's proxy/header logic has never run there. Nothing to accept or reject about this codebase yet. |
| A7 | **accepted** | Trust QA's grep across `app/`, `src/`, `content/`, config for `http://`/`//host`; the one hit is the dev-only `localhost` exemption D5 explicitly permits. |
| A12 | **not in this gate — M5/release** | No production deployment exists yet. |

## Findings I am not re-litigating

QA's F-1-1 (partial header assertion in the e2e smoke), F-1-2 (no
automated CSP guard — see TS-014-A1 above), and F-1-3 (no automated
guard for TS-017-A10/A11 — see above) are read, agreed with, and left
as QA's findings for the PM's round decision. I am not filing new
findings; I am naming, for two of them, that I read them as
load-bearing against the "single automated gate" promise rather than
cosmetic, which is why those two criteria carry a reservation instead
of a plain accept.

## Gate verdict: ACCEPTED

Every criterion genuinely in reach at M1 is accepted, with two
carrying a named reservation (TS-014-A1, TS-017-A10/A11 — real today,
not yet durably guarded) that does not by itself justify holding the
gate, since no criterion **fails** and no critical/high finding is
open (guardrail abort criterion not triggered). Two points are
recorded as unresolved rather than accepted or rejected because they
depend on something outside this codebase's control and outside any
scheduled milestone (TS-015-A11, and the matching half of
TS-014-A2's `next.*` clause) — both already carried in TS-015's own
open points; I add no new open-point entry, since none is missing.

What was ordered for a foundation milestone is there: one Next.js
App Router codebase, strict typing enforced by a single `pnpm check`
gate, a brand binding that runs through exactly one file, a
mobile-first shell proven at three widths by e2e, and a preview that
is genuinely protected (challenges unauthenticated access, opens with
the bypass header) and genuinely non-indexable (header, meta tag and
robots.txt all agree) — every one of these I confirmed myself against
the live artifact, not only against the QA report.

Accepted: 24 criteria (TS-017: 15; TS-015: 2; TS-014: 3, one with a
carried reservation) — TS-017-A1 through A11 and A13 through A17
(A12/A14 not in this gate), TS-015-A1/A2/A6, TS-014-A1/A2/A7.
Rejected: 0.
Not in this gate (named, with reason/milestone above): TS-017-A12
(M2), TS-017-A14 (M3), TS-015-A3/A4/A5/A7/A8/A9/A10/A12
(post-prototype), TS-015-A11 (external coordination, unscheduled),
TS-014-A3/A4/A8/A9/A10/A13 (M2/M4), TS-014-A5/A11 (post-prototype),
TS-014-A6 (no production deployment of this codebase yet), TS-014-A12
(M5/release).
